// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/kanpon/data-governance/internal/storage/ent/assetmetadata"
)

// AssetMetadata is the model entity for the AssetMetadata schema.
type AssetMetadata struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Asset holds the value of the "asset" field.
	Asset string `json:"asset,omitempty"`
	// ColumnName holds the value of the "column_name" field.
	ColumnName string `json:"column_name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Owner holds the value of the "owner" field.
	Owner string `json:"owner,omitempty"`
	// Tags holds the value of the "tags" field.
	Tags []string `json:"tags,omitempty"`
	// SetBy holds the value of the "set_by" field.
	SetBy uuid.UUID `json:"set_by,omitempty"`
	// SetAt holds the value of the "set_at" field.
	SetAt        time.Time `json:"set_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AssetMetadata) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case assetmetadata.FieldTags:
			values[i] = new([]byte)
		case assetmetadata.FieldAsset, assetmetadata.FieldColumnName, assetmetadata.FieldDescription, assetmetadata.FieldOwner:
			values[i] = new(sql.NullString)
		case assetmetadata.FieldSetAt:
			values[i] = new(sql.NullTime)
		case assetmetadata.FieldID, assetmetadata.FieldSetBy:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AssetMetadata fields.
func (am *AssetMetadata) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case assetmetadata.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				am.ID = *value
			}
		case assetmetadata.FieldAsset:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field asset", values[i])
			} else if value.Valid {
				am.Asset = value.String
			}
		case assetmetadata.FieldColumnName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field column_name", values[i])
			} else if value.Valid {
				am.ColumnName = value.String
			}
		case assetmetadata.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				am.Description = value.String
			}
		case assetmetadata.FieldOwner:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field owner", values[i])
			} else if value.Valid {
				am.Owner = value.String
			}
		case assetmetadata.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &am.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case assetmetadata.FieldSetBy:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field set_by", values[i])
			} else if value != nil {
				am.SetBy = *value
			}
		case assetmetadata.FieldSetAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field set_at", values[i])
			} else if value.Valid {
				am.SetAt = value.Time
			}
		default:
			am.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AssetMetadata.
// This includes values selected through modifiers, order, etc.
func (am *AssetMetadata) Value(name string) (ent.Value, error) {
	return am.selectValues.Get(name)
}

// Update returns a builder for updating this AssetMetadata.
// Note that you need to call AssetMetadata.Unwrap() before calling this method if this AssetMetadata
// was returned from a transaction, and the transaction was committed or rolled back.
func (am *AssetMetadata) Update() *AssetMetadataUpdateOne {
	return NewAssetMetadataClient(am.config).UpdateOne(am)
}

// Unwrap unwraps the AssetMetadata entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (am *AssetMetadata) Unwrap() *AssetMetadata {
	_tx, ok := am.config.driver.(*txDriver)
	if !ok {
		panic("ent: AssetMetadata is not a transactional entity")
	}
	am.config.driver = _tx.drv
	return am
}

// String implements the fmt.Stringer.
func (am *AssetMetadata) String() string {
	var builder strings.Builder
	builder.WriteString("AssetMetadata(")
	builder.WriteString(fmt.Sprintf("id=%v, ", am.ID))
	builder.WriteString("asset=")
	builder.WriteString(am.Asset)
	builder.WriteString(", ")
	builder.WriteString("column_name=")
	builder.WriteString(am.ColumnName)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(am.Description)
	builder.WriteString(", ")
	builder.WriteString("owner=")
	builder.WriteString(am.Owner)
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", am.Tags))
	builder.WriteString(", ")
	builder.WriteString("set_by=")
	builder.WriteString(fmt.Sprintf("%v", am.SetBy))
	builder.WriteString(", ")
	builder.WriteString("set_at=")
	builder.WriteString(am.SetAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AssetMetadataSlice is a parsable slice of AssetMetadata.
type AssetMetadataSlice []*AssetMetadata
