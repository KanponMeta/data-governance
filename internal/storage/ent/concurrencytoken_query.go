// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/kanpon/data-governance/internal/storage/ent/concurrencytoken"
	"github.com/kanpon/data-governance/internal/storage/ent/predicate"
)

// ConcurrencyTokenQuery is the builder for querying ConcurrencyToken entities.
type ConcurrencyTokenQuery struct {
	config
	ctx        *QueryContext
	order      []concurrencytoken.OrderOption
	inters     []Interceptor
	predicates []predicate.ConcurrencyToken
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ConcurrencyTokenQuery builder.
func (ctq *ConcurrencyTokenQuery) Where(ps ...predicate.ConcurrencyToken) *ConcurrencyTokenQuery {
	ctq.predicates = append(ctq.predicates, ps...)
	return ctq
}

// Limit the number of records to be returned by this query.
func (ctq *ConcurrencyTokenQuery) Limit(limit int) *ConcurrencyTokenQuery {
	ctq.ctx.Limit = &limit
	return ctq
}

// Offset to start from.
func (ctq *ConcurrencyTokenQuery) Offset(offset int) *ConcurrencyTokenQuery {
	ctq.ctx.Offset = &offset
	return ctq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ctq *ConcurrencyTokenQuery) Unique(unique bool) *ConcurrencyTokenQuery {
	ctq.ctx.Unique = &unique
	return ctq
}

// Order specifies how the records should be ordered.
func (ctq *ConcurrencyTokenQuery) Order(o ...concurrencytoken.OrderOption) *ConcurrencyTokenQuery {
	ctq.order = append(ctq.order, o...)
	return ctq
}

// First returns the first ConcurrencyToken entity from the query.
// Returns a *NotFoundError when no ConcurrencyToken was found.
func (ctq *ConcurrencyTokenQuery) First(ctx context.Context) (*ConcurrencyToken, error) {
	nodes, err := ctq.Limit(1).All(setContextOp(ctx, ctq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{concurrencytoken.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) FirstX(ctx context.Context) *ConcurrencyToken {
	node, err := ctq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ConcurrencyToken ID from the query.
// Returns a *NotFoundError when no ConcurrencyToken ID was found.
func (ctq *ConcurrencyTokenQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ctq.Limit(1).IDs(setContextOp(ctx, ctq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{concurrencytoken.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ctq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ConcurrencyToken entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ConcurrencyToken entity is found.
// Returns a *NotFoundError when no ConcurrencyToken entities are found.
func (ctq *ConcurrencyTokenQuery) Only(ctx context.Context) (*ConcurrencyToken, error) {
	nodes, err := ctq.Limit(2).All(setContextOp(ctx, ctq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{concurrencytoken.Label}
	default:
		return nil, &NotSingularError{concurrencytoken.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) OnlyX(ctx context.Context) *ConcurrencyToken {
	node, err := ctq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ConcurrencyToken ID in the query.
// Returns a *NotSingularError when more than one ConcurrencyToken ID is found.
// Returns a *NotFoundError when no entities are found.
func (ctq *ConcurrencyTokenQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ctq.Limit(2).IDs(setContextOp(ctx, ctq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{concurrencytoken.Label}
	default:
		err = &NotSingularError{concurrencytoken.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ctq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ConcurrencyTokens.
func (ctq *ConcurrencyTokenQuery) All(ctx context.Context) ([]*ConcurrencyToken, error) {
	ctx = setContextOp(ctx, ctq.ctx, ent.OpQueryAll)
	if err := ctq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ConcurrencyToken, *ConcurrencyTokenQuery]()
	return withInterceptors[[]*ConcurrencyToken](ctx, ctq, qr, ctq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) AllX(ctx context.Context) []*ConcurrencyToken {
	nodes, err := ctq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ConcurrencyToken IDs.
func (ctq *ConcurrencyTokenQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ctq.ctx.Unique == nil && ctq.path != nil {
		ctq.Unique(true)
	}
	ctx = setContextOp(ctx, ctq.ctx, ent.OpQueryIDs)
	if err = ctq.Select(concurrencytoken.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ctq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ctq *ConcurrencyTokenQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ctq.ctx, ent.OpQueryCount)
	if err := ctq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ctq, querierCount[*ConcurrencyTokenQuery](), ctq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) CountX(ctx context.Context) int {
	count, err := ctq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ctq *ConcurrencyTokenQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ctq.ctx, ent.OpQueryExist)
	switch _, err := ctq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ctq *ConcurrencyTokenQuery) ExistX(ctx context.Context) bool {
	exist, err := ctq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ConcurrencyTokenQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ctq *ConcurrencyTokenQuery) Clone() *ConcurrencyTokenQuery {
	if ctq == nil {
		return nil
	}
	return &ConcurrencyTokenQuery{
		config:     ctq.config,
		ctx:        ctq.ctx.Clone(),
		order:      append([]concurrencytoken.OrderOption{}, ctq.order...),
		inters:     append([]Interceptor{}, ctq.inters...),
		predicates: append([]predicate.ConcurrencyToken{}, ctq.predicates...),
		// clone intermediate query.
		sql:  ctq.sql.Clone(),
		path: ctq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RunID uuid.UUID `json:"run_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ConcurrencyToken.Query().
//		GroupBy(concurrencytoken.FieldRunID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ctq *ConcurrencyTokenQuery) GroupBy(field string, fields ...string) *ConcurrencyTokenGroupBy {
	ctq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ConcurrencyTokenGroupBy{build: ctq}
	grbuild.flds = &ctq.ctx.Fields
	grbuild.label = concurrencytoken.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RunID uuid.UUID `json:"run_id,omitempty"`
//	}
//
//	client.ConcurrencyToken.Query().
//		Select(concurrencytoken.FieldRunID).
//		Scan(ctx, &v)
func (ctq *ConcurrencyTokenQuery) Select(fields ...string) *ConcurrencyTokenSelect {
	ctq.ctx.Fields = append(ctq.ctx.Fields, fields...)
	sbuild := &ConcurrencyTokenSelect{ConcurrencyTokenQuery: ctq}
	sbuild.label = concurrencytoken.Label
	sbuild.flds, sbuild.scan = &ctq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ConcurrencyTokenSelect configured with the given aggregations.
func (ctq *ConcurrencyTokenQuery) Aggregate(fns ...AggregateFunc) *ConcurrencyTokenSelect {
	return ctq.Select().Aggregate(fns...)
}

func (ctq *ConcurrencyTokenQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ctq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ctq); err != nil {
				return err
			}
		}
	}
	for _, f := range ctq.ctx.Fields {
		if !concurrencytoken.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ctq.path != nil {
		prev, err := ctq.path(ctx)
		if err != nil {
			return err
		}
		ctq.sql = prev
	}
	return nil
}

func (ctq *ConcurrencyTokenQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ConcurrencyToken, error) {
	var (
		nodes = []*ConcurrencyToken{}
		_spec = ctq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ConcurrencyToken).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ConcurrencyToken{config: ctq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ctq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ctq *ConcurrencyTokenQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ctq.querySpec()
	_spec.Node.Columns = ctq.ctx.Fields
	if len(ctq.ctx.Fields) > 0 {
		_spec.Unique = ctq.ctx.Unique != nil && *ctq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ctq.driver, _spec)
}

func (ctq *ConcurrencyTokenQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(concurrencytoken.Table, concurrencytoken.Columns, sqlgraph.NewFieldSpec(concurrencytoken.FieldID, field.TypeUUID))
	_spec.From = ctq.sql
	if unique := ctq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ctq.path != nil {
		_spec.Unique = true
	}
	if fields := ctq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, concurrencytoken.FieldID)
		for i := range fields {
			if fields[i] != concurrencytoken.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ctq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ctq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ctq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ctq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ctq *ConcurrencyTokenQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ctq.driver.Dialect())
	t1 := builder.Table(concurrencytoken.Table)
	columns := ctq.ctx.Fields
	if len(columns) == 0 {
		columns = concurrencytoken.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ctq.sql != nil {
		selector = ctq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ctq.ctx.Unique != nil && *ctq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ctq.predicates {
		p(selector)
	}
	for _, p := range ctq.order {
		p(selector)
	}
	if offset := ctq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ctq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ConcurrencyTokenGroupBy is the group-by builder for ConcurrencyToken entities.
type ConcurrencyTokenGroupBy struct {
	selector
	build *ConcurrencyTokenQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ctgb *ConcurrencyTokenGroupBy) Aggregate(fns ...AggregateFunc) *ConcurrencyTokenGroupBy {
	ctgb.fns = append(ctgb.fns, fns...)
	return ctgb
}

// Scan applies the selector query and scans the result into the given value.
func (ctgb *ConcurrencyTokenGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ctgb.build.ctx, ent.OpQueryGroupBy)
	if err := ctgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConcurrencyTokenQuery, *ConcurrencyTokenGroupBy](ctx, ctgb.build, ctgb, ctgb.build.inters, v)
}

func (ctgb *ConcurrencyTokenGroupBy) sqlScan(ctx context.Context, root *ConcurrencyTokenQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ctgb.fns))
	for _, fn := range ctgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ctgb.flds)+len(ctgb.fns))
		for _, f := range *ctgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ctgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ctgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ConcurrencyTokenSelect is the builder for selecting fields of ConcurrencyToken entities.
type ConcurrencyTokenSelect struct {
	*ConcurrencyTokenQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cts *ConcurrencyTokenSelect) Aggregate(fns ...AggregateFunc) *ConcurrencyTokenSelect {
	cts.fns = append(cts.fns, fns...)
	return cts
}

// Scan applies the selector query and scans the result into the given value.
func (cts *ConcurrencyTokenSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cts.ctx, ent.OpQuerySelect)
	if err := cts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConcurrencyTokenQuery, *ConcurrencyTokenSelect](ctx, cts.ConcurrencyTokenQuery, cts, cts.inters, v)
}

func (cts *ConcurrencyTokenSelect) sqlScan(ctx context.Context, root *ConcurrencyTokenQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cts.fns))
	for _, fn := range cts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
