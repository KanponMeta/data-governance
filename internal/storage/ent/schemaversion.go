// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/kanpon/data-governance/internal/storage/ent/schemaversion"
)

// SchemaVersion is the model entity for the SchemaVersion schema.
type SchemaVersion struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Asset holds the value of the "asset" field.
	Asset string `json:"asset,omitempty"`
	// CodeHash holds the value of the "code_hash" field.
	CodeHash string `json:"code_hash,omitempty"`
	// SchemaHash holds the value of the "schema_hash" field.
	SchemaHash string `json:"schema_hash,omitempty"`
	// SchemaData holds the value of the "schema_data" field.
	SchemaData map[string]interface{} `json:"schema_data,omitempty"`
	// CapturedAt holds the value of the "captured_at" field.
	CapturedAt time.Time `json:"captured_at,omitempty"`
	// LastSeenAt holds the value of the "last_seen_at" field.
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// LastSeenRunID holds the value of the "last_seen_run_id" field.
	LastSeenRunID uuid.UUID `json:"last_seen_run_id,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SchemaVersion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case schemaversion.FieldSchemaData:
			values[i] = new([]byte)
		case schemaversion.FieldAsset, schemaversion.FieldCodeHash, schemaversion.FieldSchemaHash:
			values[i] = new(sql.NullString)
		case schemaversion.FieldCapturedAt, schemaversion.FieldLastSeenAt:
			values[i] = new(sql.NullTime)
		case schemaversion.FieldID, schemaversion.FieldLastSeenRunID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SchemaVersion fields.
func (sv *SchemaVersion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case schemaversion.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sv.ID = *value
			}
		case schemaversion.FieldAsset:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field asset", values[i])
			} else if value.Valid {
				sv.Asset = value.String
			}
		case schemaversion.FieldCodeHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code_hash", values[i])
			} else if value.Valid {
				sv.CodeHash = value.String
			}
		case schemaversion.FieldSchemaHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field schema_hash", values[i])
			} else if value.Valid {
				sv.SchemaHash = value.String
			}
		case schemaversion.FieldSchemaData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field schema_data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sv.SchemaData); err != nil {
					return fmt.Errorf("unmarshal field schema_data: %w", err)
				}
			}
		case schemaversion.FieldCapturedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field captured_at", values[i])
			} else if value.Valid {
				sv.CapturedAt = value.Time
			}
		case schemaversion.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				sv.LastSeenAt = value.Time
			}
		case schemaversion.FieldLastSeenRunID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_run_id", values[i])
			} else if value != nil {
				sv.LastSeenRunID = *value
			}
		default:
			sv.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SchemaVersion.
// This includes values selected through modifiers, order, etc.
func (sv *SchemaVersion) Value(name string) (ent.Value, error) {
	return sv.selectValues.Get(name)
}

// Update returns a builder for updating this SchemaVersion.
// Note that you need to call SchemaVersion.Unwrap() before calling this method if this SchemaVersion
// was returned from a transaction, and the transaction was committed or rolled back.
func (sv *SchemaVersion) Update() *SchemaVersionUpdateOne {
	return NewSchemaVersionClient(sv.config).UpdateOne(sv)
}

// Unwrap unwraps the SchemaVersion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sv *SchemaVersion) Unwrap() *SchemaVersion {
	_tx, ok := sv.config.driver.(*txDriver)
	if !ok {
		panic("ent: SchemaVersion is not a transactional entity")
	}
	sv.config.driver = _tx.drv
	return sv
}

// String implements the fmt.Stringer.
func (sv *SchemaVersion) String() string {
	var builder strings.Builder
	builder.WriteString("SchemaVersion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sv.ID))
	builder.WriteString("asset=")
	builder.WriteString(sv.Asset)
	builder.WriteString(", ")
	builder.WriteString("code_hash=")
	builder.WriteString(sv.CodeHash)
	builder.WriteString(", ")
	builder.WriteString("schema_hash=")
	builder.WriteString(sv.SchemaHash)
	builder.WriteString(", ")
	builder.WriteString("schema_data=")
	builder.WriteString(fmt.Sprintf("%v", sv.SchemaData))
	builder.WriteString(", ")
	builder.WriteString("captured_at=")
	builder.WriteString(sv.CapturedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(sv.LastSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_seen_run_id=")
	builder.WriteString(fmt.Sprintf("%v", sv.LastSeenRunID))
	builder.WriteByte(')')
	return builder.String()
}

// SchemaVersions is a parsable slice of SchemaVersion.
type SchemaVersions []*SchemaVersion
