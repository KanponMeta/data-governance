---
phase: 4
plan: 01
title: Wave 0 — 测试基础设施 + 迁移占位符
type: execute
wave: 1
depends_on: []
requirements: [LINE-01, LINE-02, LINE-03, LINE-06, META-01, META-02, META-03, META-05]
decisions_implemented: []
files_modified:
  - internal/lineage/lineagetest/fixtures.go
  - internal/lineage/lineagetest/recursive_cte_seed.go
  - internal/schema/schematest/fixtures.go
  - internal/runtime/executortest/lineage_helpers.go
  - migrations/20260509120000_phase4_lineage_schema.up.sql
  - migrations/atlas.sum
autonomous: true
must_haves:
  truths:
    - "Wave 0 fixture 包可编译 (`go build ./internal/lineage/lineagetest/... ./internal/schema/schematest/... ./internal/runtime/executortest/...`)"
    - "Schema A/B fixture 对覆盖所有 D-09 change_type (drop, add, narrow, widen, null-toggle, pk-change)"
    - "递归-CTE seeder 生成确定性的深度为 1, 5, 10, 25, 26 的 DAG"
    - "空的 Phase 4 迁移文件存在，文件名符合规范，使 Atlas 往返 + lint 在 Wave 1 填充前保持干净"
  artifacts:
    - path: "internal/lineage/lineagetest/fixtures.go"
      provides: "Asset 定义 → 预期的静态边 fixture (LINE-01 测试输入)"
      contains: "func StaticEdgeFixtures"
    - path: "internal/lineage/lineagetest/recursive_cte_seed.go"
      provides: "DAG seeder，具有深度控制，用于 LINE-03/LINE-06 CTE 测试"
      contains: "func SeedDAG"
    - path: "internal/schema/schematest/fixtures.go"
      provides: "connector.Schema A/B 对，覆盖所有 ChangeKind (META-02 测试输入)"
      contains: "func DiffPairs"
    - path: "internal/runtime/executortest/lineage_helpers.go"
      provides: "testcontainers PostgreSQL helper，加载 Phase 1–3 + Phase 4 迁移"
      contains: "func StartPhase4Container"
    - path: "migrations/20260509120000_phase4_lineage_schema.up.sql"
      provides: "空迁移占位符，使 Atlas 工具在 Wave 1 填充前看到该槽位"
      contains: "Phase 4 lineage + schema 迁移"
  key_links:
    - from: "internal/lineage/lineagetest/recursive_cte_seed.go SeedDAG"
      to: "PostgreSQL asset_edges 表 (Wave 1 将创建)"
      via: "通过原始 SQL 或 pgx batch 插入到 asset_edges"
      pattern: "INSERT INTO asset_edges"
    - from: "internal/runtime/executortest/lineage_helpers.go"
      to: "testcontainers/testcontainers-go postgres 模块"
      via: "RunContainer(ctx, postgres.WithDatabase(...))"
      pattern: "testcontainers.GenericContainer"
---

<objective>
交付所有 Phase 4 实现计划依赖的 Wave 0 共享测试基础设施。本计划交付：

1. **血缘 fixture** — asset 定义与预期的 `asset_edges` 和 `column_edges` 行配对，使 LINE-01/LINE-02 单元测试可以断言"写入器产生的内容"与真实值对比。
2. **Schema diff fixture** — `connector.Schema` 前后配对，覆盖 Wave 4 分类器将产生的每个 `ChangeKind` 枚举值。
3. **递归-CTE DAG seeder** — 在深度 1, 5, 10, 25, 26 时填充 `asset_edges`，以便 Wave 5 的 CTE 测试确认正确的深度上限 + DoS 保护 (D-14)。
4. **Executor testcontainers helper** — 启动 PostgreSQL 容器，应用 Phase 1–3 + Phase 4 迁移，返回可用于集成测试的 `*sql.DB`。
5. **空的 Phase 4 迁移存根** — 具有规范时间戳的占位符文件，使 Atlas 往返保持一致，直到 Wave 1 填充。

无业务逻辑。无生产代码。只有测试脚手架。

本计划解除对所有后续 Phase 4 计划的阻塞：Wave 1 需要迁移文件名槽位；Wave 3 需要 executor 容器 helper；Wave 4 需要 diff fixture；Wave 5 需要 CTE seeder。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
本计划不实现任何领域决策 —— 它是纯测试基础设施。Wave 0 fixture + helper 由 `.planning/phases/04-schema/04-VALIDATION.md` 规定，并在规划上下文的 `<wave_guidance>` 块中明确指出。

**为什么是 Wave 1（编号不是 Wave 0）：** Phase 3 使用 `wave: 1` 作为最低 wave。为保持跨阶段一致，Phase 4 从 `wave: 1` 开始 —— 但本计划必须在任何其他 Wave 1 计划之前完成（通过 `depends_on: []` 强制执行，本计划和实际实现 Wave 1 计划 04-02 的 `depends_on: ["04-01"]`）。阅读编排器 wave 逻辑，相同 wave 的计划仅在 `files_modified` 不重叠时并行运行；计划 04-02 修改 `migrations/20260509120000_phase4_lineage_schema.up.sql`（计划 04-01 创建为空存根），所以即使两者都标记为 wave 1，编排器也会通过文件重叠规则将 04-02 排在 04-01 之后。显式 `depends_on: ["04-01"]` 使顺序明确无误。

**stub 迁移的文件名约定：** `20260509120000_phase4_lineage_schema.up.sql` —— `20260509`（Phase 3 的 `20260508` 时间戳后一天，保持字典序正确），`120000` 匹配先前阶段的小时约定。`.up.sql` 后缀匹配项目现有文件使用的 `.sql`。检查 `ls migrations/` 后，`.sql` 是既定风格 —— 因此实际文件是 `20260509120000_phase4_lineage_schema.sql`。VALIDATION.md 中提到的 `*.up.sql` 是一般性 Atlas 约定；项目的真实约定是 `.sql`。**本计划创建 `migrations/20260509120000_phase4_lineage_schema.sql` 以匹配 Phase 1–3。**

**Schema A/B diff fixture（D-09 枚举覆盖）：** fixture 必须覆盖 Wave 4 分类器发出的每个值 —— `column_added`, `column_dropped`, `type_narrowed`, `type_widened`, `nullable_added`, `nullable_removed`, `pk_changed`, `comment_changed`, `default_changed`。Wave 4 的 TDD 测试按名称引用这些，缺失任何一个意味着 Wave 4 中测试失败。

**testcontainers helper 先例：** 仓库中不存在现有的 testcontainers helper（通过 `ls internal/runtime/` 验证）。Phase 4 引入此模式；Phase 5 将重用它。helper 接受进程内 `*storage.Storage` 接口 fixture 而不是原始 `*sql.DB`，使调用者与 pgx/database 内部保持解耦 —— 与 `internal/run/claim.go` 消费者相同的模式。

**冻结的接口消费：**
- `connector.Schema` 和 `connector.Column` (Wave 1 从现有 `connector.SchemaResponse` 升级；Wave 0 我们通过 build-tag 间接引用这些未来-Wave-1 类型，**或**使用本地 `schematest.Schema` 类型镜像计划的 `connector.Schema` 形状，Wave 4 替换为真实类型)。**本计划使用本地镜像方法**，因为 Wave 1 是并行的，我们无法导入尚不存在的类型。fixture 镜像结构体 `schematest.Schema` 携带相同字段（Columns []Column, PrimaryKey []string, RowCountEstim int64, CapturedAt time.Time），Wave 4 替换为 `connector.Schema`。
- `entgo.io/ent` 用于迁移工具（Phase 1 D-04 冻结）
- `github.com/testcontainers/testcontainers-go` v0.42.0（已在 go.mod —— 已验证）

@.planning/phases/04-schema/04-CONTEXT.md
@.planning/phases/04-schema/04-RESEARCH.md
@.planning/phases/04-schema/04-VALIDATION.md
@.planning/phases/03-scheduling-sensors-partitions/03-01-schema-events-foundation-PLAN.md
@migrations/20260508121000_phase3_schedules_sensors_backfills.sql

<interfaces>
<!-- Wave 0 fixture 的 connector.Schema 本地镜像（Wave 4 替换为真实的 connector.Schema）。-->

```go
// internal/schema/schematest/fixtures.go — 本地镜像结构体
package schematest

import "time"

type Column struct {
    Name         string
    Type         string  // connector-normalized: "int64", "varchar(255)", etc.
    Nullable     bool
    Default      *string
    IsPrimaryKey bool
    Comment      string
}

type Schema struct {
    Columns       []Column
    PrimaryKey    []string
    RowCountEstim int64
    CapturedAt    time.Time
}

// Wave 4 将替换为: connector.Schema (D-07)
```

<!-- go.mod 中现有的 testcontainers 模式：-->

```
github.com/testcontainers/testcontainers-go v0.42.0
github.com/testcontainers/testcontainers-go/modules/localstack v0.42.0
```

（尚无 postgres 模块；本计划将 `github.com/testcontainers/testcontainers-go/modules/postgres` 添加到 go.mod。）

<!-- 递归 CTE 测试的 DAG seeder 形状 (LINE-03 / LINE-06)：-->

```go
// internal/lineage/lineagetest/recursive_cte_seed.go
package lineagetest

import (
    "context"
    "database/sql"
)

// SeedDAG 在 PostgreSQL 中插入一条 asset_edges 链，使 asset_0 → asset_1 → ... → asset_depth。
// 返回拓扑排序的 asset 名称，以便调用者选择起始节点。
// 用于：
//   - LINE-03 TestRecursiveCTE — 深度 1, 5, 10
//   - LINE-06 TestImpactDepthCap — 深度 25（允许），26（拒绝）
func SeedDAG(ctx context.Context, db *sql.DB, depth int) ([]string, error)

// SeedBranching 在给定深度插入一个平衡二叉树（2^depth-1 个 assets，2^depth-2 条边）。
// 用于验证 CTE 处理扇出，而不仅仅是链。
func SeedBranching(ctx context.Context, db *sql.DB, depth int) ([]string, error)

// SeedCycle 插入 assets A→B→A，以便循环守卫测试验证路径数组的 NOT-contains 检查。
// 用于 TestCyclicLineageGuard。
func SeedCycle(ctx context.Context, db *sql.DB) (assetA, assetB string, err error)
```
</interfaces>
</context>

<tasks>

<task id="4.1.1" type="auto">
  <name>任务 1: 创建 lineage fixtures + DAG seeder + Schema diff fixtures（仅测试 Go 包）</name>
  <files>internal/lineage/lineagetest/fixtures.go, internal/lineage/lineagetest/recursive_cte_seed.go, internal/lineage/lineagetest/doc.go, internal/schema/schematest/fixtures.go, internal/schema/schematest/doc.go</files>
  <read_first>
    - .planning/phases/04-schema/04-VALIDATION.md (Wave 0 fixture 列表第 49–60 行)
    - .planning/phases/04-schema/04-CONTEXT.md § 实现决策 D-07, D-09, D-13（第 64–96 行）
    - .planning/phases/04-schema/04-RESEARCH.md § 第 §4 节 递归 CTE 遍历（第 217–263 行的示例查询）
    - .planning/phases/04-schema/04-RESEARCH.md § 第 §6 节 PostgreSQL 类型规范化表（第 766–786 行）
    - internal/connector/connector.go (现有的 connector.Column / connector.SchemaResponse — fixture 镜像应匹配未来 D-07 形状，而非当前 Phase 1 SchemaResponse)
  </read_first>
  <action>
    1. 创建 `internal/lineage/lineagetest/doc.go`，包含包文档：
       ```go
       // Package lineagetest provides shared fixtures for Phase 4 lineage tests.
       // Imported by: internal/lineage/*_test.go (Wave 3 lineage writer + Wave 5 impact analyzer).
       // No production code imports this package — it's test-only.
       package lineagetest
       ```
    2. 创建 `internal/lineage/lineagetest/fixtures.go`，包含：
       - 类型 `StaticEdgeCase struct { Asset string; Upstreams []string; ExpectedEdges []ExpectedEdge }`
       - 类型 `ExpectedEdge struct { From, To string }`
       - 函数 `func StaticEdgeFixtures() []StaticEdgeCase` 返回 4 个用例：
         a) 单个 asset，0 个上游 → 0 条边
         b) 单个 asset，1 个上游 → 1 条边 `{From: "u1", To: "self"}`
         c) 单个 asset，3 个上游 → 3 条边（Phase 2 builder 的 variadic Upstream(...)）
         d) 菱形：目标有 2 个共享共同祖先的上游 — 验证 builder DSL 不会传递去重（Phase 4 Wave 3 writer 负责）
       - 类型 `ColumnLineageCase struct { Asset string; ColumnLineage map[string][]ColumnRef; ExpectedRows []ColumnEdgeRow }`
       - 函数 `func ColumnLineageFixtures() []ColumnLineageCase` 返回 3 个用例（单列，扇入 2→1，扇出 1→2）
    3. 创建 `internal/lineage/lineagetest/recursive_cte_seed.go`，包含三个函数：
       - `func SeedDAG(ctx context.Context, db *sql.DB, depth int) ([]string, error)` — 使用单个 `pgx.CopyFrom` 或 `INSERT INTO asset_edges (...) VALUES (...), (...), ...` 批处理插入线性链 `asset_0 → asset_1 → ... → asset_<depth>` 到 `asset_edges`。每行设置 `code_hash_first = code_hash_latest = 'seed'`, `first_seen_run_id = last_seen_run_id = uuid.New()`, `first_seen_at = last_seen_at = time.Now().UTC()`, `superseded_at = NULL`。返回 asset 名称 `asset_0..asset_depth`。
       - `func SeedBranching(ctx context.Context, db *sql.DB, depth int) ([]string, error)` — 平衡二叉树。
       - `func SeedCycle(ctx context.Context, db *sql.DB) (string, string, error)` — 插入边 `cycle_a → cycle_b` 和 `cycle_b → cycle_a`。返回两个 asset 名称。
       - 所有三个函数都包含 `// NOTE: requires asset_edges table from Plan 04-02 migration to be applied. Skip with t.Skip() if Phase 4 migrations are not present.` 注释 + 早期检测检查 (`SELECT 1 FROM asset_edges LIMIT 0`)。
    4. 创建 `internal/schema/schematest/doc.go` 和 `internal/schema/schematest/fixtures.go`，包含：
       - 本地镜像类型 `Column` 和 `Schema`（与上方 `<interfaces>` 块完全一致，注释引用 D-07 + Wave-4 替换说明）。
       - 函数 `func DiffPairs() []DiffCase` 返回 9 个用例 —— 每个 `ChangeKind` 枚举值一个：
         a) `column_added` — schema A: 2 列；schema B: 3 列（添加了 `c3`）
         b) `column_dropped` — schema A: 3 列；schema B: 2 列（删除了 `c3`）
         c) `type_narrowed` — schema A: `c1 int64`；schema B: `c1 int32`
         d) `type_widened` — schema A: `c1 int32`；schema B: `c1 int64`
         e) `nullable_added` — schema A: `c1 NOT NULL`；schema B: `c1 NULLABLE`
         f) `nullable_removed` — schema A: `c1 NULLABLE`；schema B: `c1 NOT NULL`
         g) `pk_changed` — schema A: PK={`c1`}；schema B: PK={'c1','c2'}
         h) `comment_changed` — schema A: `c1 Comment="old"`；schema B: `c1 Comment="new"`
         i) `default_changed` — schema A: `c1 Default=ptr("0")`；schema B: `c1 Default=ptr("1")`
       - 每个 `DiffCase` 有字段 `{Name string; PrevSchema, NextSchema Schema; ExpectedChangeKind string; ExpectedIsBreaking bool}`。
       - 按 D-09：用例 (a) add, (d) widen, (e) null-add, (h) comment, (i) default → `ExpectedIsBreaking=false`。用例 (b) drop, (c) narrow, (f) null-remove, (g) pk-change → `ExpectedIsBreaking=true`。
    5. 运行 `go build ./internal/lineage/lineagetest/... ./internal/schema/schematest/...` 确认包可编译。
    6. 添加冒烟测试 `internal/lineage/lineagetest/fixtures_smoke_test.go`，调用 `StaticEdgeFixtures()` 和 `ColumnLineageFixtures()` 并断言 `len(...) > 0`，验证 fixture 函数返回非空切片。（无需 DB。）
    7. 添加冒烟测试 `internal/schema/schematest/fixtures_smoke_test.go`，调用 `DiffPairs()` 并断言 `len(pairs) == 9`（每个 ChangeKind 一个）。
  </action>
  <acceptance_criteria>
    - `go build ./internal/lineage/lineagetest/... ./internal/schema/schematest/...` 退出 0
    - 文件 `internal/lineage/lineagetest/fixtures.go` 存在；`grep -q 'func StaticEdgeFixtures' internal/lineage/lineagetest/fixtures.go`
    - `grep -q 'func ColumnLineageFixtures' internal/lineage/lineagetest/fixtures.go`
    - 文件 `internal/lineage/lineagetest/recursive_cte_seed.go` 存在；`grep -q 'func SeedDAG' internal/lineage/lineagetest/recursive_cte_seed.go`
    - `grep -q 'func SeedBranching' internal/lineage/lineagetest/recursive_cte_seed.go`
    - `grep -q 'func SeedCycle' internal/lineage/lineagetest/recursive_cte_seed.go`
    - 文件 `internal/schema/schematest/fixtures.go` 存在；`grep -q 'func DiffPairs' internal/schema/schematest/fixtures.go`
    - `grep -c 'Name:.*"column_added"\|Name:.*"column_dropped"\|Name:.*"type_narrowed"\|Name:.*"type_widened"\|Name:.*"nullable_added"\|Name:.*"nullable_removed"\|Name:.*"pk_changed"\|Name:.*"comment_changed"\|Name:.*"default_changed"' internal/schema/schematest/fixtures.go` 返回 9
    - `go test ./internal/lineage/lineagetest/... ./internal/schema/schematest/... -run Smoke -count=1 -timeout 30s` 退出 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./internal/lineage/lineagetest/... ./internal/schema/schematest/... && go test ./internal/lineage/lineagetest/... ./internal/schema/schematest/... -run Smoke -count=1 -timeout 30s</automated>
  </verify>
  <done>三个仅测试 Go 包可编译，包含命名的 fixture/seeder 函数，冒烟测试通过，所有 9 个 ChangeKind 用例已枚举。</done>
</task>

<task id="4.1.2" type="auto">
  <name>任务 2: testcontainers PostgreSQL executor helper + 空的 Phase 4 迁移存根</name>
  <files>internal/runtime/executortest/lineage_helpers.go, internal/runtime/executortest/doc.go, migrations/20260509120000_phase4_lineage_schema.sql, go.mod, go.sum</files>
  <read_first>
    - .planning/phases/04-schema/04-VALIDATION.md (Wave 0 列表第 49–60 行)
    - .planning/phases/04-schema/04-RESEARCH.md § 验证架构（第 1148–1216 行）
    - go.mod (当前的 testcontainers-go v0.42.0 行 + localstack 模块 — 添加 postgres 模块的模式)
    - migrations/20260508121000_phase3_schedules_sensors_backfills.sql (Phase 3 迁移文件名 + 结构模式)
    - migrations/atlas.sum (Atlas 哈希文件格式)
    - Makefile (`integration` 目标展示现有的 testcontainers + migrate-apply 模式)
  </read_first>
  <action>
    1. 将 testcontainers postgres 模块添加到 go.mod：
       ```bash
       go get github.com/testcontainers/testcontainers-go/modules/postgres@v0.42.0
       go mod tidy
       ```
    2. 创建 `internal/runtime/executortest/doc.go`：
       ```go
       // Package executortest provides shared testcontainers helpers for Phase 4
       // executor + lineage + schema integration tests.
       // No production code imports this package — it's test-only.
       package executortest
       ```
    3. 创建 `internal/runtime/executortest/lineage_helpers.go`，包含：
       - `type Phase4Container struct { *sql.DB; container testcontainers.Container; URL string }`
       - `func StartPhase4Container(ctx context.Context, t testing.TB) *Phase4Container`，该函数：
         a) 如果 `os.Getenv("CI_NO_DOCKER") == "1"` 或 Docker socket 不可达，则使用 `t.Skip("integration test: requires Docker")` 跳过。
         b) 调用 `postgres.Run(ctx, "postgres:16", postgres.WithDatabase("platform"), postgres.WithUsername("platform_app"), postgres.WithPassword("platform_app"), testcontainers.WithWaitStrategy(wait.ForLog("database system is ready").WithOccurrence(2).WithStartupTimeout(60*time.Second)))`。
         c) 通过 `container.ConnectionString(ctx, "sslmode=disable")` 读取连接字符串。
         d) 通过 `sql.Open("pgx", dsn)` 打开 `*sql.DB`。
         e) 通过原始 SQL 创建 `platform_app` 和 `platform_owner` 角色 + 授权（镜像 `migrations/20260506062521_initial.sql` 第 49–58 行）。
         f) 迭代迁移目录（按字典序排序）并通过 `db.ExecContext` 应用每个 `.sql` 文件。这复制了 Atlas 行为，无需在测试容器内使用 Atlas 二进制文件。跳过匹配 `*.down.sql` 的文件（目前没有，但有防御性）。
         g) 注册 `t.Cleanup(func() { container.Terminate(context.Background()); db.Close() })`，以便即使测试失败也能拆除容器。
         h) 返回 `&Phase4Container{DB: db, container: container, URL: dsn}`。
       - `func (c *Phase4Container) Reset(ctx context.Context, t testing.TB)`，该函数在测试用例之间 TRUNCATEs 所有 Phase 4 表（`asset_edges`, `column_edges`, `schema_versions`, `schema_changes`, `asset_versions`, `asset_metadata`）以及 `event_log`, `runs`, `run_steps`。使用 `TRUNCATE ... RESTART IDENTITY CASCADE`。
       - 函数必须容忍 Phase 4 表尚不存在（Wave 1 才创建它们）：将每个 TRUNCATE 包装在 `EXECUTE 'TRUNCATE asset_edges ...'` 中的 `DO $$ BEGIN ... EXCEPTION WHEN undefined_table THEN NULL; END $$` 块中。这样 Wave 0（无 Phase 4 迁移内容）不会导致 helper 崩溃。
    4. 创建 `migrations/20260509120000_phase4_lineage_schema.sql` 作为空存根：
       ```sql
       -- Phase 4 lineage + schema migration (Wave 1 plan 04-02 fills this in).
       -- This file is intentionally empty in Wave 0; the filename slot is reserved so
       -- Atlas roundtrip + lint checks see a known migration timestamp before plan 04-02
       -- adds CREATE TABLE statements for asset_edges, column_edges, schema_versions,
       -- schema_changes, asset_versions, asset_metadata.
       --
       -- Wave 0 reservation: see .planning/phases/04-schema/04-VALIDATION.md "Wave 0 Requirements".
       SELECT 1; -- atlas migrate lint requires at least one statement; SELECT is a no-op DDL
       ```
    5. 运行 `atlas migrate hash --env local` 更新 `migrations/atlas.sum`，记录空存根。
    6. 运行 `make migrate-lint` 确认 Atlas 接受存根且无警告。
    7. 添加集成冒烟测试 `internal/runtime/executortest/lineage_helpers_smoke_test.go`（build tag `integration`），该测试：
       - 调用 `StartPhase4Container(ctx, t)`。
       - 断言 `db.PingContext(ctx)` 成功。
       - 断言 `db.QueryRowContext(ctx, "SELECT current_database()").Scan(&dbname)` 返回 `"platform"`。
       - 断言 `db.QueryRowContext(ctx, "SELECT current_user").Scan(&user)` 返回 `"platform_app"`（或 helper 使用的任何用户 — 只要非空）。
       - 断言查询 `event_log`（Phase 1 表）在应用迁移后返回 0 行。
  </action>
  <acceptance_criteria>
    - `grep -q 'github.com/testcontainers/testcontainers-go/modules/postgres' go.mod`
    - 文件 `internal/runtime/executortest/lineage_helpers.go` 存在；`grep -q 'func StartPhase4Container' internal/runtime/executortest/lineage_helpers.go`
    - `grep -q 'func.*Reset' internal/runtime/executortest/lineage_helpers.go`
    - 文件 `migrations/20260509120000_phase4_lineage_schema.sql` 存在
    - `wc -l < migrations/20260509120000_phase4_lineage_schema.sql` 返回 ≤ 12（小存根）
    - `grep -q 'Phase 4 lineage + schema migration' migrations/20260509120000_phase4_lineage_schema.sql`
    - `make migrate-lint` 退出 0
    - `migrations/atlas.sum` 在 `atlas migrate hash --env local` 后被修改
    - `go build ./internal/runtime/executortest/...` 退出 0
    - `go test ./internal/runtime/executortest/... -tags=integration -run Smoke -count=1 -timeout 180s` 退出 0（当 Docker 可用时 — CI_NO_DOCKER=1 跳过）
  </acceptance_criteria>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./internal/runtime/executortest/... && make migrate-lint && grep -c 'Phase 4 lineage + schema migration' migrations/20260509120000_phase4_lineage_schema.sql</automated>
  </verify>
  <done>testcontainers helper 可编译，打开 PostgreSQL 容器，应用迁移，当 Docker 可用时冒烟测试通过；空的 Phase 4 迁移存根 lint 干净且 atlas.sum 已重新生成。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|----------|-------------|
| 测试代码 → CI Docker socket | testcontainers 生成容器；信任边界是开发者的本地 Docker 守护进程（不暴露于互联网） |
| 测试 fixture → 应用代码 | Wave 0 fixture 类型是本地镜像；Wave 4 替换为真实 `connector.Schema` 是类型替换，而非运行时注入 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 严重性 | 处理方式 | 缓解计划 |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | 篡改 | Schema A/B fixture 对 | 低 | 接受 | 仅测试数据，从不针对生产执行。无运行时暴露。 |
| T-04-01-02 | DoS | CI 中 testcontainers 容器泄漏 | 中 | 缓解 | `t.Cleanup(func() { container.Terminate(...) })` 在 `StartPhase4Container` 中确保测试失败或 panic 时拆除。冒烟测试验证 Cleanup hook 运行。 |
| T-04-01-03 | 信息泄露 | 测试输出中记录的 DB 连接字符串 | 低 | 接受 | testcontainers 每次运行写入随机凭证；无共享/持久 secrets。CI 日志仅内部可见。 |
| T-04-01-04 | 篡改 | Wave 1 填充时空迁移存根变为真实迁移 | 低 | 缓解 | atlas.sum 在每次更改后重新生成；CI `make migrate-lint` 阻止具有过时 sum 的合并。Wave 1 计划 04-02 明确在编辑后重新运行 `atlas migrate hash`。 |
| T-04-01-05 | DoS | 递归 CTE seeder depth=26 产生无限插入 | 低 | 缓解 | 调用者控制 `depth int` 参数；SeedDAG 签名由 Wave 5 测试传递 depth ≤ 26 限制。无运行时 DoS 向量 —— 仅测试代码。 |
</threat_model>

<verification>
- `go build ./internal/lineage/lineagetest/... ./internal/schema/schematest/... ./internal/runtime/executortest/...` 退出 0。
- `go test ./internal/lineage/lineagetest/... ./internal/schema/schematest/... -run Smoke -count=1 -timeout 30s` 退出 0。
- `make migrate-lint` 退出 0，空的 Phase 4 存根在位。
- `migrations/atlas.sum` 使用新存根的哈希更新。
- （Docker 可用的环境）`go test ./internal/runtime/executortest/... -tags=integration -run Smoke -count=1 -timeout 180s` 退出 0。
- `grep -c 'expected_change_kind\|ExpectedChangeKind' internal/schema/schematest/fixtures.go` ≥ 9（每个变更类型一个）。
- 所有 Wave 0 VALIDATION.md 要点已解决：lineagetest fixtures ✓, schematest fixtures ✓, recursive_cte_seed ✓, executortest helper ✓, 迁移存根 ✓。
</verification>

<success_criteria>
- 三个仅测试 Go 包编译干净。
- 所有 9 个 D-09 ChangeKind 值在 `DiffPairs()` 中都有 fixture 对。
- DAG seeder 支持线性链、分支树和自循环。
- testcontainers helper 启动 PostgreSQL，应用所有 Phase 1–3 + Phase 4 迁移，返回就绪的 `*sql.DB`。
- 空的 Phase 4 迁移存根 lint 干净；atlas.sum 已更新。
- 非 Docker 工作流的冒烟测试通过；当 Docker 可用时集成冒烟测试通过。
</success_criteria>

<output>
完成后，创建 `.planning/phases/04-schema/04-01-SUMMARY.md` 记录：
- 所有 fixture/helper 包 的最终文件名 + 结构体名称
- Wave 0 → Wave 1 契约（哪个下游计划读取哪个 fixture）
- 与 VALIDATION.md 的任何偏差（例如，`.up.sql` → `.sql` 文件名约定调整）
- 确认：`make migrate-lint` 干净，`make migrate-status` 显示存根为 Wave 1 填充前的最新迁移。
</output>
