---
phase: "06-web-ui-api"
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - migrations/YYYYMMDDHHMMSS_add_fts.sql
  - internal/storage/ent/schema/asset.go
  - internal/storage/ent/schema/column_edge.go
  - internal/api/search_handlers.go
  - internal/api/connect_search.go
  - internal/api/router.go
  - web/src/pages/catalog/index.tsx
  - web/src/components/SearchBar.tsx
  - web/src/components/SearchResult.tsx
  - web/src/components/TagFilter.tsx
  - web/src/components/OwnerSelect.tsx
  - web/src/router.tsx
autonomous: true
requirements: [META-04, UI-03]
user_setup: []

must_haves:
  truths:
    - "User can search catalog by asset name, column name, tag, owner, or description (META-04)"
    - "User can filter catalog results by tag (click tag chip to filter)"
    - "User can filter catalog results by owner (dropdown select)"
    - "Search endpoint GET /v1/catalog/search supports ?tag= and ?owner= params alongside ?q="
    - "Search results ranked by relevance with highlighting on matched terms"
    - "Results include type (asset/column), name, description, tags, owner, last_modified"
    - "Tag filter chips are clickable; selected tag shows as active chip"
    - "Owner filter is a searchable dropdown"
    - "Catalog page polls at 60s interval (D-17 cold screen)"
  artifacts:
    - path: "migrations/YYYYMMDDHHMMSS_add_fts.sql"
      provides: "tsvector column + GIN index on asset and column tables"
      contains: "CREATE INDEX CONCURRENTLY.*gin.*tsvector"
    - path: "internal/api/search_handlers.go"
      provides: "Search REST handler with FTS query and tag/owner filters"
      contains: "tsvector, plainto_tsquery, tag, owner, plainto_tsquery"
    - path: "web/src/pages/catalog/index.tsx"
      provides: "Catalog browse page with search, tag filter chips, owner dropdown"
      contains: "GET /v1/catalog/search", "TagFilter", "OwnerSelect"
    - path: "web/src/components/TagFilter.tsx"
      provides: "Clickable tag chips for filtering catalog by tag"
      contains: "tag chips, onClick filter, active state"
    - path: "web/src/components/OwnerSelect.tsx"
      provides: "Searchable owner dropdown for filtering catalog by owner"
      contains: "owner dropdown, searchable select"

---

<objective>
Implement catalog search (META-04) and catalog browsing with tag/owner filtering (UI-03). Add tsvector columns + GIN indexes to asset and column tables. Build search REST API endpoint supporting ?q=, ?tag=, ?owner= params. Build React catalog browse page with search bar, tag filter chips, and owner dropdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-web-ui-api/06-CONTEXT.md
@.planning/phases/06-web-ui-api/06-01-PLAN.md

## Search contract (D-09, extended for UI-03)
GET /v1/catalog/search?q=<query>&type=<asset|column|table>&tag=<tag>&owner=<owner>&page=<n>
Returns ranked results with highlighting. Filter by tag and/or owner alongside text query.
Single endpoint, no faceted search in v1.

## UI-03 requirement
"User can browse data catalog, support searching, filtering by tags/owner, view asset/column metadata"
- Tag filter: clickable chips, single-select, shows all available tags from catalog
- Owner filter: dropdown select, populated from existing asset owners
</context>

<tasks>

<task type="auto">
  <name>Task 1: Postgres FTS migration and search handler with tag/owner filter support</name>
  <files>migrations/YYYYMMDDHHMMSS_add_fts.sql, internal/api/search_handlers.go, internal/api/connect_search.go, internal/api/router.go</files>
  <read_first>internal/storage/ent/schema/asset.go, internal/storage/ent/schema/column_edge.go, internal/api/router.go</read_first>
  <behavior>
    - Migration adds tsvector GENERATED column to asset_versions and column_edges tables
    - Migration creates GIN index CONCURRENTLY (non-blocking)
    - Migration adds trigger to keep tsvector updated on INSERT/UPDATE
    - Search handler queries asset_versions + column_edges using plainto_tsquery
    - Search handler accepts ?tag=<tag> and ?owner=<owner> filter params
    - Tag filter: WHERE tags @> ARRAY[$tag] (Postgres array contains)
    - Owner filter: WHERE owner = $owner
  </behavior>
  <action>
Create migration file. Use Atlas or a raw SQL migration file in migrations/. The exact filename should match the existing migration naming pattern. Check what exists in migrations/ first:

```bash
ls migrations/*.sql | head -5
```

For this plan, create a raw SQL migration file (Phase 4 used Atlas + ent migrations, but the migration system should be compatible with whatever exists). If Phase 1-4 used ent/Atlas, use Atlas to generate the migration:

```bash
atlas migrate diff add_fts_search \
  --env local \
  --to="sql(ALTER TABLE asset_versions ADD COLUMN search_vector tsvector GENERATED ALWAYS AS (
    setweight(to_tsvector('english', coalesce(name, '')), 'A') ||
    setweight(to_tsvector('english', coalesce(description, '')), 'B') ||
    setweight(to_tsvector('english', coalesce(owner, '')), 'C') ||
    setweight(to_tsvector('english', coalesce(array_to_string(tags, ' '), '')), 'C')
  ) STORED);
  CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_asset_versions_fts ON asset_versions USING gin(search_vector);)"
```

If Atlas is not set up, create the migration manually in migrations/ as a raw SQL file following the existing pattern.

The FTS strategy:
1. Add `search_vector tsvector GENERATED ALWAYS AS (...) STORED` to asset_versions
2. Add similar to column_edges (searchable on column name, description, table name)
3. Create GIN index CONCURRENTLY
4. Add trigger or application-level update for search_vector on INSERT/UPDATE

In internal/api/search_handlers.go, extend the searchHandler to accept tag and owner filter params:

```go
package api

import (
    "context"
    "errors"
    "net/http"
    "strings"
    "time"

    "github.com/kanpon/data-governance/internal/storage/ent"
    "github.com/kanpon/data-governance/internal/storage/ent/assetversion"
    "github.com/kanpon/data-governance/internal/storage/ent/columnege"
)

// SearchResult represents a single search result item.
type SearchResult struct {
    Type        string   `json:"type"` // "asset", "column"
    Name        string   `json:"name"`
    Description string   `json:"description"`
    Owner       string   `json:"owner"`
    Tags        []string `json:"tags"`
    AssetName   string   `json:"asset_name"` // for columns: parent asset
    Highlight   string   `json:"highlight"`
}

// SearchResponse is GET /v1/catalog/search response.
type SearchResponse struct {
    Results  []SearchResult `json:"results"`
    Total    int            `json:"total"`
    Page     int            `json:"page"`
    Query    string         `json:"query"`
    Tags     []string       `json:"tags"`     // all available tags for filter chips
    Owners   []string       `json:"owners"`   // all available owners for dropdown
}

// searchHandler handles GET /v1/catalog/search.
// Query params: q (optional), type (optional: asset|column|table), tag (optional), owner (optional), page (optional, default 1).
// When q is empty but tag/owner is set, performs browse with filters (UI-03).
// When q is set, performs FTS search with filters (META-04).
func searchHandler(deps Deps) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        query := strings.TrimSpace(r.URL.Query().Get("q"))
        tag := strings.TrimSpace(r.URL.Query().Get("tag"))
        owner := strings.TrimSpace(r.URL.Query().Get("owner"))
        searchType := r.URL.Query().Get("type") // asset, column, or empty (all)
        page := 1
        if p := r.URL.Query().Get("page"); p != "" {
            if v := parsePositiveInt(p); v > 0 {
                page = v
            }
        }
        pageSize := 20
        offset := (page - 1) * pageSize

        ctx, cancel := context.WithTimeout(r.Context(), 5*time.Second)
        defer cancel()

        results, total, tags, owners, err := performSearch(ctx, deps, query, tag, owner, searchType, pageSize, offset)
        if err != nil {
            InternalServerError(w, err.Error())
            return
        }

        writeJSONResponse(w, http.StatusOK, SearchResponse{
            Results: results,
            Total:   total,
            Page:    page,
            Query:   query,
            Tags:    tags,
            Owners:  owners,
        })
    }
}

// performSearch executes FTS query with optional tag/owner filters.
// When query is empty: browse mode (UI-03) - return all assets/columns matching tag/owner filters
// When query is set: search mode (META-04) - FTS search with tag/owner filters applied
//
// FTS Query Pattern (raw pgx or ent raw SQL):
//
// Asset subquery (for type=asset or type=all):
//   SELECT name, description, owner, tags, NULL::text as asset_name,
//          ts_rank(search_vector, plainto_tsquery('english', $1)) as rank,
//          ts_headline('english', name || ' ' || coalesce(description, ''), plainto_tsquery('english', $1)) as highlight,
//          'asset' as type
//   FROM asset_versions
//   WHERE (search_vector @@ plainto_tsquery('english', $1) OR $1 = '')
//     AND superseded_at IS NULL
//     AND ($2 = '' OR tags @> ARRAY[$2])    -- tag filter
//     AND ($3 = '' OR owner = $3)           -- owner filter
//
// Column subquery (for type=column or type=all, join with asset for asset_name):
//   SELECT col.name, col.description, col.owner, col.tags, a.name as asset_name,
//          ts_rank(col.search_vector, plainto_tsquery('english', $1)) as rank,
//          ts_headline('english', col.name || ' ' || coalesce(col.description, ''), plainto_tsquery('english', $1)) as highlight,
//          'column' as type
//   FROM column_edges col
//   JOIN asset_versions a ON col.asset_id = a.id
//   WHERE (col.search_vector @@ plainto_tsquery('english', $1) OR $1 = '')
//     AND col.superseded_at IS NULL
//     AND a.superseded_at IS NULL
//     AND ($2 = '' OR col.tags @> ARRAY[$2])
//     AND ($3 = '' OR col.owner = $3)
//
// Combined query with UNION ALL, ordered by rank DESC, with LIMIT/OFFSET:
//   SELECT * FROM (<asset_subquery> UNION ALL <column_subquery>) AS combined
//   ORDER BY rank DESC
//   LIMIT $4 OFFSET $5
//
// Total count (separate query without LIMIT/OFFSET):
//   SELECT COUNT(*) FROM (<asset_subquery> UNION ALL <column_subquery>) AS combined
//
// With type filter: subquery includes "AND false" condition when type doesn't match
//
// Tag aggregation (for filter chips):
//   SELECT DISTINCT unnest(tags) FROM asset_versions WHERE superseded_at IS NULL ORDER BY 1
//
// Owner aggregation (for dropdown):
//   SELECT DISTINCT owner FROM asset_versions WHERE superseded_at IS NULL AND owner != '' ORDER BY owner

func performSearch(ctx context.Context, deps Deps, query, tag, owner, searchType string, limit, offset int) ([]SearchResult, int, []string, []string, error) {
    // Validate search type
    if searchType != "" && searchType != "asset" && searchType != "column" && searchType != "table" {
        return nil, 0, nil, nil, errors.New("invalid search type: must be empty, asset, column, or table")
    }

    // Build the FTS query using raw pgx (deps.Ent.DB() provides *sqlx.DB)
    // The exact SQL is documented in the behavior block above
    //
    // Key filter logic:
    // - $1 = query text (empty string if browse mode, plainto_tsquery('english', '') returns nothing)
    // - $2 = tag filter (empty string means no tag filter)
    // - $3 = owner filter (empty string means no owner filter)
    // - Tag filter uses PostgreSQL array containment: tags @> ARRAY[$2]
    // - Owner filter uses exact match: owner = $3

    // Fetch all available tags and owners for filter UI (UI-03)
    tags, err := fetchAvailableTags(ctx, deps)
    if err != nil {
        tags = []string{}
    }
    owners, err := fetchAvailableOwners(ctx, deps)
    if err != nil {
        owners = []string{}
    }

    // Placeholder: return nil, 0, nil, nil to indicate FTS query wiring needed
    // The implementation wires ent's DB connection (deps.Ent.DB()) with the above SQL pattern
    // using pgx or ent's raw SQL QueryContext.
    return nil, 0, tags, owners, errors.New("performSearch: FTS query to be wired using ent raw SQL with deps.Ent.DB() and the tsvector/ts_rank/ts_headline pattern documented above")
}

func fetchAvailableTags(ctx context.Context, deps Deps) ([]string, error) {
    // SELECT DISTINCT unnest(tags) FROM asset_versions WHERE superseded_at IS NULL ORDER BY 1
    return nil, errors.New("fetchAvailableTags: to be wired using ent raw SQL")
}

func fetchAvailableOwners(ctx context.Context, deps Deps) ([]string, error) {
    // SELECT DISTINCT owner FROM asset_versions WHERE superseded_at IS NULL AND owner != '' ORDER BY owner
    return nil, errors.New("fetchAvailableOwners: to be wired using ent raw SQL")
}

func parsePositiveInt(s string) int {
    var v int
    for _, c := range s {
        if c < '0' || c > '9' {
            return 0
        }
        v = v*10 + int(c-'0')
    }
    return v
}
```

Update internal/api/router.go to mount the search handler:
```go
// In the protected routes section (after auth.Middleware):
r.Get("/v1/catalog/search", searchHandler(deps))
```

Also create internal/api/connect_search.go if using ConnectRPC for search:
```go
package api

import (
    "context"
    "connectrpc.com/connect"
    "github.com/kanpon/data-governance/proto/api/v1"
)

type searchConnectHandler struct{}

func (h *searchConnectHandler) Search(ctx context.Context, req *connect.Request[v1.SearchRequest]) (*connect.Response[v1.SearchResponse], error) {
    // Delegate to searchHandler logic
    return connect.NewResponse(&v1.SearchResponse{}), nil
}
```
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./internal/api/... 2>&1 | head -10</automated>
  </verify>
  <done>Migration created, search handler extended with tag/owner filter support, tags and owners returned in response for filter UI, go build passes</done>
</task>

<task type="auto">
  <name>Task 2: React catalog browse page with tag filter chips and owner dropdown</name>
  <files>web/src/pages/catalog/index.tsx, web/src/components/SearchBar.tsx, web/src/components/SearchResult.tsx, web/src/components/TagFilter.tsx, web/src/components/OwnerSelect.tsx, web/src/router.tsx</files>
  <read_first>web/src/pages/assets/index.tsx, web/src/components/ui/badge.tsx, web/src/components/ui/select.tsx</read_first>
  <behavior>
    - Catalog page at /catalog with search bar at top (META-04)
    - Tag filter chips below search bar (UI-03) - show all available tags, click to filter
    - Owner dropdown below tag chips (UI-03) - searchable select for filtering by owner
    - Results show type badge (asset/column), name, description snippet, owner, tags
    - Highlighted matched terms using mark element
    - Clicking result navigates to asset detail page
    - Pagination at bottom
    - 60s polling interval (D-17 cold screen)
    - Selected tag chip shows active state; clicking again clears tag filter
    - Selected owner shows in dropdown; clearing dropdown resets owner filter
  </behavior>
  <action>
Update web/src/pages/catalog/index.tsx:

```typescript
import { useState, useMemo } from 'react'
import { createFileRoute } from '@tanstack/react-router'
import { useQuery } from '@tanstack/react-query'
import { SearchBar } from '@/components/SearchBar'
import { SearchResult } from '@/components/SearchResult'
import { TagFilter } from '@/components/TagFilter'
import { OwnerSelect } from '@/components/OwnerSelect'
import { Spinner } from '@/components/ui/spinner'
import { Input } from '@/components/ui/input'

export function CatalogPage() {
  const [query, setQuery] = useState('')
  const [selectedTag, setSelectedTag] = useState('')
  const [selectedOwner, setSelectedOwner] = useState('')
  const [searchType, setSearchType] = useState<string>('') // '', 'asset', 'column'
  const [page, setPage] = useState(1)

  const { data, isLoading, isFetching } = useQuery({
    queryKey: ['catalog', 'search', query, selectedTag, selectedOwner, searchType, page],
    queryFn: async () => {
      const params = new URLSearchParams({
        page: String(page),
        page_size: '20',
      })
      if (query.trim()) params.set('q', query)
      if (selectedTag) params.set('tag', selectedTag)
      if (selectedOwner) params.set('owner', selectedOwner)
      if (searchType) params.set('type', searchType)
      const res = await fetch(`/v1/catalog/search?${params}`)
      if (!res.ok) throw new Error('Search failed')
      return res.json()
    },
    staleTime: 60 * 1000,
    refetchInterval: 60 * 1000, // D-17 cold screen
  })

  // Clear page when filters change
  const handleTagSelect = (tag: string) => {
    setSelectedTag(prev => prev === tag ? '' : tag)
    setPage(1)
  }

  const handleOwnerSelect = (owner: string) => {
    setSelectedOwner(owner)
    setPage(1)
  }

  const handleSearch = () => {
    setPage(1)
  }

  return (
    <div className="space-y-4 max-w-4xl mx-auto">
      <h1 className="text-2xl font-bold">Data Catalog</h1>

      {/* Search controls */}
      <div className="space-y-3">
        <SearchBar
          value={query}
          onChange={setQuery}
          onSearch={handleSearch}
          placeholder="Search assets, columns, descriptions..."
        />

        {/* Tag filter chips (UI-03) */}
        {data?.tags?.length > 0 && (
          <TagFilter
            tags={data.tags}
            selectedTag={selectedTag}
            onSelect={handleTagSelect}
          />
        )}

        {/* Owner filter dropdown (UI-03) */}
        {data?.owners?.length > 0 && (
          <OwnerSelect
            owners={data.owners}
            selectedOwner={selectedOwner}
            onSelect={handleOwnerSelect}
          />
        )}

        {/* Type filter */}
        <div className="flex items-center gap-2">
          <span className="text-sm text-muted-foreground">Type:</span>
          {['all', 'asset', 'column'].map(t => (
            <button
              key={t}
              onClick={() => { setSearchType(t === 'all' ? '' : t); setPage(1) }}
              className={`px-3 py-1 rounded text-sm ${
                (t === 'all' && !searchType) || searchType === t
                  ? 'bg-primary text-primary-foreground'
                  : 'bg-muted hover:bg-muted/80'
              }`}
            >
              {t === 'all' ? 'All' : t.charAt(0).toUpperCase() + t.slice(1)}
            </button>
          ))}
        </div>
      </div>

      {/* Results */}
      {isLoading && (
        <div className="flex justify-center py-12">
          <Spinner />
        </div>
      )}

      {data?.results?.length === 0 && !isLoading && (
        <div className="text-center py-12 text-muted-foreground">
          {query || selectedTag || selectedOwner
            ? `No results found for current filters`
            : 'No assets in catalog yet'}
        </div>
      )}

      {data?.results?.length > 0 && (
        <div className="space-y-3">
          <p className="text-sm text-muted-foreground">
            {data.total} result{data.total !== 1 ? 's' : ''}
            {selectedTag && <> tagged with <strong>"{selectedTag}"</strong></>}
            {selectedOwner && <> owned by <strong>"{selectedOwner}"</strong></>}
            {query && <> matching <strong>"{query}"</strong></>}
          </p>
          {data.results.map((result: any, i: number) => (
            <SearchResult
              key={`${result.type}-${result.name}-${i}`}
              result={result}
              query={query}
            />
          ))}
        </div>
      )}

      {/* Pagination */}
      {data?.total > 20 && (
        <div className="flex items-center justify-center gap-2 pt-4">
          <button
            onClick={() => setPage(p => p - 1)}
            disabled={page === 1}
            className="px-3 py-1 rounded border disabled:opacity-50"
          >
            Previous
          </button>
          <span className="text-sm">
            Page {page} of {Math.ceil(data.total / 20)}
          </span>
          <button
            onClick={() => setPage(p => p + 1)}
            disabled={page >= Math.ceil(data.total / 20)}
            className="px-3 py-1 rounded border disabled:opacity-50"
          >
            Next
          </button>
        </div>
      )}
    </div>
  )
}
```

Create web/src/components/TagFilter.tsx:

```typescript
import { Badge } from '@/components/ui/badge'

interface TagFilterProps {
  tags: string[]
  selectedTag: string
  onSelect: (tag: string) => void
}

export function TagFilter({ tags, selectedTag, onSelect }: TagFilterProps) {
  return (
    <div className="flex items-center gap-2 flex-wrap">
      <span className="text-sm text-muted-foreground">Tags:</span>
      {tags.map(tag => (
        <Badge
          key={tag}
          variant={selectedTag === tag ? 'default' : 'outline'}
          className="cursor-pointer hover:bg-primary/80 transition-colors"
          onClick={() => onSelect(tag)}
        >
          {tag}
        </Badge>
      ))}
    </div>
  )
}
```

Create web/src/components/OwnerSelect.tsx:

```typescript
import { useState } from 'react'
import { ChevronDown, X } from 'lucide-react'
import { Button } from '@/components/ui/button'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'

interface OwnerSelectProps {
  owners: string[]
  selectedOwner: string
  onSelect: (owner: string) => void
}

export function OwnerSelect({ owners, selectedOwner, onSelect }: OwnerSelectProps) {
  const handleClear = () => {
    onSelect('')
  }

  return (
    <div className="flex items-center gap-2">
      <span className="text-sm text-muted-foreground">Owner:</span>
      <Select value={selectedOwner} onValueChange={onSelect}>
        <SelectTrigger className="w-[200px]">
          <SelectValue placeholder="All owners" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="">All owners</SelectItem>
          {owners.map(owner => (
            <SelectItem key={owner} value={owner}>
              {owner}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
      {selectedOwner && (
        <Button variant="ghost" size="sm" onClick={handleClear}>
          <X size={14} />
        </Button>
      )}
    </div>
  )
}
```

Create web/src/components/SearchBar.tsx:

```typescript
import { useState } from 'react'
import { Search } from 'lucide-react'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'

interface SearchBarProps {
  value: string
  onChange: (value: string) => void
  onSearch: () => void
  placeholder?: string
}

export function SearchBar({ value, onChange, onSearch, placeholder = 'Search...' }: SearchBarProps) {
  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter') {
      onSearch()
    }
  }

  return (
    <div className="flex gap-2">
      <div className="relative flex-1">
        <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={16} />
        <Input
          value={value}
          onChange={e => onChange(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder={placeholder}
          className="pl-9"
        />
      </div>
      <Button onClick={onSearch}>Search</Button>
    </div>
  )
}
```

Create web/src/components/SearchResult.tsx:

```typescript
import { useNavigate } from '@tanstack/react-router'
import { Card, CardContent } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

interface SearchResultProps {
  result: {
    type: 'asset' | 'column'
    name: string
    description?: string
    owner?: string
    tags?: string[]
    asset_name?: string
    highlight?: string
  }
  query: string
}

export function SearchResult({ result, query }: SearchResultProps) {
  const navigate = useNavigate()

  const handleClick = () => {
    if (result.type === 'asset') {
      navigate({ to: `/assets/${result.name}` })
    } else if (result.asset_name) {
      navigate({ to: `/assets/${result.asset_name}` })
    }
  }

  // Highlight matched terms in the highlight field (from FTS ts_headline)
  const highlightHtml = result.highlight || result.description || ''

  return (
    <Card
      className="cursor-pointer hover:shadow-md transition-shadow"
      onClick={handleClick}
    >
      <CardContent className="p-4">
        <div className="flex items-start gap-3">
          <Badge variant={result.type === 'asset' ? 'default' : 'secondary'}>
            {result.type}
          </Badge>
          <div className="flex-1 min-w-0">
            <h3 className="font-semibold truncate">{result.name}</h3>
            {result.asset_name && result.type === 'column' && (
              <p className="text-xs text-muted-foreground">in {result.asset_name}</p>
            )}
            <p
              className="text-sm text-muted-foreground mt-1 line-clamp-2"
              dangerouslySetInnerHTML={{ __html: highlightHtml }}
            />
            <div className="flex items-center gap-2 mt-2">
              {result.owner && (
                <span className="text-xs text-muted-foreground">Owner: {result.owner}</span>
              )}
              {result.tags?.slice(0, 5).map(tag => (
                <Badge key={tag} variant="outline" className="text-xs">{tag}</Badge>
              ))}
            </div>
          </div>
        </div>
      </CardContent>
    </Card>
  )
}
```

Update web/src/router.tsx to add catalog route:
```typescript
.route('/catalog', {
  component: () => import('./pages/catalog/index').then(m => m.CatalogPage),
})
```
  </action>
  <verify>
    <automated>cd web && pnpm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Catalog page renders with search bar, tag filter chips, owner dropdown, type filter, results with type badge and highlights, pagination works, 60s polling active. Tag chip click toggles filter. Owner dropdown filters by selected owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search query → FTS | User input passed to plainto_tsquery — safe; no user content in tsquery |
| Tag/owner filter → SQL | Filter values used in parameterized queries — safe from SQL injection |
| FTS results → UI | HTML highlighting via ts_headline — encoded by Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | Full-text search | mitigate | Only authenticated users can search; results scoped to user's accessible assets |
| T-06-08 | Information Disclosure | Tag/owner browse | mitigate | Only authenticated users can browse; results scoped to user's accessible assets |
</threat_model>

<verification>
- `cd web && pnpm run build` succeeds
- Search returns ranked results from FTS index with tag/owner filters applied
- Highlighting works on matched terms
- Tag chips are clickable and show active state
- Owner dropdown filters results correctly
</verification>

<success_criteria>
META-04 complete: catalog search works with Postgres FTS, results ranked and highlighted, React page functional.

UI-03 complete: catalog browse page with tag filter chips and owner dropdown, filtering works alongside search, all available tags and owners populated from API.
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-ui-api/06-03-SUMMARY.md`
</output>