---
phase: "06-web-ui-api"
plan: "04"
type: execute
wave: 4
depends_on: ["06-01", "06-03"]
files_modified:
  - internal/api/lineage_handlers.go
  - internal/api/connect_lineage.go
  - internal/api/router.go
  - internal/lineage/queries/lineage.sql
  - internal/lineage/queries/lineage.sql.go
  - web/src/pages/lineage/[id].tsx
  - web/src/components/LineageDAG.tsx
  - web/src/components/AssetNode.tsx
  - web/src/components/ColumnPanel.tsx
  - web/src/router.tsx
autonomous: true
requirements: [LINE-04, LINE-05]
user_setup: []

must_haves:
  truths:
    - "用户可以查看任何资产的谱系 DAG，含交互式导航（缩放、平移）"
    - "点击资产节点打开侧边面板，显示该节点的资产元数据 + 列级谱系（D-13）"
    - "邻域 API（asset_id, focus, depth）返回子图；不加载完整图"
    - "客户端 dagre 布局计算 DAG 布局；ReactFlow 渲染结果"
    - "用户缩放到资产节点时显示列级边（D-14）"
    - "本计划交付 API 脚手架（处理器 + 接线，查询实现在后续 waves）"
  artifacts:
    - path: "web/src/components/LineageDAG.tsx"
      provides: "带 dagre 布局的 ReactFlow 画布，按需获取邻域"
      contains: "ReactFlow, dagre, useNodesState, useEdgesState"
    - path: "web/src/components/AssetNode.tsx"
      provides: "资产节点的自定义 ReactFlow 节点"
      contains: "Handle, Position, 资产名称 + 类型徽章"
    - path: "web/src/components/ColumnPanel.tsx"
      provides: "点击资产节点时显示列谱系的侧边面板"
      contains: "资产元数据、列列表、列级边"
    - path: "internal/api/connect_lineage.go"
      provides: "ConnectRPC LineageService.Neighborhood 实现"
      contains: "邻域 SQL 查询、节点/边响应"
  key_links:
    - from: "web/src/components/LineageDAG.tsx"
      to: "GET /v1/connect/api.v1.LineageService/Neighborhood"
      via: "节点点击、深度变化的 TanStack Query"
      pattern: "useQuery.*Neighborhood"
    - from: "internal/api/connect_lineage.go"
      to: "internal/lineage/queries"
      via: "asset_edges + column_edges 的 sqlc 查询"
      pattern: "TraverseAssetLineage"

---

<objective>
实现交互式谱系 DAG 可视化（LINE-04），带按需获取邻域（D-12）。通过侧边面板实现列下钻（LINE-05）（D-13）。客户端 dagre 布局（D-15）。一个画布，两个缩放级别：资产级和列级（D-14）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-web-ui-api/06-CONTEXT.md
@internal/lineage/queries/lineage.sql
@internal/lineage/queries/lineage.sql.go
@proto/api/v1/api.proto (from plan 01)

## D-12: 邻域 API 形状
GET /v1/lineage/neighborhood?asset_id=<id>&depth=<n>
返回 {nodes: [{id, name, type: 'asset'|'column', columns: [...]}], edges: [{from_id, to_id, type: 'asset_lineage'|'column_lineage'}]}

## D-15: 客户端 dagre 通过 ReactFlow
```javascript
import dagre from 'dagre'
const g = new dagre.graphlib.Graph()
g.setGraph({ rankdir: 'TB', nodesep: 50, ranksep: 80 })
g.setDefaultEdgeLabel(() => ({}))
// Add nodes/edges from neighborhood response
dagre.layout(g)
// Apply positions to ReactFlow nodes
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 带递归 CTE 的谱系邻域 ConnectRPC 处理器</name>
  <files>internal/api/connect_lineage.go, internal/api/lineage_handlers.go, internal/api/router.go, internal/lineage/queries/neighborhood.sql</files>
  <read_first>internal/lineage/queries/lineage.sql.go, internal/lineage/queries/lineage.sql, internal/lineage/queries/models.go, proto/api/v1/api.proto</read_first>
  <behavior>
    - Neighborhood RPC 使用递归 CTE 查询 asset_edges 获取深度 N 以内的直接邻居
    - 返回节点（资产节点 + 如果选择了焦点节点则包含列节点）和边
    - 设置焦点节点时包含列边（D-14）
    - 使用 sqlc 查询进行邻域扩展
  </behavior>
  <action>
在 internal/api/connect_lineage.go 中添加 LineageService 实现：

```go
package api

import (
    "context"
    "strconv"

    "connectrpc.com/connect"
    "github.com/kanpon/data-governance/internal/lineage/queries"
    "github.com/kanpon/data-governance/proto/api/v1"
)

type lineageConnectHandler struct {
    deps ConnectDeps
}

func newLineageConnectHandler(deps ConnectDeps) (string, *connect.Server) {
    h := &lineageConnectHandler{deps: deps}
    return "/v1/connect/api.v1.LineageService", connect.NewServer(h)
}

func (h *lineageConnectHandler) Neighborhood(ctx context.Context, req *connect.Request[v1.NeighborhoodRequest]) (*connect.Response[v1.NeighborhoodResponse], error) {
    assetID := req.Msg.AssetId
    if assetID == "" {
        return nil, connect.NewError(connect.CodeInvalidArgument, nil)
    }
    depth := int(req.Msg.Depth)
    if depth <= 0 {
        depth = 2 // 默认 2 跳邻域
    }
    if depth > 5 {
        depth = 5 // 上限 5 以限制查询范围
    }

    // 邻域的递归 CTE：
    // WITH RECURSIVE neighborhood AS (
    //   SELECT to_asset as asset, 1 as depth, ARRAY[from_asset, to_asset] as path
    //   FROM asset_edges WHERE from_asset = $1 AND superseded_at IS NULL
    //   UNION ALL
    //   SELECT e.to_asset, n.depth + 1, n.path || e.to_asset
    //   FROM asset_edges e
    //   JOIN neighborhood n ON e.from_asset = n.asset
    //   WHERE n.depth < $2 AND e.superseded_at IS NULL
    //   AND NOT (e.to_asset = ANY(n.path))  -- 防止循环
    // )
    // SELECT DISTINCT asset, depth FROM neighborhood ORDER BY depth, asset LIMIT 50

    nodes := []*v1.AssetNode{}
    edges := []*v1.LineageEdge{}

    // 占位符：查询 asset_edges 获取邻域
    // 对于每个访问的资产，创建类型为 'asset' 的 AssetNode
    // 对于 asset_edges 中的每条边，创建类型为 'asset_lineage' 的 LineageEdge

    // 示例响应（占位符）：
    // nodes = append(nodes, &v1.AssetNode{
    //     Id: assetID,
    //     Name: assetID,
    //     Type: "asset",
    //     Columns: []*v1.ColumnInfo{},
    // })

    return connect.NewResponse(&v1.NeighborhoodResponse{
        Nodes: nodes,
        Edges: edges,
    }), nil
}

func (h *lineageConnectHandler) Impact(ctx context.Context, req *connect.Request[v1.ImpactRequest]) (*connect.Response[v1.ImpactResponse], error) {
    // 委托给 lineage/impact 包的现有 impact.Analyze
    return nil, connect.NewError(connect.CodeUnimplemented, nil)
}
```

在 internal/api/router.go 中挂载谱系 ConnectRPC 处理器（通过 plan 01 的 mountConnectRPC 已完成）。

在 internal/lineage/queries/neighborhood.sql 中添加新的 sqlc 查询：

```sql
-- name: NeighborhoodAssets :many
-- 递归 CTE 获取深度 N 以内的资产邻域
WITH RECURSIVE neighborhood AS (
    -- 基础：焦点资产的直接邻居（上游和下游）
    SELECT
        e.from_asset as prev_asset,
        e.to_asset as asset,
        1 AS depth,
        ARRAY[e.from_asset, e.to_asset] AS path
    FROM asset_edges e
    WHERE e.from_asset = $1 AND e.superseded_at IS NULL

    UNION ALL

    -- 递归：再扩展一跳
    SELECT
        e.from_asset as prev_asset,
        e.to_asset as asset,
        n.depth + 1,
        n.path || e.to_asset
    FROM asset_edges e
    JOIN neighborhood n ON e.from_asset = n.asset
    WHERE n.depth < $2
      AND e.superseded_at IS NULL
      AND NOT (e.to_asset = ANY(n.path))  -- 循环预防
)
SELECT DISTINCT asset, depth FROM neighborhood ORDER BY depth, asset LIMIT 50
```

然后运行 `sqlc generate` 生成 neighborhood.sql.go。

注意：邻域查询与现有的 impact.Analyze 分开，因为它返回用于可视化的图结构（节点 + 边），而不仅仅是资产列表。
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./internal/api/... 2>&1 | head -10</automated>
  </verify>
  <done>LineageService.Neighborhood 处理器存在，sqlc 邻域查询已生成，go build 通过</done>
</task>

<task type="auto">
  <name>Task 2: 带 dagre 布局的 ReactFlow DAG 组件</name>
  <files>web/src/components/LineageDAG.tsx, web/src/components/AssetNode.tsx, web/src/components/ColumnPanel.tsx, web/src/pages/lineage/[id].tsx</files>
  <read_first>web/package.json (check @xyflow/react version)</read_first>
  <behavior>
    - LineageDAG 使用 dagre 布局渲染 ReactFlow 画布
    - 资产节点是显示资产名称 + 类型徽章的自定义节点
    - 点击节点，ColumnPanel 打开为侧边面板
    - 缩放控制，最小地图，加载时 fit-view
    - 深度选择器展开/折叠邻域
    - 列级视图：缩放到资产显示列节点和列边
  </behavior>
  <action>
创建 web/src/components/LineageDAG.tsx：

```typescript
import { useCallback, useMemo, useState } from 'react'
import {
  ReactFlow,
  Node,
  Edge,
  Controls,
  MiniMap,
  Background,
  useNodesState,
  useEdgesState,
  MarkerType,
} from '@xyflow/react'
import dagre from 'dagre'
import '@xyflow/react/dist/style.css'
import { AssetNode } from './AssetNode'
import { ColumnPanel } from './ColumnPanel'

const nodeTypes = {
  asset: AssetNode,
}

interface NeighborhoodData {
  nodes: Array<{
    id: string
    name: string
    type: 'asset' | 'column'
    columns?: Array<{ name: string; type: string }>
  }>
  edges: Array<{
    from_id: string
    to_id: string
    type: 'asset_lineage' | 'column_lineage'
  }>
}

interface LineageDAGProps {
  focusAssetId: string
  initialDepth?: number
  data: NeighborhoodData | null
  isLoading: boolean
  onDepthChange: (depth: number) => void
  currentDepth: number
}

export function LineageDAG({
  focusAssetId,
  initialDepth = 2,
  data,
  isLoading,
  onDepthChange,
  currentDepth,
}: LineageDAGProps) {
  const [selectedNode, setSelectedNode] = useState<string | null>(null)
  const [showColumnView, setShowColumnView] = useState(false)

  // 将 API 响应转换为 ReactFlow 节点/边
  const { initialNodes, initialEdges } = useMemo(() => {
    if (!data) return { initialNodes: [], initialEdges: [] }

    const nodes: Node[] = data.nodes.map(node => ({
      id: node.id,
      type: 'asset',
      position: { x: 0, y: 0 }, // dagre 将计算
      data: {
        label: node.name,
        type: node.type,
        columns: node.columns,
        isFocus: node.id === focusAssetId,
      },
    }))

    const edges: Edge[] = data.edges
      .filter(e => e.type === 'asset_lineage' || showColumnView)
      .map(edge => ({
        id: `${edge.from_id}-${edge.to_id}`,
        source: edge.from_id,
        target: edge.to_id,
        type: 'smoothstep',
        animated: edge.type === 'asset_lineage',
        markerEnd: { type: MarkerType.ArrowClosed },
        style: { stroke: '#94a3b8' },
      }))

    return { initialNodes: nodes, initialEdges: edges }
  }, [data, focusAssetId, showColumnView])

  // 应用 dagre 布局
  const { nodes: layoutedNodes, edges: layoutedEdges } = useMemo(() => {
    if (initialNodes.length === 0) return { nodes: [], edges: initialEdges }

    const g = new dagre.graphlib.Graph()
    g.setDefaultEdgeLabel(() => ({}))
    g.setGraph({ rankdir: 'TB', nodesep: 60, ranksep: 80 })

    initialNodes.forEach(node => g.setNode(node.id, { width: 180, height: 60 }))
    initialEdges.forEach(edge => g.setEdge(edge.source, edge.target))

    dagre.layout(g)

    const layoutedNodes = initialNodes.map(node => {
      const dagreNode = g.node(node.id)
      return {
        ...node,
        position: {
          x: dagreNode.x - 90, // 居中节点
          y: dagreNode.y - 30,
        },
      }
    })

    return { nodes: layoutedNodes, edges: initialEdges }
  }, [initialNodes, initialEdges])

  const [nodes, , onNodesChange] = useNodesState(layoutedNodes)
  const [edges, , onEdgesChange] = useEdgesState(layoutedEdges)

  const onNodeClick = useCallback((event: React.MouseEvent, node: Node) => {
    setSelectedNode(node.id)
  }, [])

  const onPaneClick = useCallback(() => {
    setSelectedNode(null)
    setShowColumnView(false)
  }, [])

  const selectedNodeData = selectedNode
    ? data?.nodes.find(n => n.id === selectedNode)
    : null

  return (
    <div className="relative h-[calc(100vh-200px)]">
      {isLoading && (
        <div className="absolute inset-0 bg-background/50 z-10 flex items-center justify-center">
          <div className="animate-spin h-8 w-8 border-4 border-primary border-t-transparent rounded-full" />
        </div>
      )}

      <ReactFlow
        nodes={layoutedNodes}
        edges={layoutedEdges}
        onNodesChange={onNodesChange}
        onEdgesChange={onEdgesChange}
        onNodeClick={onNodeClick}
        onPaneClick={onPaneClick}
        nodeTypes={nodeTypes}
        fitView
        fitViewOptions={{ padding: 0.2 }}
      >
        <Background />
        <Controls />
        <MiniMap />
      </ReactFlow>

      {/* 深度选择器 */}
      <div className="absolute top-4 right-4 flex items-center gap-2 bg-background border rounded-lg p-2 shadow-md">
        <span className="text-sm">深度：</span>
        {[1, 2, 3].map(d => (
          <button
            key={d}
            onClick={() => onDepthChange(d)}
            className={`px-2 py-1 rounded text-sm ${
              currentDepth === d
                ? 'bg-primary text-primary-foreground'
                : 'hover:bg-muted'
            }`}
          >
            {d}
          </button>
        ))}
      </div>

      {/* 列切换 */}
      {selectedNode && (
        <button
          onClick={() => setShowColumnView(v => !v)}
          className="absolute top-4 left-4 px-3 py-1.5 bg-background border rounded-lg text-sm shadow-md hover:bg-muted"
        >
          {showColumnView ? '隐藏列' : '显示列'}
        </button>
      )}

      {/* 列面板（D-13：节点点击时的侧边面板） */}
      {selectedNodeData && (
        <ColumnPanel
          node={selectedNodeData}
          onClose={() => setSelectedNode(null)}
        />
      )}
    </div>
  )
}
```

创建 web/src/components/AssetNode.tsx：

```typescript
import { Handle, Position, NodeProps } from '@xyflow/react'
import { Badge } from '@/components/ui/badge'

interface AssetNodeData {
  label: string
  type: 'asset' | 'column'
  columns?: Array<{ name: string; type: string }>
  isFocus: boolean
}

export function AssetNode({ data, selected }: NodeProps<AssetNodeData>) {
  return (
    <div
      className={`px-4 py-2 rounded-lg border-2 min-w-[140px] ${
        selected
          ? 'border-primary shadow-lg'
          : data.isFocus
          ? 'border-primary/50'
          : 'border-border'
      } bg-background`}
    >
      <Handle type="target" position={Position.Top} className="!bg-muted-foreground" />
      <div className="flex items-center gap-2">
        {data.isFocus && <Badge variant="default" className="text-xs">焦点</Badge>}
        <span className="font-medium truncate">{data.label}</span>
      </div>
      <div className="text-xs text-muted-foreground mt-0.5 capitalize">{data.type}</div>
      <Handle type="source" position={Position.Bottom} className="!bg-muted-foreground" />
    </div>
  )
}
```

创建 web/src/components/ColumnPanel.tsx：

```typescript
import { X } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

interface ColumnPanelProps {
  node: {
    id: string
    name: string
    type: 'asset' | 'column'
    columns?: Array<{ name: string; type: string }>
  }
  onClose: () => void
}

export function ColumnPanel({ node, onClose }: ColumnPanelProps) {
  return (
    <div className="absolute right-0 top-0 h-full w-80 bg-background border-l shadow-lg z-20 overflow-y-auto">
      <div className="flex items-center justify-between p-4 border-b">
        <h3 className="font-semibold truncate">{node.name}</h3>
        <Button variant="ghost" size="icon" onClick={onClose}>
          <X size={16} />
        </Button>
      </div>

      <div className="p-4 space-y-4">
        <div>
          <h4 className="text-sm font-medium mb-2">类型</h4>
          <Badge variant="secondary">{node.type}</Badge>
        </div>

        {node.columns && node.columns.length > 0 && (
          <div>
            <h4 className="text-sm font-medium mb-2">列 ({node.columns.length})</h4>
            <div className="space-y-1">
              {node.columns.map(col => (
                <div key={col.name} className="flex items-center justify-between text-sm py-1 border-b">
                  <span className="font-mono">{col.name}</span>
                  <span className="text-muted-foreground text-xs">{col.type}</span>
                </div>
              ))}
            </div>
          </div>
        )}

        <div>
          <h4 className="text-sm font-medium mb-2">元数据</h4>
          <div className="text-sm space-y-1">
            <div className="flex justify-between">
              <span className="text-muted-foreground">ID</span>
              <span className="font-mono text-xs">{node.id.slice(0, 12)}...</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}
```

创建 web/src/pages/lineage/[id].tsx：

```typescript
import { createFileRoute, useParams } from '@tanstack/react-router'
import { useQuery } from '@tanstack/react-query'
import { LineageDAG } from '@/components/LineageDAG'
import { Spinner } from '@/components/ui/spinner'
import { Button } from '@/components/ui/button'

export function LineagePage() {
  const params = useParams({ from: '/lineage/$id' })
  const assetId = params.id
  const [depth, setDepth] = useState(2)

  const { data, isLoading, refetch } = useQuery({
    queryKey: ['lineage', 'neighborhood', assetId, depth],
    queryFn: async () => {
      const params = new URLSearchParams({
        asset_id: assetId,
        depth: String(depth),
      })
      const res = await fetch(`/v1/connect/api.v1.LineageService/Neighborhood?${params}`)
      if (!res.ok) throw new Error('Failed to fetch lineage')
      return res.json()
    },
    staleTime: 60 * 1000,
  })

  return (
    <div className="h-screen flex flex-col">
      <div className="flex items-center gap-4 p-4 border-b">
        <h1 className="text-xl font-bold">谱系：{assetId}</h1>
        <Button variant="outline" size="sm" onClick={() => refetch()}>
          刷新
        </Button>
      </div>

      <div className="flex-1">
        <LineageDAG
          focusAssetId={assetId}
          initialDepth={depth}
          data={data}
          isLoading={isLoading}
          onDepthChange={setDepth}
          currentDepth={depth}
        />
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd web && pnpm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Lineage DAG 使用 ReactFlow + dagre 布局渲染，资产节点可点击，ColumnPanel 显示列详情，深度选择器有效</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|----------|-------------|
| ReactFlow -> API | 通过 Vite 代理（含 auth cookie）获取 |
| API -> DB | 邻域 SQL 使用参数化查询 |

## STRIDE 威胁注册

| 威胁 ID | 类别 | 组件 | 处理方式 | 缓解计划 |
|---------|----------|-----------|-------------|-----------------|
| T-06-08 | 信息泄露 | 谱系数据 | mitigate | 需要有效 JWT；谱系边限定于可访问的资产 |
| T-06-09 | 拒绝服务 | 大深度 | mitigate | 深度上限 5；每个邻域查询结果限制 50 个节点 |
</threat_model>

<verification>
- `cd web && pnpm run build` 成功
- 导航到 /lineage/:id 显示带资产节点的 DAG
- 点击节点打开 ColumnPanel
- 深度选择器改变邻域大小
</verification>

<success_criteria>
LINE-04 完成：带缩放/平移/节点点击的交互式 DAG。LINE-05 完成：通过显示列级谱系的侧边面板进行列下钻。
</success_criteria>

<output>
完成后，创建 `.planning/phases/06-web-ui-api/06-04-SUMMARY.md`
</output>