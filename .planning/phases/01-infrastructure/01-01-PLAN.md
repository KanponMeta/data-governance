---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/platform/main.go
  - internal/storage/.gitkeep
  - internal/auth/.gitkeep
  - internal/connector/.gitkeep
  - internal/connector/proto/.gitkeep
  - internal/connector/gen/.gitkeep
  - internal/event/.gitkeep
  - docker-compose.yml
  - Dockerfile
  - Makefile
  - .dockerignore
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - CORE-04
tags:
  - go
  - bootstrap
  - docker

must_haves:
  truths:
    - "`go build ./cmd/platform/...` 成功并产生单个二进制文件"
    - "`docker compose up --build platform postgres` 启动两个服务，平台退出干净并带有占位符日志行"
    - "go.mod 声明所有 Phase 1 运行时依赖并锁定版本"
    - "目录布局与 D-04 完全匹配 (cmd/platform, internal/{storage,auth,connector,event}, 无顶层 pkg/)"
    - "`make build`、`make test`、`make generate` 目标存在并指向正确的工具"
  artifacts:
    - path: "go.mod"
      provides: "模块声明 + 依赖清单"
      contains: "module github.com/kanpon/data-governance"
    - path: "cmd/platform/main.go"
      provides: "单二进制入口桩"
      min_lines: 15
    - path: "docker-compose.yml"
      provides: "本地开发栈 (platform + postgres) 带健康检查"
      contains: "healthcheck"
    - path: "Makefile"
      provides: "标准 build/test/generate/migrate 目标"
      contains: "build:"
    - path: "Dockerfile"
      provides: "平台二进制文件的多阶段构建"
      contains: "FROM golang"
  key_links:
    - from: "docker-compose.yml platform service"
      to: "Dockerfile"
      via: "build context"
      pattern: "build:"
    - from: "docker-compose.yml platform service"
      to: "postgres service"
      via: "depends_on with service_healthy condition"
      pattern: "service_healthy"
    - from: "Makefile"
      to: "go build ./cmd/platform/..."
      via: "build target"
      pattern: "go build"
---

<objective>
引导 Go 模块，锁定 D-04 的目录布局，在 `go.mod` 中声明每个 Phase 1 依赖，提供带健康检查的 Docker Compose 开发栈。生成一个可工作的单二进制框架供下游计划扩展——尚无业务逻辑，只有可构建的脚手架。

目的: 为所有后续阶段建立项目形状。避免以后的"寻宝游戏"——所有路径都是预先确定的。
输出: 可编译、可运行、Docker 化的空平台。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@CLAUDE.md
</context>

<threat_model>
## 信任边界

| 边界 | 描述 |
|----------|-------------|
| Host → Container | Docker Compose 隔离服务；postgres 仅通过 compose 网络可达 |
| Repo → Image | Dockerfile 构建上下文定义哪些代码/密钥被复制到镜像中 |

## STRIDE 威胁寄存器

| 威胁 ID | 类别 | 组件 | 处理方式 | 缓解计划 |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | 信息泄露 | 仓库 / Docker 镜像 | 缓解 | `.gitignore` 排除 `.env`、`*.key`、构建产物；`.dockerignore` 排除 `.git`、`.env*` 以便密钥永不进入镜像；`.env.example` 仅发送占位符名称 |
| T-01-02 | 篡改 | 构建流水线 | 缓解 | Dockerfile 使用固定的 `golang:1.22.x-alpine` 基础镜像；多阶段构建仅将二进制文件和 CA 证书复制到 `gcr.io/distroless/static-debian12` 最终镜像以移除 shell/包管理器攻击面 |
| T-01-03 | 拒绝服务 | postgres 容器 | 接受 | 仅本地开发；生产部署使用 Kubernetes (Phase 2+)。健康检查快速失败以便平台不会对已关闭的 DB 接受流量 |
| T-01-04 | 权限提升 | 容器运行时 | 缓解 | 最终镜像以非 root 用户运行 (`USER nonroot:nonroot` 来自 distroless)；compose 在可能时设置 `read_only: true` |

**应用的 ASVS L1 控制:** V14.1 (构建)、V14.2 (依赖)、V14.3 (意外安全泄露)
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Go 模块 + 按 D-04 的目录布局</name>
  <files>go.mod, internal/storage/.gitkeep, internal/auth/.gitkeep, internal/connector/.gitkeep, internal/connector/proto/.gitkeep, internal/connector/gen/.gitkeep, internal/event/.gitkeep, cmd/platform/main.go, .gitignore</files>
  <read_first>
    - .planning/phases/01-infrastructure/01-CONTEXT.md (D-04 指定确切目录布局)
    - CLAUDE.md (技术栈 — 给出所需模块版本)
  </read_first>
  <action>
运行 `go mod init github.com/kanpon/data-governance` (Go 1.22)。按 D-04 精确创建目录树:

```
cmd/platform/main.go
internal/storage/.gitkeep
internal/auth/.gitkeep
internal/connector/.gitkeep
internal/connector/proto/.gitkeep
internal/connector/gen/.gitkeep
internal/event/.gitkeep
```

不要创建顶层 `pkg/` 目录 (D-04 明确禁止 v1)。

`cmd/platform/main.go` 内容 (框架 — Plan 03 将用 chi 路由器 + connect-go 服务器替换):

```go
package main

import (
	"context"
	"log/slog"
	"os"
	"os/signal"
	"syscall"
)

func main() {
	logger := slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo}))
	slog.SetDefault(logger)

	ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer stop()

	slog.Info("platform.starting", "version", "0.1.0-phase1-skeleton")

	<-ctx.Done()
	slog.Info("platform.stopped")
}
```

`.gitignore` 必须包含 (每行一个): `bin/`、`*.exe`、`*.test`、`*.out`、`coverage.txt`、`.env`、`.env.local`、`*.key`、`*.pem`、`node_modules/`、`dist/`、`.idea/`、`.vscode/`、`*.swp`、`migrations/migrations.sum.lock`。

`.gitkeep` 文件的存在仅是为了使空目录可提交；它们应该是空文件。
  </action>
  <verify>
    <automated>test -f go.mod && test -f cmd/platform/main.go && test -d internal/storage && test -d internal/auth && test -d internal/connector/proto && test -d internal/connector/gen && test -d internal/event && ! test -d pkg && go build ./cmd/platform/...</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` 第一行等于 `module github.com/kanpon/data-governance`
    - `go.mod` `go` 指令指定 `1.22` 或更高
    - 目录存在: `cmd/platform/`、`internal/storage/`、`internal/auth/`、`internal/connector/`、`internal/connector/proto/`、`internal/connector/gen/`、`internal/event/`
    - 顶层 `pkg/` 目录不存在 (`! test -d pkg` 退出 0)
    - `cmd/platform/main.go` 包含 `func main()` 和 `slog.Info("platform.starting"`
    - `.gitignore` 包含行 `.env` (grep `^\.env$` 退出 0)
    - `go build ./cmd/platform/...` 退出 0
  </acceptance_criteria>
  <done>模块已初始化，布局已锁定，空二进制文件构建并运行。</done>
</task>

<task type="auto">
  <name>Task 2: 添加所有 Phase 1 运行时依赖 + Makefile</name>
  <files>go.mod, go.sum, Makefile, .env.example</files>
  <read_first>
    - go.mod (在 Task 1 中创建)
    - CLAUDE.md (技术表 — 精确的模块路径和版本锁定)
  </read_first>
  <action>
通过 `go get` 添加 Phase 1 运行时 + 工具依赖。精确运行每个命令:

```
go get entgo.io/ent@v0.14.0
go get ariga.io/atlas@latest
go get github.com/jackc/pgx/v5@v5.5.0
go get github.com/jackc/pgx/v5/stdlib@v5.5.0
go get github.com/jackc/pgx/v5/pgxpool@v5.5.0
go get github.com/go-chi/chi/v5@v5.2.5
go get github.com/golang-jwt/jwt/v5@v5.3.0
go get golang.org/x/crypto/bcrypt@latest
go get connectrpc.com/connect@v1.19.0
go get google.golang.org/protobuf@latest
go get github.com/prometheus/client_golang@v1.19.0
go get go.opentelemetry.io/otel@v1.27.0
go get github.com/google/uuid@latest
go get github.com/stretchr/testify@latest
go get github.com/google/go-cmp/cmp@latest
```

工具依赖 (记录为 `// indirect` 即可；如果后续计划需要，我们将通过 `tools.go` 正式化):

```
go get -tool entgo.io/ent/cmd/ent@v0.14.0
go get -tool ariga.io/atlas/cmd/atlas@latest
go get -tool google.golang.org/protobuf/cmd/protoc-gen-go@latest
go get -tool connectrpc.com/connect/cmd/protoc-gen-connect-go@latest
```

(如果安装的 Go 工具链上 `go get -tool` 不可用，请在 `internal/tools/tools.go` 下使用 `//go:build tools` 构建标签创建 `tools.go` 文件，并导入每个工具路径；下游计划仅依赖于二进制文件在 PATH 上或通过 `go run` 调用。)

添加依赖后，运行 `go mod tidy`。

创建 `Makefile`，精确包含这些目标 (使用 TAB 缩进，不是空格):

```
SHELL := /bin/bash
GO ?= go
BINARY := bin/platform
PKG := ./...

.PHONY: build test lint generate migrate run docker-build clean tidy

build:
	$(GO) build -o $(BINARY) ./cmd/platform

test:
	$(GO) test -race -count=1 $(PKG)

lint:
	$(GO) vet $(PKG)

generate:
	$(GO) generate $(PKG)

migrate:
	$(GO) run ./cmd/platform migrate

run: build
	./$(BINARY)

docker-build:
	docker compose build

tidy:
	$(GO) mod tidy

clean:
	rm -rf bin/
```

创建 `.env.example`，仅包含占位符值 (无真实密钥):

```
# Platform
PLATFORM_HTTP_ADDR=:8080
PLATFORM_LOG_LEVEL=info

# Database
DATABASE_URL=postgres://platform:platform@postgres:5432/platform?sslmode=disable

# JWT
JWT_SIGNING_KEY=replace-with-base64-encoded-32-byte-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

# Admin bootstrap (仅在首次启动时使用)
PLATFORM_ADMIN_EMAIL=admin@example.com
PLATFORM_ADMIN_PASSWORD=replace-on-first-start
```

创建 Makefile 后再次运行 `go mod tidy` 以确保 go.sum 一致。
  </action>
  <verify>
    <automated>go mod tidy && grep -q "entgo.io/ent" go.mod && grep -q "github.com/go-chi/chi/v5" go.mod && grep -q "github.com/jackc/pgx/v5" go.mod && grep -q "github.com/golang-jwt/jwt/v5" go.mod && grep -q "connectrpc.com/connect" go.mod && grep -q "ariga.io/atlas" go.mod && grep -q "github.com/prometheus/client_golang" go.mod && grep -q "go.opentelemetry.io/otel" go.mod && grep -E "^build:" Makefile && grep -E "^test:" Makefile && grep -E "^generate:" Makefile && grep -E "^migrate:" Makefile && grep -q "JWT_SIGNING_KEY" .env.example && make build</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` 包含 require 行: `entgo.io/ent`、`ariga.io/atlas`、`github.com/jackc/pgx/v5`、`github.com/go-chi/chi/v5`、`github.com/golang-jwt/jwt/v5`、`golang.org/x/crypto`、`connectrpc.com/connect`、`google.golang.org/protobuf`、`github.com/prometheus/client_golang`、`go.opentelemetry.io/otel`、`github.com/google/uuid`、`github.com/stretchr/testify`
    - `go mod tidy` 退出 0 且运行后无更改
    - `Makefile` 包含精确目标 `build:`、`test:`、`lint:`、`generate:`、`migrate:`、`run:`、`docker-build:`、`tidy:`、`clean:` (通过 `grep -E "^<target>:" Makefile` 验证每个)
    - `make build` 退出 0 并产生 `bin/platform`
    - `.env.example` 包含字面字符串 `JWT_SIGNING_KEY`、`DATABASE_URL`、`PLATFORM_HTTP_ADDR`
    - `.env.example` 不包含任何真实密钥 (JWT_SIGNING_KEY 行不超过 64 个 base64 字符)
  </acceptance_criteria>
  <done>依赖清单已锁定并整理；Makefile 驱动每个常见工作流程；示例 env 文件记录所需变量。</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfile + 带健康检查的 Docker Compose 开发栈</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore</files>
  <read_first>
    - cmd/platform/main.go (在 Task 1 中创建 — 需要知道二进制入口点)
    - .env.example (在 Task 2 中创建 — 定义 compose 必须连接的 env 变量)
    - Makefile (Task 2 — `docker-build` 目标取决于此文件存在)
  </read_first>
  <action>
创建多阶段构建的 `Dockerfile`:

```dockerfile
# syntax=docker/dockerfile:1.7

FROM golang:1.22-alpine AS build
WORKDIR /src
RUN apk add --no-cache git ca-certificates
COPY go.mod go.sum ./
RUN go mod download
COPY . .
ARG VERSION=0.1.0-phase1
RUN CGO_ENABLED=0 GOOS=linux go build \
    -trimpath \
    -ldflags "-s -w -X main.version=${VERSION}" \
    -o /out/platform ./cmd/platform

FROM gcr.io/distroless/static-debian12:nonroot AS runtime
WORKDIR /app
COPY --from=build /out/platform /app/platform
USER nonroot:nonroot
EXPOSE 8080
ENTRYPOINT ["/app/platform"]
```

创建 `.dockerignore`:

```
.git
.gitignore
.env
.env.*
!.env.example
bin/
node_modules/
dist/
*.test
*.out
coverage.txt
.idea/
.vscode/
README*.md
docs/
```

创建 `docker-compose.yml` (compose v2 schema；不要包含 `version:` 键 — 已弃用):

```yaml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: platform
      POSTGRES_PASSWORD: platform
      POSTGRES_DB: platform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U platform -d platform"]
      interval: 5s
      timeout: 5s
      retries: 10

  platform:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PLATFORM_HTTP_ADDR: ":8080"
      PLATFORM_LOG_LEVEL: "info"
      DATABASE_URL: "postgres://platform:platform@postgres:5432/platform?sslmode=disable"
      JWT_SIGNING_KEY: "${JWT_SIGNING_KEY:-dev-only-do-not-use-in-prod-32bytes!!}"
      JWT_ACCESS_TTL: "15m"
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "/app/platform", "healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s

volumes:
  postgres_data:
```

注意: `platform` 健康检查命令 (`/app/platform healthcheck`) 将在 Plan 03 部署 HTTP 服务器时实现。Plan 01 的 compose 定义声明契约；在 Plan 03 落地之前，健康检查失败是可以接受的。

用 `docker compose config` 验证 compose 文件解析。验证 `docker compose build platform` 成功。
  </action>
  <verify>
    <automated>docker compose config > /dev/null && grep -q "FROM golang:1.22-alpine AS build" Dockerfile && grep -q "gcr.io/distroless/static-debian12:nonroot" Dockerfile && grep -q "USER nonroot:nonroot" Dockerfile && grep -q "service_healthy" docker-compose.yml && grep -q "pg_isready" docker-compose.yml && grep -q "healthcheck:" docker-compose.yml && grep -q "^\.env$" .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` 包含字面 `FROM golang:1.22-alpine AS build` 和 `gcr.io/distroless/static-debian12:nonroot`
    - `Dockerfile` 包含 `USER nonroot:nonroot` (非 root 最终阶段)
    - `Dockerfile` 构建参数支持 `VERSION` 链接器标志 (`grep -q "main.version" Dockerfile`)
    - `docker-compose.yml` 解析干净: `docker compose config` 退出 0
    - `docker-compose.yml` 定义两个服务: `postgres`、`platform` (`grep -E "^  (postgres|platform):" docker-compose.yml | wc -l` 返回 2)
    - `docker-compose.yml` 声明 postgres 依赖的 `service_healthy` 条件
    - postgres 服务有 `pg_isready` 健康检查
    - `.dockerignore` 排除 `.git`、`.env`、`bin/`、`node_modules/`
    - `docker compose build platform` 退出 0 (镜像构建成功)
  </acceptance_criteria>
  <done>Docker 栈定义完成，带健康检查、非 root 运行时镜像、密钥从构建上下文中排除。</done>
</task>

</tasks>

<verification>
所有任务完成后，从仓库根目录运行:
1. `go build ./cmd/platform/...` 退出 0
2. `make build` 产生 `bin/platform`
3. `docker compose config` 退出 0
4. `docker compose build platform` 退出 0
5. `docker compose up -d postgres` 在 30 秒内将 postgres 置于健康状态 (`docker compose ps postgres` 显示 `healthy`)；然后 `docker compose down -v`
6. 目录树与 D-04 完全匹配 (顶层无 `pkg/`)
</verification>

<success_criteria>
- 单二进制文件可通过 `go build` 和 `make build` 构建
- Docker Compose 开发栈启动 postgres 并达到健康状态
- 所有 Phase 1 运行时 + 工具依赖在 go.mod 中声明
- 布局与 D-04 匹配，无偏差
- 无密钥随仓库发送 (`.gitignore` 和 `.dockerignore` 排除 `.env*`)
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-infrastructure/01-01-SUMMARY.md`，包含: 依赖已锁定、布局已建立、docker 栈已运行、与 CONTEXT 的偏差 (无偏差)、Plan 02 的注意事项。
</output>