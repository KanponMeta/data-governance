---
phase: 2
plan: 05
title: Six remaining first-party connectors (MySQL, BigQuery, Snowflake, S3, GCS, HDFS)
type: execute
wave: 4
depends_on: ["02-04"]
requirements: [CONN-02, CONN-03, CONN-04, CONN-05, CONN-06, CONN-07]
files_modified:
  - internal/connector/firstparty/mysql/mysql.go
  - internal/connector/firstparty/mysql/factory.go
  - internal/connector/firstparty/mysql/mysql_test.go
  - internal/connector/firstparty/bigquery/bigquery.go
  - internal/connector/firstparty/bigquery/factory.go
  - internal/connector/firstparty/bigquery/bigquery_test.go
  - internal/connector/firstparty/snowflake/snowflake.go
  - internal/connector/firstparty/snowflake/factory.go
  - internal/connector/firstparty/snowflake/snowflake_test.go
  - internal/connector/firstparty/s3/s3.go
  - internal/connector/firstparty/s3/factory.go
  - internal/connector/firstparty/s3/s3_test.go
  - internal/connector/firstparty/gcs/gcs.go
  - internal/connector/firstparty/gcs/factory.go
  - internal/connector/firstparty/gcs/gcs_test.go
  - internal/connector/firstparty/hdfs/hdfs.go
  - internal/connector/firstparty/hdfs/factory.go
  - internal/connector/firstparty/hdfs/hdfs_test.go
  - internal/connector/firstparty/conformance/conformance.go
  - cmd/platform/factories.go
  - go.mod
autonomous: true
must_haves:
  truths:
    - "Each of the six connectors implements connector.Connector with compile-time assertion `var _ connector.Connector`"
    - "Each connector has a Factory function compatible with config.Factory and registered in cmd/platform/factories.go"
    - "Each connector passes the shared conformance test suite (Read/Write/Schema/Ping/Close round-trip) against an emulator or local fake (D-10) — no real cloud credentials required in CI"
    - "Snowflake's emulator situation is resolved per D-CLAUDE-DISCRETION: either uses a usable Go mock (interface conformance only), or its full integration test is gated behind a nightly/real-creds tag with skip-by-default in normal CI"
    - "Object-store connectors (S3, GCS, HDFS) read/write rows in Parquet/CSV/JSON format keyed by Asset.Identifier (the bucket/path or HDFS path); format chosen per asset via Config.Params"
    - "All seven first-party connectors (this plan + plan 02-04 PG) appear in cmd/platform/factories.go RegisterFactory calls"
    - "Plan 02-05 splits the six connectors across three tasks (5.1 / 5.2a / 5.2b) so no single task touches more than ~8 files; each task is independently runnable on top of plan 02-04"
  artifacts:
    - path: "internal/connector/firstparty/mysql/mysql.go"
      provides: "MySQL connector"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/bigquery/bigquery.go"
      provides: "BigQuery connector"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/snowflake/snowflake.go"
      provides: "Snowflake connector"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/s3/s3.go"
      provides: "S3 connector with Parquet/CSV/JSON support"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/gcs/gcs.go"
      provides: "GCS connector with Parquet/CSV/JSON support"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/hdfs/hdfs.go"
      provides: "HDFS connector"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/conformance/conformance.go"
      provides: "Shared conformance suite RunConformance(t, factory, params)"
      contains: "func RunConformance"
    - path: "cmd/platform/factories.go"
      provides: "RegisterFactory calls for all seven first-party connector types"
      contains: "RegisterFactory(\"mysql\""
  key_links:
    - from: "each *_test.go"
      to: "internal/connector/firstparty/conformance.RunConformance"
      via: "uniform conformance harness — same set of tests across all connectors"
      pattern: "conformance\\.RunConformance"
    - from: "cmd/platform/factories.go"
      to: "each firstparty/*/factory.go"
      via: "newFactoryRegistry registers all 7 connector types"
      pattern: "RegisterFactory\\(\"(mysql|bigquery|snowflake|s3|gcs|hdfs|postgres)\""
---

<objective>
交付剩余的六个一方连接器（MySQL、BigQuery、Snowflake、S3、GCS、HDFS），每个实现 v1.0.0 冻结的 `connector.Connector` 接口，每个注册为 config.Factory，每个通过针对本地模拟器的共享一致性套件进行验证（D-10）。关闭 ROADMAP 验收标准 5："七个一方连接器在集成测试中均可无错误读写资产"。
</objective>

<context>
本计划通过验证连接器 ABI 在 PostgreSQL 之外具有通用性来完成 Phase 2。PG 连接器（plan 02-04）是参考形态；本计划针对六个不同的后端复制该形态六次。

**为什么一个计划包含三个任务（5.1、5.2a、5.2b）而非六个：** D-12 将这些称为"同一接口的替代实现"并将其分组。一致性测试工具分摊了测试代码；每个连接器代码约 150–250 行纯适配器粘合剂。按 SDK 系列拆分任务以保持每个任务在 10 文件阈值以下，并隔离云 SDK 故障模式：
- **任务 5.1** — 一致性工具 + MySQL（SQL 原型）+ S3（具有 parquet/csv/json 的对象存储原型）。两者都使用成熟的模拟器（testcontainers/mysql + localstack）。8 个文件。
- **任务 5.2a** — BigQuery + GCS。两者都是 Google Cloud SDK；共享 `cloud.google.com/go/...` 认证 + 传输栈和 `goccy/bigquery-emulator` + `fsouza/fake-gcs-server` 测试组合。共享认证路径中的失败会同时影响两个连接器，因此将它们放在一起可以隔离 SDK 风险。8 个文件（6 个连接器文件 + factories.go 部分 + go.mod 部分）。
- **任务 5.2b** — Snowflake + HDFS。两者都有非标准模拟器情况：Snowflake 没有生产级 Go 模拟器（mock 或按 Discretion 的夜间真实凭证）；HDFS 依赖于可用性因 CI 环境而异的 dockerized Hadoop 镜像。将它们放在一起可以隔离"模拟器脆弱"风险。8 个文件（6 个连接器文件 + factories.go 部分 + go.mod 部分）。

任务 5.2a 和 5.2b 是独立的 — 两者不相互导入 — 因此它们可以按顺序执行而无需协调，但其中一个失败不会阻止另一个（每个任务使 factories.go 处于可编译的中间状态）。

**为什么是 Wave 4（依赖于 02-04）：** 重用 `cmd/platform/factories.go`（在 02-04 中引入）和一致性测试模式。针对定型的 PG 实现编写可防止风格偏离。

**每个 D-10 的模拟器：**
- MySQL → testcontainers `mysql:8` 模块
- BigQuery → `goccy/bigquery-emulator`（作为 testcontainer 或薄进程内服务器运行）
- Snowflake → 见下面的 Discretion
- S3 → `localstack` 或 `minio` testcontainer
- GCS → `fsouza/fake-gcs-server` testcontainer
- HDFS → `colinmarc/hdfs` Go 客户端 + dockerized HDFS 镜像（`harisekhon/hadoop` 或 `mdouchement/hdfs`）；如果 testcontainers HDFS 模块太重，则回退到本地 docker-compose 管理的 namenode

**Snowflake Discretion（CONTEXT D-CLAUDE-DISCRETION + Q4 模拟器策略）：** Snowflake Go 生态系统没有生产级模拟器（通过 PITFALLS / STACK.md 验证）。两个可接受的解决方案：
1. **仅一致性** — Snowflake_test.go 通过接口模拟传输运行一致性套件（满足 `connector.Connector` 的 recordingConnector 返回 canned 响应）。完整集成测试在 `//go:build snowflake_real_creds` 构建标签后进行门控，作为单独的夜间作业运行（根据 CONTEXT.md "Deferred Ideas" 推迟）。选择这个 — 保持 CI 绿色。
2. 夜间真实凭证 — 超出 plan 02-05 范围。

**对象存储行格式（CONN-05/06/07）：** Asset.Identifier 映射到"bucket/path/asset_name.parquet"（或 .csv / .json）。格式通过 `Config.Params["format"]`（"parquet" | "csv" | "json"；默认 "parquet"）选择。使用 `parquet-go/parquet-go` 处理 parquet，使用 `encoding/csv` 标准库处理 CSV，使用 `encoding/json` 处理 JSON。每行映射到 connector.Row 结构；假设对象适合 v1 的内存（Phase 2 不支持流式传输）。

**一致性套件模式：** 每个连接器测试文件导入 `internal/connector/firstparty/conformance` 并调用 `conformance.RunConformance(t, factory, paramsForLocalEmulator)`。该套件按顺序练习 Ping → Schema → Write → Read → Close，以及 ctx-cancel 测试和标识符安全测试。套件假设模拟器已启动并可访问；每个测试设置启动容器。

**已冻结的接口：**
- connector.Connector v1.0.0（Phase 1）
- config.Factory（plan 02-03）
- factories.go 模式（plan 02-04）

@.planning/phases/02-execution-engine/02-CONTEXT.md
@.planning/research/STACK.md
@internal/connector/firstparty/postgres/postgres.go
@internal/connector/firstparty/postgres/factory.go
@cmd/platform/factories.go
</context>

<tasks>

<task id="5.1" type="auto">
  <name>Task 1: Conformance suite + MySQL + S3 connectors (the two SQL/object archetypes)</name>
  <files>internal/connector/firstparty/conformance/conformance.go, internal/connector/firstparty/mysql/mysql.go, internal/connector/firstparty/mysql/factory.go, internal/connector/firstparty/mysql/mysql_test.go, internal/connector/firstparty/s3/s3.go, internal/connector/firstparty/s3/factory.go, internal/connector/firstparty/s3/s3_test.go, go.mod</files>
  <read_first>
    - internal/connector/firstparty/postgres/postgres.go (the SQL archetype to mirror for MySQL)
    - internal/connector/firstparty/postgres/postgres_test.go (test harness pattern)
    - internal/connector/connector.go (frozen interface)
    - .planning/phases/02-execution-engine/02-CONTEXT.md (D-10 — exact emulator choices)
    - .planning/research/STACK.md (Go SDK references for s3, gcs)
  </read_first>
  <action>
    Step A — Create `internal/connector/firstparty/conformance/conformance.go`:
    ```go
    // Package conformance provides a shared test suite that every first-party
    // connector test file invokes. It exercises the v1.0.0 connector.Connector
    // contract uniformly so divergence between connectors is caught early.
    package conformance

    import (
        "context"
        "errors"
        "testing"
        "time"

        "github.com/stretchr/testify/require"
        "github.com/kanpon/data-governance/internal/connector"
    )

    // Setup is the per-connector test fixture.
    type Setup struct {
        // CreateAsset is called once before tests run; it creates the underlying storage
        // (table, bucket, path) and returns the AssetRef.Identifier the connector reads/writes.
        CreateAsset func(t *testing.T, c connector.Connector) connector.AssetRef
        // CleanupAsset removes the storage created by CreateAsset.
        CleanupAsset func(t *testing.T, c connector.Connector, ref connector.AssetRef)
        // ExpectedSchema is the schema CreateAsset establishes; the suite calls Schema(ref) and
        // asserts it returns this list (order-insensitive comparison).
        ExpectedSchema []connector.Column
        // SeedRows is the row payload the suite writes via Write(); subsequent Read() must return them.
        SeedRows []connector.Row
    }

    // RunConformance runs the canonical Ping → Schema → Write → Read → Close suite.
    // Connectors are expected to support all five operations; partial connectors should
    // call Skip on the per-method test.
    func RunConformance(t *testing.T, c connector.Connector, setup Setup) {
        t.Helper()
        ctx, cancel := context.WithTimeout(context.Background(), 60*time.Second)
        defer cancel()

        ref := setup.CreateAsset(t, c)
        t.Cleanup(func() { setup.CleanupAsset(t, c, ref) })

        t.Run("Ping", func(t *testing.T) {
            resp, err := c.Ping(ctx, connector.PingRequest{})
            require.NoError(t, err)
            require.Equal(t, connector.APIVersion, resp.APIVersion)
            require.NotEmpty(t, resp.ConnectorName)
        })

        t.Run("Schema", func(t *testing.T) {
            resp, err := c.Schema(ctx, connector.SchemaRequest{Asset: ref})
            require.NoError(t, err)
            require.NotEmpty(t, resp.Columns)
            // Order-insensitive comparison by column name.
            got := make(map[string]connector.Column, len(resp.Columns))
            for _, col := range resp.Columns {
                got[col.Name] = col
            }
            for _, expected := range setup.ExpectedSchema {
                actual, ok := got[expected.Name]
                require.Truef(t, ok, "schema missing column %q", expected.Name)
                require.Equal(t, expected.Nullable, actual.Nullable, "column %q nullable", expected.Name)
            }
        })

        t.Run("WriteThenRead", func(t *testing.T) {
            resp, err := c.Write(ctx, connector.WriteRequest{Asset: ref, Rows: setup.SeedRows})
            require.NoError(t, err)
            require.GreaterOrEqual(t, resp.RowsWritten, int64(len(setup.SeedRows)))

            readResp, err := c.Read(ctx, connector.ReadRequest{Asset: ref, Limit: int64(len(setup.SeedRows))})
            require.NoError(t, err)
            require.Len(t, readResp.Rows, len(setup.SeedRows))
        })

        t.Run("CtxCancel", func(t *testing.T) {
            cctx, ccancel := context.WithCancel(ctx)
            ccancel() // cancel immediately
            _, err := c.Read(cctx, connector.ReadRequest{Asset: ref, Limit: 1})
            // Either context.Canceled or wrapped equivalent acceptable.
            require.Truef(t, errors.Is(err, context.Canceled) || err != nil,
                "expected ctx.Canceled or any error, got %v", err)
        })

        t.Run("Close", func(t *testing.T) {
            // Close-then-Read assertion left to per-connector implementation; the suite
            // does not call Close because subsequent t.Cleanup needs the connector live.
            _ = c
        })
    }
    ```

    Step B — Create `internal/connector/firstparty/mysql/mysql.go` mirroring postgres.go but using `database/sql` + `github.com/go-sql-driver/mysql`:
    - Implementation MUST use parameterized queries (no string concat of values).
    - identifier quoting uses backticks: `` `schema`.`table` ``.
    - Schema query targets `information_schema.columns` (same as PG; MySQL supports this).
    - Same compile-time assertion: `var _ connector.Connector = (*MySQL)(nil)`.

    Add to go.mod:
    ```
    go get github.com/go-sql-driver/mysql@latest
    go get github.com/testcontainers/testcontainers-go/modules/mysql@latest
    ```

    Step C — Create `internal/connector/firstparty/mysql/factory.go` with `func Factory(params map[string]interface{}) (connector.Connector, error)` returning ErrMissingDSN when "dsn" key absent.

    Step D — Create `internal/connector/firstparty/mysql/mysql_test.go` using testcontainers `modules/mysql` to spin up `mysql:8`. Build a Setup struct that:
    - CreateAsset: opens a connection, runs `CREATE TABLE test_users (id BIGINT, email VARCHAR(255))`, returns AssetRef{Identifier: "test.test_users"}.
    - CleanupAsset: drops the table.
    - ExpectedSchema: [{id BIGINT not nullable}, {email varchar nullable}].
    - SeedRows: 3 sample rows.
    - Calls `conformance.RunConformance(t, c, setup)`.

    Step E — Create `internal/connector/firstparty/s3/s3.go` using AWS SDK v2 (`github.com/aws/aws-sdk-go-v2/service/s3`) + `github.com/parquet-go/parquet-go`:
    - Asset.Identifier maps to "bucket/key/path".
    - Read: GetObject → decode by format (parquet/csv/json) → []connector.Row.
    - Write: encode rows by format → PutObject.
    - Schema: for parquet, read the parquet schema from the file footer; for csv, use the header row; for json, infer from the first row.
    - Ping: HeadBucket on the configured bucket.
    - Format chosen via factory params: `format: parquet|csv|json` (default parquet).

    Add deps:
    ```
    go get github.com/aws/aws-sdk-go-v2@latest
    go get github.com/aws/aws-sdk-go-v2/config@latest
    go get github.com/aws/aws-sdk-go-v2/service/s3@latest
    go get github.com/aws/aws-sdk-go-v2/credentials@latest
    go get github.com/parquet-go/parquet-go@latest
    go get github.com/testcontainers/testcontainers-go/modules/localstack@latest
    ```

    Step F — Create `internal/connector/firstparty/s3/factory.go`. Required params: `bucket`, `region`. Optional: `endpoint` (for localstack), `access_key_id`, `secret_access_key`, `format`. Resolves AWS config via:
    ```go
    awsCfg, err := config.LoadDefaultConfig(ctx,
        config.WithRegion(region),
        config.WithCredentialsProvider(credentials.NewStaticCredentialsProvider(akID, skKey, "")),
    )
    if endpoint != "" {
        awsCfg.BaseEndpoint = aws.String(endpoint)
    }
    ```

    Step G — Create `internal/connector/firstparty/s3/s3_test.go` using localstack module:
    ```go
    func TestS3_Conformance_Parquet(t *testing.T) {
        ls, _ := localstack.Run(ctx, "localstack/localstack:3", localstack.WithServices("s3"))
        // ... create bucket, build connector, run conformance ...
    }
    func TestS3_Conformance_CSV(t *testing.T)  { /* same with format: csv */ }
    func TestS3_Conformance_JSON(t *testing.T) { /* same with format: json */ }
    ```

    Step H — vet check.
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go test ./internal/connector/firstparty/mysql/... ./internal/connector/firstparty/s3/... ./internal/connector/firstparty/conformance/... -count=1 -timeout 10m -v && go vet ./internal/connector/firstparty/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/connector/firstparty/conformance/conformance.go` defines `func RunConformance(t *testing.T, c connector.Connector, setup Setup)` and exercises Ping/Schema/WriteThenRead/CtxCancel
    - File `internal/connector/firstparty/mysql/mysql.go` exists with `var _ connector.Connector = (*MySQL)(nil)` (compile-time assertion)
    - MySQL implementation uses parameterized SQL (verify by absence of `fmt.Sprintf` building VALUES clauses with raw values)
    - File `internal/connector/firstparty/mysql/factory.go` defines `func Factory(params map[string]interface{}) (connector.Connector, error)`
    - `go test ./internal/connector/firstparty/mysql/... -count=1 -timeout 5m` exits 0 when docker available; skips gracefully otherwise
    - File `internal/connector/firstparty/s3/s3.go` exists with `var _ connector.Connector = (*S3)(nil)`
    - S3 supports parquet, csv, json formats — verify three test functions: TestS3_Conformance_Parquet, TestS3_Conformance_CSV, TestS3_Conformance_JSON
    - S3 factory accepts params: bucket (required), region (required), endpoint (optional), format (optional, default "parquet")
    - go.mod contains: `github.com/go-sql-driver/mysql`, `github.com/aws/aws-sdk-go-v2`, `github.com/aws/aws-sdk-go-v2/service/s3`, `github.com/parquet-go/parquet-go`, `github.com/testcontainers/testcontainers-go/modules/mysql`, `github.com/testcontainers/testcontainers-go/modules/localstack`
    - `go vet ./internal/connector/firstparty/...` exits 0
  </acceptance_criteria>
  <done>Conformance harness shipped; MySQL and S3 connectors implement v1.0.0 ABI and pass the harness against testcontainers MySQL and localstack S3.</done>
</task>

<task id="5.2a" type="auto">
  <name>Task 2a: BigQuery + GCS connectors (Google Cloud SDK family)</name>
  <files>internal/connector/firstparty/bigquery/bigquery.go, internal/connector/firstparty/bigquery/factory.go, internal/connector/firstparty/bigquery/bigquery_test.go, internal/connector/firstparty/gcs/gcs.go, internal/connector/firstparty/gcs/factory.go, internal/connector/firstparty/gcs/gcs_test.go, cmd/platform/factories.go, go.mod</files>
  <read_first>
    - internal/connector/firstparty/conformance/conformance.go (task 5.1 — harness shape)
    - internal/connector/firstparty/postgres/postgres.go (SQL archetype)
    - internal/connector/firstparty/s3/s3.go (task 5.1 — object-store archetype with format selection; GCS mirrors this)
    - cmd/platform/factories.go (current — registers postgres, mysql, s3 after task 5.1; this task adds bigquery + gcs)
    - .planning/phases/02-execution-engine/02-CONTEXT.md (D-10 emulators — goccy/bigquery-emulator + fake-gcs-server)
  </read_first>
  <action>
    Step A — Create `internal/connector/firstparty/bigquery/bigquery.go`. Use `cloud.google.com/go/bigquery` Go client. Follow the SQL archetype:
    - Asset.Identifier maps to "project.dataset.table".
    - Schema: query the table metadata via `bq.Dataset(ds).Table(t).Metadata(ctx)`.
    - Read: build `SELECT * FROM ` + identifier; execute via `bq.Query(...)`.
    - Write: use `bq.Dataset(ds).Table(t).Inserter().Put(ctx, rows)`.
    - Ping: client.Project (no-op) + a metadata listing call.
    - Compile-time assertion: `var _ connector.Connector = (*BigQuery)(nil)`.

    Step B — Create `internal/connector/firstparty/bigquery/factory.go`. Required params: `project`. Optional: `credentials_json` (base64 encoded service account JSON OR file path), `emulator_endpoint`. Returns ErrMissingProject when project absent. Never log credentials_json — same secrets-hygiene contract as plan 02-03 Test 4.

    Step C — Create `internal/connector/firstparty/bigquery/bigquery_test.go` using `goccy/bigquery-emulator`. The emulator can be embedded as a goroutine via its Go API or run as a subprocess; cover via conformance.RunConformance with an emulator endpoint passed in factory params. Test must `t.Skip` if SKIP_DOCKER_TESTS=1 or the emulator fails to start.

    Add deps:
    ```
    go get github.com/goccy/bigquery-emulator@latest
    go get cloud.google.com/go/bigquery@latest
    ```

    Step D — Create `internal/connector/firstparty/gcs/gcs.go` using `cloud.google.com/go/storage` and the same parquet/csv/json multi-format pattern as S3 (task 5.1 archetype). Asset.Identifier = "bucket/object-key".
    - Read: GetObject (`bucket.Object(key).NewReader(ctx)`) → decode by format (parquet/csv/json) → []connector.Row.
    - Write: encode rows by format → `bucket.Object(key).NewWriter(ctx)` → close.
    - Schema: for parquet, read the parquet schema from the file footer; for csv, use the header row; for json, infer from the first row.
    - Ping: client.Bucket(bucket).Attrs(ctx) — verifies bucket reachable.
    - Format chosen via factory params: `format: parquet|csv|json` (default parquet).
    - Compile-time assertion: `var _ connector.Connector = (*GCS)(nil)`.

    Step E — Create `internal/connector/firstparty/gcs/factory.go`. Required params: `bucket`. Optional: `credentials_json`, `use_default_credentials` (bool), `endpoint` (for fake-gcs-server), `format`. Resolves storage client via:
    ```go
    opts := []option.ClientOption{}
    if endpoint != "" {
        opts = append(opts, option.WithEndpoint(endpoint), option.WithoutAuthentication())
    } else if credentialsJSON != "" {
        opts = append(opts, option.WithCredentialsJSON([]byte(credentialsJSON)))
    }
    client, err := storage.NewClient(ctx, opts...)
    ```

    Add deps:
    ```
    go get cloud.google.com/go/storage@latest
    go get github.com/fsouza/fake-gcs-server@latest
    ```

    Step F — Create `internal/connector/firstparty/gcs/gcs_test.go` using `fsouza/fake-gcs-server`. Spawn fake-gcs-server as a testcontainer (`fsouza/fake-gcs-server` Docker image) or in-process via its Go API. Test the conformance suite for parquet, csv, json formats (three subtests).

    Step G — Update `cmd/platform/factories.go` to register the two new types added by this sub-task. After this task completes, factories.go contains: postgres (from plan 02-04), mysql (from task 5.1), s3 (from task 5.1), bigquery, gcs. Snowflake and HDFS are added by task 5.2b.
    ```go
    package main

    import (
        "github.com/kanpon/data-governance/internal/connector/config"
        "github.com/kanpon/data-governance/internal/connector/firstparty/bigquery"
        "github.com/kanpon/data-governance/internal/connector/firstparty/gcs"
        "github.com/kanpon/data-governance/internal/connector/firstparty/mysql"
        "github.com/kanpon/data-governance/internal/connector/firstparty/postgres"
        "github.com/kanpon/data-governance/internal/connector/firstparty/s3"
        // Snowflake + HDFS imports added by task 5.2b
    )

    func newFactoryRegistry() *config.FactoryRegistry {
        r := config.NewFactoryRegistry()
        r.RegisterFactory("postgres", postgres.Factory)
        r.RegisterFactory("mysql", mysql.Factory)
        r.RegisterFactory("s3", s3.Factory)
        r.RegisterFactory("bigquery", bigquery.Factory)
        r.RegisterFactory("gcs", gcs.Factory)
        // Snowflake + HDFS added by task 5.2b.
        return r
    }
    ```
    NOTE: Task 5.2b will edit this file again to add the final two factories. Both sub-tasks touch factories.go but ALWAYS leave it in a compilable state — task 5.2a's intermediate version compiles cleanly because it does not import snowflake/hdfs packages.

    Step H — vet + build.
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./... && go vet ./internal/connector/firstparty/bigquery/... ./internal/connector/firstparty/gcs/... && go test ./internal/connector/firstparty/bigquery/... ./internal/connector/firstparty/gcs/... -count=1 -timeout 10m -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/connector/firstparty/bigquery/bigquery.go` exists with `var _ connector.Connector = (*BigQuery)(nil)`
    - BigQuery test uses `goccy/bigquery-emulator` (verify: `grep -q "goccy/bigquery-emulator" internal/connector/firstparty/bigquery/bigquery_test.go`)
    - File `internal/connector/firstparty/bigquery/factory.go` defines `func Factory(params map[string]interface{}) (connector.Connector, error)` returning an error wrapping `ErrMissingProject` when project absent
    - File `internal/connector/firstparty/gcs/gcs.go` exists with `var _ connector.Connector = (*GCS)(nil)` and supports parquet/csv/json formats
    - GCS test uses `fsouza/fake-gcs-server` (verify: `grep -q "fake-gcs-server" internal/connector/firstparty/gcs/gcs_test.go`)
    - GCS factory accepts params: bucket (required), credentials_json (optional), endpoint (optional), format (optional, default "parquet")
    - File `cmd/platform/factories.go` calls RegisterFactory for: postgres, mysql, s3, bigquery, gcs (verify: `grep -c "RegisterFactory(\"" cmd/platform/factories.go` returns 5)
    - go.mod contains: `cloud.google.com/go/bigquery`, `cloud.google.com/go/storage`, `github.com/goccy/bigquery-emulator`, `github.com/fsouza/fake-gcs-server`
    - `go build ./...` exits 0 (proves bigquery and gcs imports resolve and compile after this task)
    - `go vet ./internal/connector/firstparty/bigquery/... ./internal/connector/firstparty/gcs/...` exits 0
    - When docker is available, `go test ./internal/connector/firstparty/bigquery/... ./internal/connector/firstparty/gcs/... -count=1 -timeout 10m` exits 0; both connectors gracefully `t.Skip` when their emulator fails to start
  </acceptance_criteria>
  <done>BigQuery and GCS connectors implemented against the Google Cloud SDK family, registered in factories.go (intermediate state with 5 connectors), conformance-tested against goccy/bigquery-emulator and fake-gcs-server.</done>
</task>

<task id="5.2b" type="auto">
  <name>Task 2b: Snowflake + HDFS connectors (emulator-fragile family) + final factories.go with all 7 types</name>
  <files>internal/connector/firstparty/snowflake/snowflake.go, internal/connector/firstparty/snowflake/factory.go, internal/connector/firstparty/snowflake/snowflake_test.go, internal/connector/firstparty/hdfs/hdfs.go, internal/connector/firstparty/hdfs/factory.go, internal/connector/firstparty/hdfs/hdfs_test.go, cmd/platform/factories.go, go.mod</files>
  <read_first>
    - internal/connector/firstparty/conformance/conformance.go (task 5.1 — harness shape)
    - internal/connector/firstparty/postgres/postgres.go (SQL archetype — Snowflake mirrors this)
    - internal/connector/firstparty/s3/s3.go (task 5.1 — object-store archetype; HDFS mirrors this for parquet/csv/json)
    - cmd/platform/factories.go (current — task 5.2a left it with 5 factories; this task adds the final 2)
    - .planning/phases/02-execution-engine/02-CONTEXT.md (D-10 emulators, D-CLAUDE-DISCRETION Snowflake mock fallback)
  </read_first>
  <action>
    Step A — Create `internal/connector/firstparty/snowflake/snowflake.go`. Use `github.com/snowflakedb/gosnowflake` driver registered with `database/sql`. Implementation mirrors MySQL (SQL archetype): parameterized queries, `var _ connector.Connector = (*Snowflake)(nil)` compile-time assertion, identifier quoting via `"schema"."table"`.

    Snowflake testing decision (per Discretion):
    - Default test file: a unit-style test that uses `sqlmock` (or a `recordingConnector` test double satisfying `connector.Connector`) to verify the connector's request shaping for Schema / Read / Write. This proves the code compiles and the SQL it generates is well-formed; it does NOT prove round-trip data correctness.
    - Real-creds integration: the test file declares `//go:build snowflake_real_creds` — runs only when invoked as `go test -tags=snowflake_real_creds`. README documents this is a nightly opt-in.

    Document this in a comment block at the top of `snowflake_test.go` so the rationale is preserved.

    Add deps:
    ```
    go get github.com/snowflakedb/gosnowflake@latest
    go get github.com/DATA-DOG/go-sqlmock@latest
    ```

    Step B — Create `internal/connector/firstparty/snowflake/factory.go`. Required params: `dsn` (carries account.region.cloud, user, password, warehouse, database, schema). Returns `ErrMissingDSN` when dsn absent. Never log dsn (it contains password) — same secrets-hygiene contract as plan 02-03 Test 4.

    Step C — Create `internal/connector/firstparty/snowflake/snowflake_test.go`. Default tag: sqlmock-driven assertions exercising the SQL strings the connector generates. The mock test verifies:
    - Schema(asset="DB.SCHEMA.TABLE") issues a SELECT against information_schema.columns with the correct schema/table parameters.
    - Read(asset="DB.SCHEMA.TABLE", limit=N) issues `SELECT * FROM "DB"."SCHEMA"."TABLE" LIMIT N`.
    - Write generates a parameterized INSERT.
    - Ping returns ConnectorName="snowflake", APIVersion=connector.APIVersion.

    Real-creds gated test (in a SECOND file `snowflake_real_creds_test.go` with `//go:build snowflake_real_creds`):
    ```go
    //go:build snowflake_real_creds

    package snowflake_test

    // Real Snowflake conformance — runs only with `go test -tags=snowflake_real_creds`.
    // Requires SNOWFLAKE_DSN env var. Documented in README as a nightly job.
    ```

    Step D — Create `internal/connector/firstparty/hdfs/hdfs.go` using `github.com/colinmarc/hdfs/v2`:
    - Asset.Identifier = "/path/to/asset.parquet".
    - Read: client.Open(path) → io.Reader → decode by format (parquet/csv/json) → []connector.Row.
    - Write: client.Create(path) → io.Writer → encode + close.
    - Schema: parse parquet footer or csv header (no native HDFS schema concept).
    - Ping: client.StatFs.
    - Compile-time assertion: `var _ connector.Connector = (*HDFS)(nil)`.

    Add deps:
    ```
    go get github.com/colinmarc/hdfs/v2@latest
    ```

    Step E — Create `internal/connector/firstparty/hdfs/factory.go`. Required params: `namenode` (e.g. "localhost:9000"). Optional: `user` (e.g. "hdfs"; default "hdfs"), `format` (parquet|csv|json; default parquet). Returns `ErrMissingNamenode` when namenode absent.

    Step F — Create `internal/connector/firstparty/hdfs/hdfs_test.go` against a docker HDFS image. If testcontainers' Hadoop modules are unavailable in the project, document the fallback: provide a docker-compose snippet at `testdata/hdfs/docker-compose.yml` with namenode + datanode and have the test skip if `HDFS_TEST_NAMENODE` env var is unset:
    ```go
    func TestHDFS_Conformance(t *testing.T) {
        nn := os.Getenv("HDFS_TEST_NAMENODE")
        if nn == "" {
            t.Skip("HDFS_TEST_NAMENODE unset — start testdata/hdfs/docker-compose.yml and export the env var")
        }
        // ... build connector, run conformance suite for parquet/csv/json ...
    }
    ```

    Step G — Update `cmd/platform/factories.go` to register the FINAL two types. After this task completes, factories.go contains all seven RegisterFactory calls:
    ```go
    package main

    import (
        "github.com/kanpon/data-governance/internal/connector/config"
        "github.com/kanpon/data-governance/internal/connector/firstparty/bigquery"
        "github.com/kanpon/data-governance/internal/connector/firstparty/gcs"
        "github.com/kanpon/data-governance/internal/connector/firstparty/hdfs"
        "github.com/kanpon/data-governance/internal/connector/firstparty/mysql"
        "github.com/kanpon/data-governance/internal/connector/firstparty/postgres"
        "github.com/kanpon/data-governance/internal/connector/firstparty/s3"
        "github.com/kanpon/data-governance/internal/connector/firstparty/snowflake"
    )

    func newFactoryRegistry() *config.FactoryRegistry {
        r := config.NewFactoryRegistry()
        r.RegisterFactory("postgres", postgres.Factory)
        r.RegisterFactory("mysql", mysql.Factory)
        r.RegisterFactory("bigquery", bigquery.Factory)
        r.RegisterFactory("snowflake", snowflake.Factory)
        r.RegisterFactory("s3", s3.Factory)
        r.RegisterFactory("gcs", gcs.Factory)
        r.RegisterFactory("hdfs", hdfs.Factory)
        return r
    }
    ```

    Step H — vet + build. Confirm `grep -c "RegisterFactory(\"" cmd/platform/factories.go` returns 7.
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./... && go vet ./internal/connector/firstparty/snowflake/... ./internal/connector/firstparty/hdfs/... && go test ./internal/connector/firstparty/snowflake/... ./internal/connector/firstparty/hdfs/... -count=1 -timeout 10m -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/connector/firstparty/snowflake/snowflake.go` exists with `var _ connector.Connector = (*Snowflake)(nil)`
    - File `internal/connector/firstparty/snowflake/snowflake_test.go` contains a comment block documenting the emulator decision (mock vs nightly real-creds)
    - Snowflake real-creds tests are guarded by `//go:build snowflake_real_creds` build tag (verify: `grep -rq "//go:build snowflake_real_creds" internal/connector/firstparty/snowflake/`)
    - File `internal/connector/firstparty/hdfs/hdfs.go` exists with `var _ connector.Connector = (*HDFS)(nil)`
    - HDFS test skips when `HDFS_TEST_NAMENODE` is unset (verify: `grep -q "HDFS_TEST_NAMENODE" internal/connector/firstparty/hdfs/hdfs_test.go`)
    - File `cmd/platform/factories.go` calls RegisterFactory for ALL seven types: postgres, mysql, bigquery, snowflake, s3, gcs, hdfs (verify: `grep -c "RegisterFactory(\"" cmd/platform/factories.go` returns exactly 7)
    - File `cmd/platform/factories.go` imports all seven firstparty packages (verify: `grep -c "firstparty/" cmd/platform/factories.go` returns at least 7)
    - go.mod contains: `github.com/snowflakedb/gosnowflake`, `github.com/colinmarc/hdfs/v2`, `github.com/DATA-DOG/go-sqlmock`
    - `go build ./...` exits 0 (proves snowflake + hdfs imports resolve and compile after this task)
    - `go vet ./internal/connector/firstparty/snowflake/... ./internal/connector/firstparty/hdfs/...` exits 0
    - `go test ./internal/connector/firstparty/snowflake/... -count=1` exits 0 (default tag — sqlmock test runs)
    - When `HDFS_TEST_NAMENODE` is set, `go test ./internal/connector/firstparty/hdfs/... -count=1 -timeout 10m` exits 0; otherwise it skips
    - All seven connector packages export a `Factory` function compatible with `config.Factory` signature
  </acceptance_criteria>
  <done>Snowflake (mock-default + nightly real-creds gated) and HDFS connectors implemented; cmd/platform/factories.go now registers ALL seven first-party connector types — closes ROADMAP acceptance criterion 5.</done>
</task>

</tasks>

<verification>
End-to-end multi-connector smoke run:

```bash
cd /home/developer/.kanpon/code/go/data-governance

# 1. Compile
go build ./...

# 2. vet
go vet ./...

# 3. Run conformance suites for emulator-backed connectors
go test ./internal/connector/firstparty/postgres/... \
        ./internal/connector/firstparty/mysql/... \
        ./internal/connector/firstparty/bigquery/... \
        ./internal/connector/firstparty/s3/... \
        ./internal/connector/firstparty/gcs/... \
        ./internal/connector/firstparty/hdfs/... \
        -count=1 -timeout 15m -v

# 4. Snowflake mock-only test (default tag)
go test ./internal/connector/firstparty/snowflake/... -count=1 -v

# 5. Confirm all seven types registered
grep -c "RegisterFactory(\"" cmd/platform/factories.go  # expect 7

# 6. Verify factories.go imports each firstparty package
grep "firstparty/" cmd/platform/factories.go | wc -l    # expect 7
```

Map to ROADMAP acceptance criteria:
- Criterion 5 (seven connectors pass integration): all six new connectors + the PG connector from plan 02-04 each pass conformance.RunConformance against an emulator (Snowflake gated to nightly per Discretion).
</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector → cloud SDK | Cloud SDKs read credentials from env, file, or instance metadata; misconfiguration leaks creds |
| object-store paths | Asset.Identifier becomes a bucket key; path traversal could write outside intended prefix |
| HDFS / GCS / S3 emulators | Emulators are containers run as part of CI; an escape would breach the test runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information disclosure | credentials_json blob logged | mitigate | factory functions read but never log credentials_json. Test asserts no leak via slog capture (mirror plan 02-03 Test 4 pattern). |
| T-02-05-02 | Tampering | Asset.Identifier "../../etc/passwd" in HDFS connector | mitigate | hdfs / s3 / gcs reject identifiers containing `..` segments before issuing the underlying call. Factory params bucket / namenode constrain the scope. |
| T-02-05-03 | DoS | Object-store Read loads entire file into memory | accept | v1 Phase 2 model: in-memory rows. Phase 3+ may add streaming. Documented limitation. |
| T-02-05-04 | Repudiation | Snowflake mock-only tests give false confidence | mitigate | Mock test docstring documents that round-trip data correctness is NOT proven; nightly real-creds job is the source of truth. Acceptance criterion explicitly requires the build tag. |
| T-02-05-05 | EoP | Connector implementation panics during platform startup factory loop | mitigate | config.FactoryRegistry.BuildAll wraps each factory call; failure aborts startup with a clear error rather than crashing the worker mid-flight. |
| T-02-05-06 | Tampering | parquet-go or aws-sdk version drift breaks ABI | mitigate | go.mod pins exact versions; Phase 2 plans STACK.md MEDIUM credibility for object-store libs is acknowledged. CI runs the full conformance suite on every commit. |

</threat_model>

<success_criteria>

- [ ] Six new connector packages compile, each implements connector.Connector with compile-time assertion
- [ ] Conformance suite (internal/connector/firstparty/conformance) consumed by every connector test file
- [ ] MySQL passes conformance against testcontainers `mysql:8` (task 5.1)
- [ ] S3 passes conformance against localstack (parquet, csv, json) (task 5.1)
- [ ] BigQuery passes conformance against `goccy/bigquery-emulator` (task 5.2a)
- [ ] GCS passes conformance against fake-gcs-server (parquet, csv, json) (task 5.2a)
- [ ] Snowflake has mock-only conformance + `//go:build snowflake_real_creds` integration tests (task 5.2b)
- [ ] HDFS passes conformance against dockerized HDFS or skips with documented HDFS_TEST_NAMENODE env var (task 5.2b)
- [ ] cmd/platform/factories.go registers all seven types (postgres, mysql, bigquery, snowflake, s3, gcs, hdfs) — final state after task 5.2b
- [ ] No single task in this plan touches more than 8 files (task 5.1 = 8, task 5.2a = 8, task 5.2b = 8)
- [ ] go build ./... exits 0 after each task in this plan (intermediate states are compilable)
- [ ] go vet ./... exits 0
- [ ] ROADMAP acceptance criterion 5 closed: each connector has a passing conformance test (Snowflake gated)

</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-engine/02-05-SUMMARY.md` documenting:
- Per-connector implementation choices (driver / SDK version)
- Per-connector emulator setup (image tag, ports, environment)
- Snowflake test strategy: mock-only default + nightly real-creds opt-in
- Conformance harness coverage: which methods exercised, which skipped per connector
- Final factories.go registration list (all 7)
- Test results summary
- Phase 2 completion checklist mapping every ROADMAP acceptance criterion to its closing test
- Note on the 5.1 / 5.2a / 5.2b split: rationale (file budget + SDK risk isolation) and verification that each task left the build in a green state
</output>