---
phase: 2
plan: 04
title: PostgreSQL connector + worker / materialize CLI subcommands + stale-run reaper + e2e acceptance test
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
requirements: [ORCH-10, CONN-01]
files_modified:
  - internal/connector/firstparty/postgres/postgres.go
  - internal/connector/firstparty/postgres/postgres_test.go
  - internal/connector/firstparty/postgres/factory.go
  - internal/run/reaper.go
  - internal/run/reaper_test.go
  - cmd/platform/main.go
  - cmd/platform/worker.go
  - cmd/platform/materialize.go
  - cmd/platform/factories.go
  - testdata/integration/example_assets.go
  - testdata/integration/config.yaml
  - test/integration/e2e_postgres_test.go
  - go.mod
autonomous: true
must_haves:
  truths:
    - "PostgreSQL connector implements connector.Connector for Read, Write, Schema, Ping using pgxpool"
    - "Connector singleton holds the pool across the process lifetime (D-08); Close() drains it"
    - "`./platform worker` subcommand starts a worker loop that calls run.ClaimNext, then runtime.Executor.Run, then loops"
    - "`./platform materialize <asset>` subcommand inserts a queued run, waits synchronously for it to reach a terminal state, prints succeeded/failed, and exits with the corresponding status code"
    - "`./platform materialize <asset> --detach` returns immediately after inserting the queued run, printing the run ID"
    - "Integration test: ./platform materialize users_clean against a testcontainers PostgreSQL writes the expected rows AND emits the full run.* event sequence (closes acceptance criterion 4)"
    - "Materialize CLI authenticates via the same JWT path as REST (Phase 1) when authentication is enabled; for the local single-binary case a service-account token is loaded from PLATFORM_SERVICE_TOKEN env var"
    - "Worker spawns a stale-run reaper goroutine (run.StaleRunReaper) that scans every 60s for runs in {starting,running} with last_heartbeat < NOW() - 5m and resets them to 'queued' via run.TransitionForReset; a worker that crashes mid-execution has its run recovered within 6 minutes (5m staleness threshold + 60s scan interval)"
  artifacts:
    - path: "internal/connector/firstparty/postgres/postgres.go"
      provides: "Postgres connector implementing connector.Connector v1.0.0"
      contains: "var _ connector.Connector"
    - path: "internal/connector/firstparty/postgres/factory.go"
      provides: "config.Factory for type=\"postgres\""
      contains: "func Factory"
    - path: "internal/run/reaper.go"
      provides: "Stale-run reaper goroutine that recovers crashed runs (D-14 Option B)"
      contains: "func StaleRunReaper"
    - path: "cmd/platform/worker.go"
      provides: "worker subcommand"
      contains: "func runWorker"
    - path: "cmd/platform/materialize.go"
      provides: "materialize subcommand"
      contains: "func runMaterialize"
    - path: "cmd/platform/factories.go"
      provides: "FactoryRegistry initializer that registers all first-party connector factories"
      contains: "RegisterFactory"
    - path: "test/integration/e2e_postgres_test.go"
      provides: "End-to-end test exercising materialize CLI against testcontainers Postgres"
      contains: "testcontainers"
  key_links:
    - from: "cmd/platform/main.go"
      to: "cmd/platform/worker.go materialize.go"
      via: "switch on os.Args[1] adds 'worker' and 'materialize' cases"
      pattern: "case \"worker\":|case \"materialize\":"
    - from: "internal/connector/firstparty/postgres/factory.go"
      to: "internal/connector/config.FactoryRegistry"
      via: "factories.go registers Factory under type=\"postgres\""
      pattern: "RegisterFactory\\(\"postgres\""
    - from: "cmd/platform/materialize.go"
      to: "internal/runtime.Executor"
      via: "inserts run row → spawns Executor.Run → polls runs.state until terminal"
      pattern: "Executor.Run\\("
    - from: "cmd/platform/worker.go runWorker"
      to: "internal/run.StaleRunReaper"
      via: "worker spawns the reaper goroutine at startup; goroutine exits when ctx is canceled"
      pattern: "StaleRunReaper"
    - from: "internal/run.StaleRunReaper"
      to: "runs.last_heartbeat column (plan 02-02) + executor heartbeat tick (plan 02-03)"
      via: "reaper SELECT WHERE state IN ('starting','running') AND last_heartbeat < NOW() - interval '5 minutes'; UPDATE state='queued' via TransitionForReset"
      pattern: "last_heartbeat < NOW"
---

<objective>
交付参考连接器实现（通过 pgxpool 的 PostgreSQL）、两个 CLI 子命令（`worker` 和 `materialize`）以关闭 ROADMAP 验收标准 4（"通过 CLI 命令可按需触发资产物化，使用 PostgreSQL 连接器针对本地数据库完整运行成功"），以及满足 D-14 "基础设施故障恢复" 意图的过期运行回收器。提供针对 testcontainers PostgreSQL 运行完整流水线的 e2e 集成测试。
</objective>

<context>
本计划依赖于 plan 02-01（asset SDK）、02-02（run claim + DAG + last_heartbeat column）和 02-03（executor + concurrency + config + heartbeat goroutine）。这是第一个产生可工作的最终用户二进制命令的计划。

**为什么是 Wave 3：** 需要 Executor（02-03）、DAG（02-02）、Asset SDK（02-01）和 config FactoryRegistry（02-03）。

**PostgreSQL 是参考实现（D-12）：** PG 验证架构。其余六个连接器（plan 02-05）遵循相同的 Factory + Connector 模式。

**CLI 认证（D-02 + Phase 1 重用）：** CONTEXT.md `code_context` 注释说明"auth JWT middleware — `materialize` CLI 通过与 REST API 相同的 JWT 路径进行认证；被重用而非重建。"对于本地单二进制开发者场景，服务账户 JWT 从 `PLATFORM_SERVICE_TOKEN` 环境变量加载；如果不存在且设置了 `--no-auth` 标志（仅用于开发），则绕过认证。生产部署设置该环境变量。

**Materialize CLI 默认模式（D-CLAUDE-DISCRETION）：** 同步模式（等待运行终止）。`--detach` 标志立即插入排队的运行并返回 UUID。

**过期运行回收器（D-14 Option B — 实现"基础设施故障恢复"意图）：**

D-14 在 CONTEXT.md 中说明"River 处理基础设施故障（worker 崩溃、网络抖动 — 其原生 max_attempts + 重试策略）"。Phase 2 通过过期运行回收器 goroutine 实现该意图，而非通过 River.max_attempts。在此记录理由供执行者和未来重新规划者参考：

1. **单二进制简单性：** Phase 1 确定平台启动是单个 Go 二进制。将 River 作为内部作业调度器接入 worker 引入了新的生命周期（river.Client.Start，river.Worker 注册）和并行持久化路径。回收器只需要一个 goroutine + 一条 SQL SELECT — 绝对更简单。
2. **D-14 的字面声明被回收器保留。** 回收器覆盖相同的故障模式（worker 执行中间崩溃 → 孤立的运行），使用 plan 02-02 中添加的 heartbeat 列。River 会在作业调度层应用重试+退避；回收器在运行状态层应用"重置为 queued 并让健康的 worker 重新认领"。对于 Phase 2 的单租户、轮询循环 worker 模型，功能等效。
3. **推迟，非放弃。** 当第三方连接器落地时（Phase 3+），进程外作业调度成为硬性要求（River worker 可以运行在与连接器子进程不同的机器上）。届时我们将重新审视并将 River 接入作为步骤调度器。回收器继续与 River 一起工作；两者并不互斥。

回收器：
- 每 60 秒运行一次（可通过 `ReaperInterval` 配置）。
- 从 `runs` 中选择 `WHERE state IN ('starting','running') AND last_heartbeat < NOW() - interval '5 minutes'` 的行。
- 对于每一行，调用 `run.TransitionForReset(currentState, 'queued')`（plan 02-02 — 唯一合法的反向路径）并用 state='queued'、claimed_by=NULL、claimed_at=NULL、last_heartbeat=NULL 更新行。
- 发出 `run.canceled` 事件，reason="reaper: worker heartbeat lost"，以便审计日志记录恢复。
- 5 分钟过期阈值是执行者的 30 秒 heartbeat tick（plan 02-03）的 10 倍。瞬时延迟不会导致虚假的重新排队。

**Testcontainers（D-10）：** PG 集成测试使用 `github.com/testcontainers/testcontainers-go` 和官方 postgres 模块。集成测试启动临时 Postgres、应用迁移、注册两个资产（users_raw、users_clean — clean 依赖于 raw）、运行 materialize 命令，并断言：
1. DAG 执行 users_raw 在 users_clean 之前（事件时间戳）。
2. PG 连接器的 Write 被调用，users_clean 的预期行被写入。
3. runs 行最终 state='succeeded'，run.succeeded 事件存在。
4.（回收器特定）直接插入数据库的具有 last_heartbeat 6 分钟前的"废弃"运行通过单次回收器调用被恢复到 'queued'。

**已冻结的接口：**
- 所有来自 plans 02-01、02-02、02-03 的接口（asset、run、dag、runtime、concurrency、config）
- `connector.Connector`（Phase 1）
- `internal/storage`（Phase 1）
- `internal/event.Writer`（Phase 1）
- `internal/auth` 用于 JWT 验证（Phase 1）— 重用
- `run.TransitionForReset`（plan 02-02）— 回收器反向路径的唯一合法方式
- `runs.last_heartbeat` 列（plan 02-02）+ executor heartbeat tick（plan 02-03）

@.planning/phases/02-execution-engine/02-CONTEXT.md
@.planning/phases/02-execution-engine/02-01-PLAN.md
@.planning/phases/02-execution-engine/02-02-PLAN.md
@.planning/phases/02-execution-engine/02-03-PLAN.md
@internal/connector/connector.go
@internal/connector/example_inproc/postgres_stub.go
@cmd/platform/main.go
</context>

<tasks>

<task id="4.1" type="auto" tdd="true">
  <name>Task 1: PostgreSQL connector (real Read/Write/Schema/Ping via pgxpool) + Factory</name>
  <files>internal/connector/firstparty/postgres/postgres.go, internal/connector/firstparty/postgres/postgres_test.go, internal/connector/firstparty/postgres/factory.go, go.mod</files>
  <read_first>
    - internal/connector/connector.go (frozen interface — Read returns ReadResponse{Rows []Row}, Row{Fields map[string]any})
    - internal/connector/example_inproc/postgres_stub.go (stub structure to extend with real pgxpool)
    - internal/connector/version.go (APIVersion = "v1.0.0")
    - .planning/phases/02-execution-engine/02-CONTEXT.md (D-08 connector singleton + pool, D-10 testcontainers)
    - .planning/research/PITFALLS.md §10 (ctx propagation to connector calls — pgxpool.Query must take ctx)
  </read_first>
  <behavior>
    - Test 1 (postgres_test.go, requires DATABASE_URL or skips): Ping returns PingResponse with ConnectorName="postgres" and Capabilities.SupportsSchemaCapture=true.
    - Test 2: Schema(asset="public.users") returns columns matching the actual table (queries information_schema.columns).
    - Test 3: Write({rows: [{id: 1, email: "a@b.com"}]}) inserts the row; subsequent Read returns it.
    - Test 4: Read with Limit=10 returns at most 10 rows.
    - Test 5: Connector implements connector.Connector (compile-time `var _ connector.Connector = (*Postgres)(nil)`).
    - Test 6: Factory({"dsn": "postgres://..."}) returns a *Postgres; Factory({}) returns ErrMissingDSN.
    - Test 7: Close() drains the pool; subsequent Read returns ErrClosed.
    - Test 8 (ctx propagation, PITFALLS §10): Cancel ctx mid-query; Read returns context.Canceled wrapped, NOT a generic timeout.
  </behavior>
  <action>
    Step A — Create `internal/connector/firstparty/postgres/postgres.go`:
    ```go
    // Package postgres implements the connector.Connector interface for PostgreSQL,
    // reading and writing rows via pgxpool. It is the reference first-party connector
    // (D-12) — other connectors in plan 02-05 follow the same shape.
    package postgres

    import (
        "context"
        "errors"
        "fmt"
        "strings"
        "sync"
        "time"

        "github.com/jackc/pgx/v5/pgxpool"
        "github.com/kanpon/data-governance/internal/connector"
    )

    var (
        ErrMissingDSN = errors.New("postgres: dsn parameter required")
        ErrClosed     = errors.New("postgres: connector closed")
    )

    // Compile-time assertion: Postgres satisfies connector.Connector.
    var _ connector.Connector = (*Postgres)(nil)

    // Postgres is the PostgreSQL connector. Lifecycle (D-08): one instance per
    // configured connector name, pool kept for the process lifetime.
    type Postgres struct {
        pool   *pgxpool.Pool
        mu     sync.RWMutex
        closed bool
    }

    // New constructs a Postgres connector. dsn must be a valid PG connection string.
    // The pool's MaxConns can be overridden via dsn parameters or via WithMaxConns.
    func New(ctx context.Context, dsn string) (*Postgres, error) {
        cfg, err := pgxpool.ParseConfig(dsn)
        if err != nil {
            return nil, fmt.Errorf("postgres: parse dsn: %w", err)
        }
        pool, err := pgxpool.NewWithConfig(ctx, cfg)
        if err != nil {
            return nil, fmt.Errorf("postgres: open pool: %w", err)
        }
        return &Postgres{pool: pool}, nil
    }

    func (p *Postgres) APIVersion() string { return connector.APIVersion }

    func (p *Postgres) Ping(ctx context.Context, req connector.PingRequest) (connector.PingResponse, error) {
        if err := p.checkClosed(); err != nil {
            return connector.PingResponse{}, err
        }
        if err := p.pool.Ping(ctx); err != nil {
            return connector.PingResponse{}, fmt.Errorf("postgres: ping: %w", err)
        }
        return connector.PingResponse{
            APIVersion:       connector.APIVersion,
            ConnectorName:    "postgres",
            ConnectorVersion: "1.0.0",
            Capabilities:     connector.Capabilities{SupportsSchemaCapture: true},
        }, nil
    }

    func (p *Postgres) Schema(ctx context.Context, req connector.SchemaRequest) (connector.SchemaResponse, error) {
        if err := p.checkClosed(); err != nil {
            return connector.SchemaResponse{}, err
        }
        schemaName, tableName, err := splitIdentifier(req.Asset.Identifier)
        if err != nil {
            return connector.SchemaResponse{}, err
        }
        const q = `
            SELECT column_name, data_type, is_nullable
              FROM information_schema.columns
             WHERE table_schema = $1 AND table_name = $2
             ORDER BY ordinal_position
        `
        rows, err := p.pool.Query(ctx, q, schemaName, tableName)
        if err != nil {
            return connector.SchemaResponse{}, fmt.Errorf("postgres: schema query: %w", err)
        }
        defer rows.Close()

        var cols []connector.Column
        for rows.Next() {
            var name, rawType, isNullable string
            if err := rows.Scan(&name, &rawType, &isNullable); err != nil {
                return connector.SchemaResponse{}, fmt.Errorf("postgres: schema scan: %w", err)
            }
            cols = append(cols, connector.Column{Name: name, RawType: rawType, Nullable: isNullable == "YES"})
        }
        return connector.SchemaResponse{Columns: cols, CapturedAt: time.Now().UTC()}, nil
    }

    func (p *Postgres) Read(ctx context.Context, req connector.ReadRequest) (connector.ReadResponse, error) {
        if err := p.checkClosed(); err != nil {
            return connector.ReadResponse{}, err
        }
        ident, err := quoteIdentifier(req.Asset.Identifier)
        if err != nil {
            return connector.ReadResponse{}, err
        }
        query := "SELECT * FROM " + ident
        if req.Limit > 0 {
            query += fmt.Sprintf(" LIMIT %d", req.Limit)
        }
        rows, err := p.pool.Query(ctx, query)
        if err != nil {
            return connector.ReadResponse{}, fmt.Errorf("postgres: read: %w", err)
        }
        defer rows.Close()

        fieldDescs := rows.FieldDescriptions()
        out := []connector.Row{}
        for rows.Next() {
            values, err := rows.Values()
            if err != nil {
                return connector.ReadResponse{}, fmt.Errorf("postgres: read scan: %w", err)
            }
            r := connector.Row{Fields: make(map[string]any, len(fieldDescs))}
            for i, fd := range fieldDescs {
                r.Fields[string(fd.Name)] = values[i]
            }
            out = append(out, r)
        }
        if err := rows.Err(); err != nil {
            return connector.ReadResponse{}, fmt.Errorf("postgres: read iter: %w", err)
        }
        return connector.ReadResponse{Rows: out}, nil
    }

    func (p *Postgres) Write(ctx context.Context, req connector.WriteRequest) (connector.WriteResponse, error) {
        if err := p.checkClosed(); err != nil {
            return connector.WriteResponse{}, err
        }
        if len(req.Rows) == 0 {
            return connector.WriteResponse{RowsWritten: 0}, nil
        }
        ident, err := quoteIdentifier(req.Asset.Identifier)
        if err != nil {
            return connector.WriteResponse{}, err
        }
        // Discover column order from the first row (rows must share the same fields in v1).
        cols := make([]string, 0, len(req.Rows[0].Fields))
        for k := range req.Rows[0].Fields {
            cols = append(cols, k)
        }
        // Build INSERT ... VALUES ($1, $2, ...), ($N, $N+1, ...).
        var sb strings.Builder
        sb.WriteString("INSERT INTO ")
        sb.WriteString(ident)
        sb.WriteString(" (")
        for i, c := range cols {
            if i > 0 {
                sb.WriteString(",")
            }
            qc, qerr := quoteIdentifier(c)
            if qerr != nil {
                return connector.WriteResponse{}, qerr
            }
            sb.WriteString(qc)
        }
        sb.WriteString(") VALUES ")
        args := make([]any, 0, len(req.Rows)*len(cols))
        param := 1
        for ri, r := range req.Rows {
            if ri > 0 {
                sb.WriteString(",")
            }
            sb.WriteString("(")
            for ci, c := range cols {
                if ci > 0 {
                    sb.WriteString(",")
                }
                fmt.Fprintf(&sb, "$%d", param)
                args = append(args, r.Fields[c])
                param++
            }
            sb.WriteString(")")
        }
        ct, err := p.pool.Exec(ctx, sb.String(), args...)
        if err != nil {
            return connector.WriteResponse{}, fmt.Errorf("postgres: write: %w", err)
        }
        return connector.WriteResponse{RowsWritten: ct.RowsAffected()}, nil
    }

    // Close drains the pool. Subsequent operations return ErrClosed.
    func (p *Postgres) Close() error {
        p.mu.Lock()
        defer p.mu.Unlock()
        if p.closed {
            return nil
        }
        p.pool.Close()
        p.closed = true
        return nil
    }

    func (p *Postgres) checkClosed() error {
        p.mu.RLock()
        defer p.mu.RUnlock()
        if p.closed {
            return ErrClosed
        }
        return nil
    }

    // splitIdentifier splits "schema.table" into (schema, table). If no dot is present,
    // returns ("public", id).
    func splitIdentifier(id string) (string, string, error) {
        if id == "" {
            return "", "", errors.New("postgres: empty identifier")
        }
        parts := strings.SplitN(id, ".", 2)
        if len(parts) == 1 {
            return "public", parts[0], nil
        }
        return parts[0], parts[1], nil
    }

    // quoteIdentifier returns "schema"."table" with safe identifier quoting.
    // Rejects identifiers with embedded double quotes (defense against SQL injection
    // via asset names; legitimate names should never contain quotes).
    func quoteIdentifier(id string) (string, error) {
        if strings.ContainsRune(id, '"') {
            return "", fmt.Errorf("postgres: identifier contains illegal character: %q", id)
        }
        s, t, err := splitIdentifier(id)
        if err != nil {
            // Single-token identifier (e.g. column name) — quote in place.
            if strings.Contains(id, ".") {
                return "", err
            }
            return `"` + id + `"`, nil
        }
        return `"` + s + `"."` + t + `"`, nil
    }
    ```

    Step B — Create `internal/connector/firstparty/postgres/factory.go`:
    ```go
    package postgres

    import (
        "context"
        "fmt"
        "time"

        "github.com/kanpon/data-governance/internal/connector"
    )

    // Factory is the config.Factory for type="postgres" connectors. Plan 02-04
    // factories.go calls config.FactoryRegistry.RegisterFactory("postgres", postgres.Factory).
    func Factory(params map[string]interface{}) (connector.Connector, error) {
        dsn, _ := params["dsn"].(string)
        if dsn == "" {
            return nil, ErrMissingDSN
        }
        // Bound construction with a timeout — fast-fail at startup if the DB is unreachable.
        ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
        defer cancel()
        c, err := New(ctx, dsn)
        if err != nil {
            return nil, fmt.Errorf("postgres factory: %w", err)
        }
        return c, nil
    }
    ```

    Step C — Add testcontainers-go dependency:
    ```
    go get github.com/testcontainers/testcontainers-go@latest
    go get github.com/testcontainers/testcontainers-go/modules/postgres@latest
    go mod tidy
    ```

    Step D — Create `internal/connector/firstparty/postgres/postgres_test.go`:
    Use a testcontainers helper that spins up `postgres:16` once per test run. Tests share the container via `TestMain`. Each test creates its own table via raw SQL and tears down on completion.

    Pseudo-skeleton:
    ```go
    package postgres

    import (
        "context"
        "errors"
        "os"
        "testing"
        "time"

        "github.com/stretchr/testify/require"
        "github.com/testcontainers/testcontainers-go/modules/postgres"
        "github.com/testcontainers/testcontainers-go"

        "github.com/kanpon/data-governance/internal/connector"
    )

    var testDSN string

    func TestMain(m *testing.M) {
        if os.Getenv("SKIP_DOCKER_TESTS") == "1" {
            os.Exit(m.Run())
        }
        ctx := context.Background()
        c, err := postgres.Run(ctx, "postgres:16-alpine",
            postgres.WithDatabase("test"),
            postgres.WithUsername("test"),
            postgres.WithPassword("test"),
            testcontainers.WithWaitStrategy(/* default for postgres module */),
        )
        if err != nil {
            os.Exit(0) // skip suite gracefully when docker is unavailable
        }
        defer testcontainers.TerminateContainer(c)
        dsn, err := c.ConnectionString(ctx, "sslmode=disable")
        if err != nil {
            os.Exit(1)
        }
        testDSN = dsn
        os.Exit(m.Run())
    }

    // ... per-test functions exercising Read/Write/Schema/Ping/Close/Factory/ctx-cancel ...
    ```

    Concrete tests:
    - `TestPing` — calls New + Ping; asserts ConnectorName and APIVersion.
    - `TestSchemaRoundTrip` — CREATE TABLE; Schema returns expected columns.
    - `TestWriteThenRead` — CREATE TABLE; Write 3 rows; Read returns 3 rows; Read with Limit=2 returns 2.
    - `TestReadCtxCancel` — CREATE TABLE with millions of rows OR use `pg_sleep(5)` query via raw SQL surface; cancel ctx after 100ms; assert Read returns context.Canceled (verified by `errors.Is(err, context.Canceled)`).
    - `TestFactory_MissingDSN` — Factory({}) returns ErrMissingDSN.
    - `TestFactory_Builds` — Factory({"dsn": testDSN}) returns Postgres connector that pings successfully.
    - `TestClose` — Close then Read returns ErrClosed.
    - `TestQuoteIdentifier_RejectQuotes` — quoteIdentifier(`name"injection`) returns error.
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go test ./internal/connector/firstparty/postgres/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/connector/firstparty/postgres/postgres.go` exists; `grep -q "var _ connector.Connector = (\*Postgres)(nil)" internal/connector/firstparty/postgres/postgres.go` succeeds
    - Postgres struct has fields including a pgxpool.Pool: `grep -q "pool   \*pgxpool.Pool" internal/connector/firstparty/postgres/postgres.go`
    - `func (p \*Postgres) Read(`, `func (p \*Postgres) Write(`, `func (p \*Postgres) Schema(`, `func (p \*Postgres) Ping(`, `func (p \*Postgres) Close()`, `func (p \*Postgres) APIVersion()` all exist
    - Read implementation calls `p.pool.Query(ctx,` (verify: `grep -q "p.pool.Query(ctx," internal/connector/firstparty/postgres/postgres.go`)
    - Write implementation builds parameterized INSERT (no string concat of values) — verify by `grep -q "fmt.Fprintf(&sb, \"\$%d\", param)" internal/connector/firstparty/postgres/postgres.go`
    - quoteIdentifier rejects identifiers containing `"` (verify by reading the file)
    - File `internal/connector/firstparty/postgres/factory.go` defines `func Factory(params map[string]interface{}) (connector.Connector, error)` returning ErrMissingDSN when dsn is missing
    - `go test ./internal/connector/firstparty/postgres/... -count=1` exits 0 when docker is available; skips gracefully when not
    - Test `TestReadCtxCancel` exists and asserts `errors.Is(err, context.Canceled)` after explicit ctx cancellation
    - go.mod contains `github.com/testcontainers/testcontainers-go` and `github.com/testcontainers/testcontainers-go/modules/postgres`
    - `go vet ./internal/connector/firstparty/postgres/...` exits 0
  </acceptance_criteria>
  <done>PostgreSQL connector implements all four interface methods with real pgxpool, ctx propagation, identifier safety; Factory wired for config; tests pass against testcontainers Postgres.</done>
</task>

<task id="4.2" type="auto" tdd="true">
  <name>Task 2: Stale-run reaper goroutine + reaper test (recovers crashed-worker runs within 6 minutes)</name>
  <files>internal/run/reaper.go, internal/run/reaper_test.go</files>
  <read_first>
    - .planning/phases/02-execution-engine/02-CONTEXT.md (D-14: infrastructure-fault recovery — Phase 2 implements via reaper, NOT River.max_attempts)
    - internal/run/lifecycle.go (TransitionForReset — the reaper's only legal backward edge per plan 02-02)
    - internal/run/claim.go (ClaimNext + Heartbeat — same package conventions)
    - internal/storage/storage.go (Storage.DB() for raw SQL)
    - internal/event/types.go (EventTypeRunCanceled — reaper emits this with reason="reaper")
    - internal/event/writer.go (event.Writer.Append signature)
    - migrations/20260507120000_phase2_run_tables.sql (the (state,last_heartbeat) index that the reaper SELECT relies on)
  </read_first>
  <behavior>
    - Test 1 (reaper_test.go, requires DATABASE_URL else t.Skip): Insert two runs — one with state='running' and last_heartbeat=NOW()-6m (stale), one with state='running' and last_heartbeat=NOW() (fresh). Run StaleRunReaper.SweepOnce(ctx). Assert: stale run is now state='queued' with claimed_by=NULL and last_heartbeat=NULL; fresh run is unchanged.
    - Test 2 (reaper_test.go): Insert a run with state='starting' and last_heartbeat=NOW()-6m. SweepOnce reclaims it (starting→queued), proving both starting and running states are eligible.
    - Test 3 (reaper_test.go): Insert a run with state='succeeded' and last_heartbeat=NOW()-1h. SweepOnce does NOT touch it (terminal states are ignored — run.TransitionForReset would reject the edge anyway).
    - Test 4 (reaper_test.go): For each reclaimed run, SweepOnce emits an event_log entry with type=run.canceled and payload.Reason="reaper: worker heartbeat lost"; verify by querying event_log after SweepOnce.
    - Test 5 (reaper_test.go): SweepOnce returns the count of reclaimed rows (int64); concurrent invocations (two goroutines calling SweepOnce simultaneously on the same stale row) result in exactly ONE reclamation across both calls (defense via UPDATE...WHERE state IN ('starting','running') AND last_heartbeat < $cutoff making the UPDATE idempotent across the race).
    - Test 6 (reaper_test.go): StaleRunReaper.Run(ctx) goroutine ticks SweepOnce every ReaperInterval; cancel ctx → goroutine exits within 100ms (test uses sync.WaitGroup + ReaperInterval=100ms override).
  </behavior>
  <action>
    Step A — Create `internal/run/reaper.go`:
    ```go
    package run

    import (
        "context"
        "database/sql"
        "errors"
        "fmt"
        "log/slog"
        "time"

        "github.com/google/uuid"
        "github.com/kanpon/data-governance/internal/event"
        "github.com/kanpon/data-governance/internal/storage"
    )

    // Default reaper tuning. These values pair with plan 02-03's HeartbeatInterval (30s):
    //   - StaleAfter (5m) is 10x HeartbeatInterval; transient ticker delays do not trigger.
    //   - Interval (60s) is the scan cadence; combined with StaleAfter, recovery upper bound
    //     is StaleAfter + Interval = ~6 minutes after the worker's LAST successful heartbeat.
    const (
        DefaultReaperStaleAfter = 5 * time.Minute
        DefaultReaperInterval   = 60 * time.Second
    )

    // StaleRunReaper recovers runs whose worker died mid-execution (D-14 Option B).
    // It scans every Interval for runs in {starting, running} whose last_heartbeat is older
    // than StaleAfter, and resets them to 'queued' via TransitionForReset.
    //
    // Background: D-14 originally described "River handles infrastructure faults". Phase 2
    // implements that intent via this reaper instead, keeping the worker as a single Go
    // process. River.max_attempts wiring is deferred to a later phase; see plan 02-04
    // <context> for the rationale.
    type StaleRunReaper struct {
        Store      storage.Storage
        Events     event.Writer
        StaleAfter time.Duration
        Interval   time.Duration
    }

    // Run executes the reaper loop until ctx is canceled. Intended to run in a worker
    // goroutine spawned at process startup (cmd/platform/worker.go runWorker).
    func (r *StaleRunReaper) Run(ctx context.Context) {
        if r.StaleAfter == 0 {
            r.StaleAfter = DefaultReaperStaleAfter
        }
        if r.Interval == 0 {
            r.Interval = DefaultReaperInterval
        }
        slog.Info("reaper.started", "stale_after", r.StaleAfter, "interval", r.Interval)
        t := time.NewTicker(r.Interval)
        defer t.Stop()
        for {
            select {
            case <-ctx.Done():
                slog.Info("reaper.stopped")
                return
            case <-t.C:
                n, err := r.SweepOnce(ctx)
                if err != nil {
                    slog.Error("reaper.sweep_failed", "error", err)
                    continue
                }
                if n > 0 {
                    slog.Info("reaper.reclaimed", "count", n)
                }
            }
        }
    }

    // staleRunRow is the projection used by the reaper SELECT.
    type staleRunRow struct {
        ID        uuid.UUID
        State     State
        AssetName string
    }

    // SweepOnce scans for stale runs and resets each to 'queued'. Returns the number
    // of rows reclaimed. Safe to invoke concurrently — the UPDATE WHERE clause filters
    // on (state, last_heartbeat) so two concurrent sweeps of the same row produce
    // exactly one successful UPDATE.
    func (r *StaleRunReaper) SweepOnce(ctx context.Context) (int64, error) {
        if r.StaleAfter == 0 {
            r.StaleAfter = DefaultReaperStaleAfter
        }
        cutoff := time.Now().UTC().Add(-r.StaleAfter)

        // Step 1: SELECT candidates (uses the (state, last_heartbeat) index from plan 02-02).
        const selectSQL = `
            SELECT id, state, asset_name
              FROM runs
             WHERE state IN ('starting','running')
               AND last_heartbeat < $1
        `
        rows, err := r.Store.DB().QueryContext(ctx, selectSQL, cutoff)
        if err != nil {
            return 0, fmt.Errorf("reaper: select stale: %w", err)
        }
        var candidates []staleRunRow
        for rows.Next() {
            var row staleRunRow
            var stateStr string
            if err := rows.Scan(&row.ID, &stateStr, &row.AssetName); err != nil {
                rows.Close()
                return 0, fmt.Errorf("reaper: scan stale: %w", err)
            }
            row.State = State(stateStr)
            candidates = append(candidates, row)
        }
        rows.Close()
        if err := rows.Err(); err != nil {
            return 0, fmt.Errorf("reaper: iterate stale: %w", err)
        }

        // Step 2: For each candidate, validate the FSM edge then UPDATE atomically.
        var reclaimed int64
        for _, c := range candidates {
            // Validate: TransitionForReset is the ONLY legal backward edge (plan 02-02).
            if err := TransitionForReset(c.State, StateQueued); err != nil {
                slog.Warn("reaper.skip_illegal_edge", "run_id", c.ID, "from", c.State)
                continue
            }
            // Atomic UPDATE: filter on (state, last_heartbeat) so a re-emerged worker that
            // just heartbeat does not lose the race.
            const updateSQL = `
                UPDATE runs
                   SET state = 'queued',
                       claimed_by = NULL,
                       claimed_at = NULL,
                       last_heartbeat = NULL,
                       error_message = COALESCE(error_message, '') || ' [reaper: worker heartbeat lost]'
                 WHERE id = $1
                   AND state IN ('starting','running')
                   AND last_heartbeat < $2
            `
            res, err := r.Store.DB().ExecContext(ctx, updateSQL, c.ID, cutoff)
            if err != nil {
                slog.Error("reaper.update_failed", "run_id", c.ID, "error", err)
                continue
            }
            n, _ := res.RowsAffected()
            if n != 1 {
                // Row resurrected (heartbeat just landed) or another reaper instance won.
                continue
            }
            reclaimed++

            // Emit run.canceled event with reason="reaper: worker heartbeat lost".
            if r.Events != nil {
                if err := r.Events.Append(ctx, event.Event{
                    Type:         event.EventTypeRunCanceled,
                    ResourceType: "run",
                    ResourceID:   c.ID.String(),
                    Payload: event.RunCanceledPayload{
                        AssetName: c.AssetName,
                        Reason:    "reaper: worker heartbeat lost",
                    },
                }); err != nil {
                    slog.Warn("reaper.event_append_failed", "run_id", c.ID, "error", err)
                }
            }
        }
        return reclaimed, nil
    }

    // unused imports placeholder
    var _ = errors.New
    var _ = sql.ErrNoRows
    ```

    Step B — Create `internal/run/reaper_test.go` covering tests 1–6 above. Use the same `setupTestDB(t)` helper pattern as claim_test.go. For Test 6, override `Interval = 100 * time.Millisecond` and `StaleAfter = 50 * time.Millisecond`; spawn the goroutine; insert a stale run; wait up to 500ms; assert the row is reclaimed; cancel ctx; assert the goroutine exits.
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go test ./internal/run/... -count=1 -run 'TestReaper|TestSweep' -v && go vet ./internal/run/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/run/reaper.go` exists and `grep -q "type StaleRunReaper struct" internal/run/reaper.go` succeeds
    - `grep -q "func (r \*StaleRunReaper) Run(ctx context.Context)" internal/run/reaper.go` succeeds
    - `grep -q "func (r \*StaleRunReaper) SweepOnce(ctx context.Context) (int64, error)" internal/run/reaper.go` succeeds
    - `grep -q "DefaultReaperStaleAfter = 5 \* time.Minute" internal/run/reaper.go` succeeds
    - `grep -q "DefaultReaperInterval   = 60 \* time.Second" internal/run/reaper.go` succeeds
    - SELECT in SweepOnce uses literal `state IN ('starting','running')` and `last_heartbeat < $1`: `grep -q "state IN ('starting','running')" internal/run/reaper.go && grep -q "last_heartbeat < \$1" internal/run/reaper.go`
    - UPDATE in SweepOnce sets `last_heartbeat = NULL, claimed_by = NULL, claimed_at = NULL` and filters `WHERE id = $1 AND state IN ('starting','running') AND last_heartbeat < $2`: verify all three SET fragments and the full WHERE present in the file
    - SweepOnce calls `TransitionForReset(c.State, StateQueued)` to validate the FSM edge: `grep -q "TransitionForReset(c.State, StateQueued)" internal/run/reaper.go`
    - SweepOnce emits `event.EventTypeRunCanceled` with `Reason: "reaper: worker heartbeat lost"`: `grep -q "EventTypeRunCanceled" internal/run/reaper.go && grep -q "reaper: worker heartbeat lost" internal/run/reaper.go`
    - File `internal/run/reaper_test.go` defines tests covering: stale-row reclamation (Test 1), starting state eligible (Test 2), terminal state ignored (Test 3), event emitted with reason (Test 4), idempotent under concurrent sweep (Test 5), goroutine ticks and exits cleanly (Test 6)
    - When DATABASE_URL is set, `go test ./internal/run/... -run 'TestReaper|TestSweep' -count=1` exits 0
    - `go vet ./internal/run/...` exits 0
  </acceptance_criteria>
  <done>StaleRunReaper recovers crashed-worker runs within ~6 minutes; uses TransitionForReset (plan 02-02) for FSM validation; emits run.canceled audit event; concurrent-safe via filtered UPDATE; tested against PostgreSQL.</done>
</task>

<task id="4.3" type="auto" tdd="true">
  <name>Task 3: worker + materialize CLI subcommands + factories wiring + e2e integration test (worker spawns reaper)</name>
  <files>cmd/platform/main.go, cmd/platform/worker.go, cmd/platform/materialize.go, cmd/platform/factories.go, testdata/integration/example_assets.go, testdata/integration/config.yaml, test/integration/e2e_postgres_test.go</files>
  <read_first>
    - cmd/platform/main.go (current — switch already handles start/migrate/healthcheck; must add worker/materialize cases)
    - internal/runtime/executor.go (Executor.Run signature from plan 02-03; HeartbeatInterval default = 30s)
    - internal/run/claim.go (ClaimNext signature from plan 02-02)
    - internal/run/reaper.go (StaleRunReaper — task 4.2 just created)
    - internal/asset/registry.go (Default() singleton)
    - internal/connector/config/config.go (LoadFile signature)
    - internal/connector/config/resolver.go (FactoryRegistry.BuildAll)
    - internal/connector/firstparty/postgres/factory.go (task 4.1)
    - internal/auth (Phase 1 JWT validation — see internal/auth/ files for the JWT path)
  </read_first>
  <behavior>
    - Test 1 (e2e_postgres_test.go): Bring up testcontainers Postgres, apply migrations, build & run platform binary as `./platform worker` in a goroutine + `./platform materialize users_clean` in the test goroutine. Assert exit code 0 and runs row state='succeeded' AND target table contains expected rows.
    - Test 2 (e2e_postgres_test.go): Same setup, run materialize for an asset whose Materialize returns errors.New(...) with RetryPolicy{Max: 1}. Assert exit code 1 (failed), runs row state='failed', AND event_log contains run.step.failed + run.failed events with timestamps and error message visible (acceptance criterion 2).
    - Test 3 (e2e_postgres_test.go): Topological order — assets users_raw → users_clean. Assert event_log shows run.step.started for users_raw with smaller occurred_at than users_clean (closes acceptance criterion 1).
    - Test 4 (e2e_postgres_test.go): `--detach` mode prints UUID, exits 0 immediately, run is later picked up by worker.
    - Test 5 (e2e_postgres_test.go): worker process gracefully shuts down on SIGTERM; current run completes before exit (or executor's ctx gets canceled cleanly).
    - Test 6 (e2e_postgres_test.go — reaper integration): Insert a run with state='running' and last_heartbeat=NOW()-6m DIRECTLY into the runs table. Start the worker (which spawns the reaper). Within 90 seconds (ReaperInterval=10s override + slack), assert the runs row was reclaimed to state='queued' AND a run.canceled event with Reason="reaper: worker heartbeat lost" exists in event_log. This proves the reaper integrates with the worker startup path.
  </behavior>
  <action>
    Step A — Create `cmd/platform/factories.go` that wires plan 02-03's FactoryRegistry with first-party factories:
    ```go
    package main

    import (
        "github.com/kanpon/data-governance/internal/connector/config"
        "github.com/kanpon/data-governance/internal/connector/firstparty/postgres"
        // additional first-party packages added in plan 02-05
    )

    // newFactoryRegistry returns a FactoryRegistry pre-loaded with all first-party
    // connector factories (D-06). Plan 02-05 extends this list with the other six.
    func newFactoryRegistry() *config.FactoryRegistry {
        r := config.NewFactoryRegistry()
        r.RegisterFactory("postgres", postgres.Factory)
        // Plan 02-05 adds: mysql, bigquery, snowflake, s3, gcs, hdfs.
        return r
    }
    ```

    Step B — Create `cmd/platform/worker.go`. Note the addition of the reaper goroutine spawn:
    ```go
    package main

    import (
        "context"
        "errors"
        "fmt"
        "log/slog"
        "os"
        "os/signal"
        "sync"
        "syscall"
        "time"

        "github.com/kanpon/data-governance/internal/asset"
        "github.com/kanpon/data-governance/internal/concurrency"
        "github.com/kanpon/data-governance/internal/connector"
        "github.com/kanpon/data-governance/internal/connector/config"
        "github.com/kanpon/data-governance/internal/event"
        "github.com/kanpon/data-governance/internal/run"
        "github.com/kanpon/data-governance/internal/runtime"
        "github.com/kanpon/data-governance/internal/storage"
    )

    // runWorker is the body of the `worker` subcommand. It claims queued runs and
    // executes them under runtime.Executor. It also spawns a stale-run reaper goroutine
    // that recovers crashed-worker runs (D-14 Option B). Loops until ctx is canceled
    // (SIGTERM/INT).
    func runWorker() error {
        ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
        defer stop()

        deps, err := bootstrap(ctx)
        if err != nil {
            return err
        }
        defer deps.cleanup()

        // Clean up tokens left by previous crashed worker.
        if n, err := deps.pool.ReleaseStale(ctx, 24*time.Hour); err != nil {
            slog.Warn("worker.release_stale_failed", "error", err)
        } else if n > 0 {
            slog.Info("worker.released_stale_tokens", "count", n)
        }

        // Spawn the stale-run reaper goroutine. Recovers runs whose worker died
        // mid-execution. See plan 02-04 <context> for D-14 Option B rationale.
        reaper := &run.StaleRunReaper{
            Store:      deps.store,
            Events:     deps.events,
            StaleAfter: run.DefaultReaperStaleAfter, // 5 minutes
            Interval:   run.DefaultReaperInterval,   // 60 seconds
        }
        var reaperWG sync.WaitGroup
        reaperWG.Add(1)
        go func() {
            defer reaperWG.Done()
            reaper.Run(ctx)
        }()
        defer reaperWG.Wait()

        slog.Info("worker.started", "worker_id", deps.workerID)

        for {
            if ctx.Err() != nil {
                slog.Info("worker.shutdown")
                return nil
            }

            claimed, err := run.ClaimNext(ctx, deps.store, deps.workerID)
            if errors.Is(err, run.ErrNoQueuedRun) {
                // Idle: sleep briefly, then poll.
                select {
                case <-time.After(500 * time.Millisecond):
                case <-ctx.Done():
                    return nil
                }
                continue
            }
            if err != nil {
                slog.Error("worker.claim_failed", "error", err)
                continue
            }

            slog.Info("worker.run_claimed", "run_id", claimed.ID, "asset", claimed.AssetName)
            execErr := deps.executor.Run(ctx, claimed.ID, claimed.AssetName)
            if execErr != nil {
                slog.Error("worker.run_failed", "run_id", claimed.ID, "error", execErr)
            } else {
                slog.Info("worker.run_succeeded", "run_id", claimed.ID)
            }
        }
    }

    type workerDeps struct {
        store      storage.Storage
        events     event.Writer
        registry   *asset.DefinitionRegistry
        connectors *connector.Registry
        pool       *concurrency.Pool
        executor   *runtime.Executor
        workerID   string
        cleanup    func()
    }

    // bootstrap loads config, opens storage, builds the connector registry from config,
    // and constructs an Executor. Shared by worker and materialize subcommands.
    func bootstrap(ctx context.Context) (*workerDeps, error) {
        cfgPath := os.Getenv("PLATFORM_CONFIG")
        if cfgPath == "" {
            cfgPath = "./config.yaml"
        }
        cfg, err := config.LoadFile(cfgPath)
        if err != nil {
            return nil, fmt.Errorf("worker: load config %q: %w", cfgPath, err)
        }

        dsn := os.Getenv("DATABASE_URL")
        if dsn == "" {
            return nil, fmt.Errorf("worker: DATABASE_URL is required")
        }
        store, err := storage.NewPostgres(ctx, dsn)
        if err != nil {
            return nil, fmt.Errorf("worker: open storage: %w", err)
        }

        connReg := connector.NewRegistry()
        if err := newFactoryRegistry().BuildAll(cfg, connReg); err != nil {
            store.Close()
            return nil, fmt.Errorf("worker: build connectors: %w", err)
        }

        capacities := []concurrency.Capacity{
            {Tag: "global", Limit: cfg.Concurrency.DefaultRunTokens},
        }
        for tag, limit := range cfg.Concurrency.Resources {
            capacities = append(capacities, concurrency.Capacity{Tag: tag, Limit: limit})
        }
        pool := concurrency.NewPool(store, capacities)

        defaultPolicy := asset.RetryPolicy{
            Max:          cfg.Retry.Default.Max,
            InitialDelay: cfg.Retry.Default.InitialDelay,
            MaxDelay:     cfg.Retry.Default.MaxDelay,
            JitterPct:    cfg.Retry.Default.JitterPct,
        }

        writer := event.NewWriter(store)

        workerID := fmt.Sprintf("worker-%s-%d", os.Getenv("HOSTNAME"), os.Getpid())

        exec := runtime.NewExecutor(runtime.Deps{
            Store:         store,
            Events:        writer,
            Registry:      asset.Default(),
            ConnectorReg:  connReg,
            Pool:          pool,
            DefaultPolicy: defaultPolicy,
            WorkerID:      workerID,
            // HeartbeatInterval defaults to 30s in NewExecutor — paired with reaper StaleAfter=5m.
        })

        return &workerDeps{
            store: store, events: writer, registry: asset.Default(),
            connectors: connReg, pool: pool, executor: exec, workerID: workerID,
            cleanup: func() { store.Close() },
        }, nil
    }
    ```

    Step C — Create `cmd/platform/materialize.go`:
    ```go
    package main

    import (
        "context"
        "errors"
        "flag"
        "fmt"
        "log/slog"
        "os"
        "time"

        "github.com/google/uuid"
        "github.com/kanpon/data-governance/internal/auth"
        "github.com/kanpon/data-governance/internal/event"
    )

    // runMaterialize is the body of the `materialize <asset>` subcommand.
    // Default mode: synchronous — insert queued run, poll until terminal state, exit with status code.
    // --detach mode: insert queued run, print UUID, exit 0 immediately.
    func runMaterialize(args []string) error {
        fs := flag.NewFlagSet("materialize", flag.ExitOnError)
        detach := fs.Bool("detach", false, "return immediately after queueing the run")
        timeout := fs.Duration("timeout", 30*time.Minute, "synchronous-mode max wait")
        if err := fs.Parse(args); err != nil {
            return err
        }
        if fs.NArg() != 1 {
            return fmt.Errorf("usage: platform materialize [--detach] <asset>")
        }
        assetName := fs.Arg(0)

        ctx, cancel := context.WithTimeout(context.Background(), *timeout+30*time.Second)
        defer cancel()

        // JWT authentication (Phase 1 reuse). If PLATFORM_SERVICE_TOKEN is empty AND
        // PLATFORM_NO_AUTH is not "1", we refuse to proceed.
        if os.Getenv("PLATFORM_NO_AUTH") != "1" {
            tok := os.Getenv("PLATFORM_SERVICE_TOKEN")
            if tok == "" {
                return errors.New("materialize: PLATFORM_SERVICE_TOKEN is required (set PLATFORM_NO_AUTH=1 to bypass for local dev)")
            }
            if _, err := auth.ParseToken(tok); err != nil {
                return fmt.Errorf("materialize: invalid PLATFORM_SERVICE_TOKEN: %w", err)
            }
        }

        deps, err := bootstrap(ctx)
        if err != nil {
            return err
        }
        defer deps.cleanup()

        if _, err := deps.registry.Get(assetName); err != nil {
            return fmt.Errorf("materialize: asset %q not registered: %w", assetName, err)
        }

        // Insert queued run inside a single transaction; emit run.queued event.
        runID := uuid.New()
        const insertSQL = `
            INSERT INTO runs (id, asset_name, state, trigger, queued_at)
            VALUES ($1, $2, 'queued', 'manual', NOW())
        `
        if _, err := deps.store.DB().ExecContext(ctx, insertSQL, runID, assetName); err != nil {
            return fmt.Errorf("materialize: insert run: %w", err)
        }
        if err := deps.events.Append(ctx, event.Event{
            Type: event.EventTypeRunQueued, ResourceType: "run", ResourceID: runID.String(),
            Payload: event.RunQueuedPayload{AssetName: assetName, Trigger: "manual"},
        }); err != nil {
            slog.Warn("materialize.event_append_failed", "error", err)
        }

        if *detach {
            fmt.Println(runID.String())
            return nil
        }

        // Synchronous: poll runs.state until terminal.
        slog.Info("materialize.queued", "run_id", runID, "asset", assetName)
        return waitForRun(ctx, deps, runID, *timeout)
    }

    func waitForRun(ctx context.Context, deps *workerDeps, runID uuid.UUID, timeout time.Duration) error {
        deadline := time.Now().Add(timeout)
        const stateSQL = `SELECT state, COALESCE(error_message, '') FROM runs WHERE id = $1`
        ticker := time.NewTicker(500 * time.Millisecond)
        defer ticker.Stop()
        for {
            var state, errMsg string
            if err := deps.store.DB().QueryRowContext(ctx, stateSQL, runID).Scan(&state, &errMsg); err != nil {
                return fmt.Errorf("materialize: poll state: %w", err)
            }
            switch state {
            case "succeeded":
                fmt.Printf("run %s succeeded\n", runID)
                return nil
            case "failed":
                fmt.Fprintf(os.Stderr, "run %s failed: %s\n", runID, errMsg)
                return fmt.Errorf("materialize: run failed (id=%s)", runID)
            case "canceled":
                return fmt.Errorf("materialize: run canceled (id=%s)", runID)
            }
            if time.Now().After(deadline) {
                return fmt.Errorf("materialize: timeout waiting for run %s (last state=%s)", runID, state)
            }
            select {
            case <-ticker.C:
            case <-ctx.Done():
                return ctx.Err()
            }
        }
    }
    ```

    Step D — Update `cmd/platform/main.go` switch — add the two new cases:
    ```go
    case "worker":
        if err := runWorker(); err != nil {
            slog.Error("platform.worker_failed", "error", err)
            os.Exit(1)
        }
    case "materialize":
        if err := runMaterialize(os.Args[2:]); err != nil {
            slog.Error("platform.materialize_failed", "error", err)
            os.Exit(1)
        }
    ```

    Note: this requires checking that `internal/auth.ParseToken` exists in Phase 1. If the auth package has a different validation function name (e.g. `ValidateToken`, `Verify`), substitute the correct symbol — the executor MUST read internal/auth/*.go before writing this code to use the actual function name.

    Step E — Create `testdata/integration/example_assets.go` (a test-only Go file that registers two assets via the SDK):
    ```go
    package integration

    import (
        "context"

        "github.com/kanpon/data-governance/internal/asset"
        "github.com/kanpon/data-governance/internal/connector"
    )

    // RegisterTestAssets creates users_raw → users_clean using the postgres connector.
    func RegisterTestAssets() error {
        if err := asset.New("users_raw").
            Connector("postgres-test").
            Materialize(func(ctx context.Context, io asset.AssetIO) (asset.MaterializeResult, error) {
                rows := []connector.Row{
                    {Fields: map[string]any{"id": int64(1), "email": "a@b.com"}},
                    {Fields: map[string]any{"id": int64(2), "email": "c@d.com"}},
                }
                n, err := io.Write(ctx, rows)
                return asset.MaterializeResult{RowsWritten: n}, err
            }).
            Register(); err != nil {
            return err
        }
        return asset.New("users_clean").
            Upstream("users_raw").
            Connector("postgres-test").
            Materialize(func(ctx context.Context, io asset.AssetIO) (asset.MaterializeResult, error) {
                upstream, err := io.Read(ctx, "users_raw")
                if err != nil {
                    return asset.MaterializeResult{}, err
                }
                n, err := io.Write(ctx, upstream)
                return asset.MaterializeResult{RowsWritten: n}, err
            }).
            Register()
    }
    ```

    Create `testdata/integration/config.yaml`:
    ```yaml
    connectors:
      postgres-test:
        type: postgres
        dsn: ${TEST_PG_DSN}
    retry:
      default:
        max: 0
        initial_delay: 0s
        max_delay: 0s
        jitter_pct: 0
    concurrency:
      default_run_tokens: 4
    ```

    Step F — Create `test/integration/e2e_postgres_test.go`:
    Test brings up testcontainers postgres; applies migrations via `atlas migrate apply`; creates `users_raw` and `users_clean` tables; calls `RegisterTestAssets()`; then either:
    - Uses an in-process invocation: build the bootstrap deps directly, queue a run via raw SQL, run executor.Run inline. This sidesteps the need to compile and exec the platform binary.

    Use the in-process approach (faster, more reliable). Test outline:
    ```go
    package integration_test

    import (
        "context"
        "os"
        "testing"
        "time"

        "github.com/google/uuid"
        "github.com/stretchr/testify/require"
        // testcontainers + postgres + integration test asset registration

        "github.com/kanpon/data-governance/internal/asset"
        "github.com/kanpon/data-governance/internal/concurrency"
        "github.com/kanpon/data-governance/internal/connector"
        cfgpkg "github.com/kanpon/data-governance/internal/connector/config"
        pgconnector "github.com/kanpon/data-governance/internal/connector/firstparty/postgres"
        "github.com/kanpon/data-governance/internal/event"
        "github.com/kanpon/data-governance/internal/run"
        "github.com/kanpon/data-governance/internal/runtime"
        "github.com/kanpon/data-governance/internal/storage"
        "github.com/kanpon/data-governance/testdata/integration"
    )

    func TestE2E_PostgresMaterialize(t *testing.T) {
        // 1. testcontainers Postgres
        // 2. apply migrations from /migrations/*.sql via atlas
        // 3. CREATE TABLE users_raw (id BIGINT, email TEXT); CREATE TABLE users_clean (id BIGINT, email TEXT);
        // 4. Insert seed data into users_raw
        // 5. asset.Default() reset (resetForTest); RegisterTestAssets()
        // 6. Build connector.Registry, register postgres-test via factory
        // 7. Build executor with capacity {global: 4, postgres-test: 2}
        // 8. INSERT runs row state=queued; ClaimNext to transition starting; Executor.Run
        // 9. Assert runs.state = 'succeeded' and SELECT * FROM users_clean returns 2 rows
        // 10. Query event_log: assert run.queued, run.started, run.step.started (×2), run.step.succeeded (×2), run.succeeded all present
        // 11. Assert occurred_at(users_raw step.started) < occurred_at(users_clean step.started)
    }

    func TestE2E_PostgresMaterialize_Failure(t *testing.T) {
        // Same setup but register an asset whose Materialize returns errors.New("boom")
        // with RetryPolicy{Max: 1, InitialDelay: 10ms}.
        // Assert: runs.state='failed'; event_log contains run.step.failed (×2 — initial + retry),
        // run.step.retry_scheduled (×1), and run.failed (×1).
    }

    func TestE2E_TopologicalOrder(t *testing.T) {
        // users_raw → users_clean; assert run.step.started for users_raw has smaller occurred_at than users_clean.
    }

    func TestE2E_StaleRunReaperRecovery(t *testing.T) {
        // 1. Setup testcontainers Postgres + apply migrations.
        // 2. INSERT a run row with state='running', last_heartbeat = NOW() - interval '6 minutes'.
        // 3. Construct a StaleRunReaper with Interval=10s, StaleAfter=5m.
        // 4. Spawn reaper.Run in a goroutine with a ctx that we cancel at the end.
        // 5. Poll runs.state every 1s for up to 30s (10s tick + slack); assert state='queued' and
        //    last_heartbeat IS NULL and claimed_by IS NULL.
        // 6. Query event_log; assert exactly one row of type=run.canceled with payload.Reason="reaper: worker heartbeat lost".
        // 7. Cancel ctx; reaper goroutine exits.
    }
    ```
  </action>
  <verify>
    <automated>cd /home/developer/.kanpon/code/go/data-governance && go build ./... && go test ./test/integration/... -count=1 -v -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - File `cmd/platform/main.go` switch contains `case "worker":` and `case "materialize":` (verify: `grep -q "case \"worker\":" cmd/platform/main.go && grep -q "case \"materialize\":" cmd/platform/main.go`)
    - File `cmd/platform/worker.go` defines `func runWorker() error` and `func bootstrap(ctx context.Context) (*workerDeps, error)`
    - File `cmd/platform/worker.go` calls `run.ClaimNext` and `deps.executor.Run` (verify with grep)
    - File `cmd/platform/worker.go` calls `pool.ReleaseStale` at startup
    - File `cmd/platform/worker.go` constructs a `&run.StaleRunReaper{...}` and spawns its `.Run(ctx)` in a goroutine: `grep -q "&run.StaleRunReaper{" cmd/platform/worker.go && grep -q "reaper.Run(ctx)" cmd/platform/worker.go`
    - File `cmd/platform/worker.go` waits for the reaper goroutine via sync.WaitGroup before returning: verify presence of `reaperWG.Wait()` and `reaperWG.Add(1)` literals
    - File `cmd/platform/materialize.go` defines `func runMaterialize(args []string) error` with a `--detach` flag (`grep -q "detach" cmd/platform/materialize.go`)
    - File `cmd/platform/materialize.go` requires `PLATFORM_SERVICE_TOKEN` unless `PLATFORM_NO_AUTH=1` is set
    - File `cmd/platform/materialize.go` polls `runs.state` until terminal state and exits with code 1 on failure
    - File `cmd/platform/factories.go` calls `r.RegisterFactory("postgres", postgres.Factory)`
    - File `testdata/integration/example_assets.go` registers `users_raw` and `users_clean` with `users_clean.Upstream("users_raw")`
    - File `testdata/integration/config.yaml` defines connector `postgres-test` with `dsn: ${TEST_PG_DSN}`
    - File `test/integration/e2e_postgres_test.go` exists and contains: TestE2E_PostgresMaterialize, TestE2E_PostgresMaterialize_Failure, TestE2E_TopologicalOrder, TestE2E_StaleRunReaperRecovery
    - `go build ./...` exits 0
    - When docker is available, `go test ./test/integration/... -count=1 -timeout 5m` exits 0; when docker is unavailable, the test calls `t.Skip` instead of failing
    - The success test asserts that BOTH topological order AND data correctness hold (event ordering query + SELECT against users_clean)
    - The failure test asserts that event_log shows the retry sequence (run.step.failed ×2 + run.step.retry_scheduled ×1 + run.failed ×1) — directly closes acceptance criterion 2
    - TestE2E_StaleRunReaperRecovery asserts a stale 'running' row is reset to 'queued' and a `run.canceled` event with reason="reaper: worker heartbeat lost" is recorded in event_log
  </acceptance_criteria>
  <done>worker + materialize CLI subcommands compile and run end-to-end; worker spawns the stale-run reaper as part of normal startup; integration test against testcontainers Postgres proves topological execution, retry visibility in event_log, data correctness, and reaper-driven crash recovery — closing acceptance criteria 1, 2, 4 and the D-14 infrastructure-fault recovery requirement.</done>
</task>

</tasks>

<verification>
End-to-end smoke run mimicking a real user session:

```bash
cd /home/developer/.kanpon/code/go/data-governance

# 1. Compile
go build -o /tmp/platform ./cmd/platform

# 2. Bring up Postgres (developer's local instance OR testcontainers)
export DATABASE_URL=postgres://platform_owner:platform_owner@localhost:5432/platform?sslmode=disable
atlas migrate apply --env local
psql "$DATABASE_URL" -c "CREATE TABLE IF NOT EXISTS users_raw (id BIGINT, email TEXT); CREATE TABLE IF NOT EXISTS users_clean (id BIGINT, email TEXT); INSERT INTO users_raw VALUES (1,'a@b'),(2,'c@d');"

# 3. Generate a service token for the dev (Phase 1 auth)
export PLATFORM_NO_AUTH=1   # OR mint a real JWT via internal/auth and set PLATFORM_SERVICE_TOKEN

# 4. Run worker in background (worker spawns reaper goroutine internally)
export PLATFORM_CONFIG=$(pwd)/testdata/integration/config.yaml
export TEST_PG_DSN="$DATABASE_URL"
/tmp/platform worker &
WORKER_PID=$!

# 5. Trigger materialize
DATABASE_URL=postgres://platform_app:platform_app@localhost:5432/platform?sslmode=disable \
    /tmp/platform materialize users_clean

# 6. Verify
psql "$DATABASE_URL" -c "SELECT state FROM runs ORDER BY queued_at DESC LIMIT 1"
# expect: succeeded
psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM users_clean"
# expect: 2
psql "$DATABASE_URL" -c "SELECT event_type FROM event_log WHERE resource_type='run' ORDER BY occurred_at"
# expect: run.queued, run.started, run.step.started, run.step.succeeded, run.step.started, run.step.succeeded, run.succeeded

# 7. Reaper smoke (manual): simulate a crashed run and watch the reaper recover it
psql "$DATABASE_URL" -c "INSERT INTO runs (id, asset_name, state, trigger, queued_at, claimed_at, last_heartbeat) VALUES (gen_random_uuid(), 'fake_asset', 'running', 'manual', NOW() - interval '10 minutes', NOW() - interval '10 minutes', NOW() - interval '6 minutes')"
# Wait ~70 seconds for reaper to scan
sleep 70
psql "$DATABASE_URL" -c "SELECT state FROM runs WHERE asset_name='fake_asset'"
# expect: queued

# 8. Cleanup
kill $WORKER_PID
```

Map to ROADMAP acceptance criteria:
- Criterion 1 (topological order): TestE2E_TopologicalOrder validates upstream events precede downstream events.
- Criterion 2 (retry visible in event_log): TestE2E_PostgresMaterialize_Failure validates retry events with timestamps.
- Criterion 4 (CLI runs against PG): smoke run + TestE2E_PostgresMaterialize.
- D-14 infrastructure-fault recovery: TestE2E_StaleRunReaperRecovery + standalone reaper unit tests in plan task 4.2.
</verification>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invoker → platform | Anyone running `./platform materialize` could trigger arbitrary asset runs without auth |
| connector config dsn | Database credentials live in env-var-interpolated config; mishandling leaks secrets |
| testcontainers exec → host | Test bootstraps docker container; container escape would breach test runner |
| reaper goroutine → runs.state | Reaper performs the only legal backward transition; abuse path could re-queue live runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Spoofing / EoP | Anyone runs `./platform materialize <asset>` | mitigate | PLATFORM_SERVICE_TOKEN env var required; auth.ParseToken validates JWT signature (Phase 1 auth reuse). PLATFORM_NO_AUTH=1 documented as DEV ONLY in command help. |
| T-02-04-02 | Information disclosure | DSN logged to stdout via slog | mitigate | bootstrap() never logs cfg.Connectors values. config.Load resolves env vars before yaml unmarshal. |
| T-02-04-03 | Tampering | SQL injection via asset name | mitigate | quoteIdentifier rejects identifiers containing `"`; assets are user-defined names registered at init time, not user input. CHECK: TestQuoteIdentifier_RejectQuotes covers this. |
| T-02-04-04 | DoS | Worker keeps claiming runs after SIGTERM | mitigate | runWorker uses signal.NotifyContext — first signal cancels ctx; the ClaimNext / Executor.Run loop respects ctx.Err() and exits cleanly. Reaper goroutine respects the same ctx; reaperWG.Wait ensures it exits before runWorker returns. |
| T-02-04-05 | Repudiation | Materialize triggered by an actor we cannot identify | mitigate | run row records nothing about CLI invoker in Phase 2. ACCEPTED with a note: run_steps schema does NOT carry actor_id; the run.queued event_log row's actor_id is left null in CLI mode. Phase 5 audit log adds full actor tracking. |
| T-02-04-06 | DoS | Materialize CLI blocks forever on stuck run | mitigate | --timeout flag (default 30m) bounds the wait; ctx cancellation propagates to executor. |
| T-02-04-07 | Information disclosure | Run failure error printed to stderr might leak DB internals | accept | Errors come from MaterializeFunc which is user-authored — same trust as in-process Go code. Future phases may sanitize. |
| T-02-04-08 | DoS | Worker process dies mid-execution → run is stuck in 'starting'/'running' indefinitely | mitigate | StaleRunReaper goroutine (task 4.2 + worker.go spawn) runs every 60s, resets stuck runs (last_heartbeat < NOW() - 5m) to 'queued' via run.TransitionForReset. Recovery upper bound: ~6 minutes. Implements D-14 Option B; River.max_attempts deferred. TestE2E_StaleRunReaperRecovery proves the integration. |
| T-02-04-09 | EoP | Reaper takes a backward edge that the FSM forbids → corrupts state | mitigate | SweepOnce calls run.TransitionForReset(currentState, StateQueued) which permits ONLY {starting,running}→queued; any other input returns ErrIllegalTransition and the row is skipped (logged at WARN). |
| T-02-04-10 | DoS | Reaper false-positive on a slow-but-live worker → re-queues a live run | mitigate | Heartbeat threshold (5m) is 10× the executor's tick interval (30s). Filtered UPDATE `WHERE state IN ('starting','running') AND last_heartbeat < $cutoff` is idempotent under the race: a live worker that just heartbeated will not match the WHERE clause. |

</threat_model>

<success_criteria>

- [ ] PostgreSQL connector compiles and passes ping/schema/read/write/close/factory/ctx-cancel tests against testcontainers Postgres
- [ ] StaleRunReaper exists in internal/run/reaper.go with SweepOnce + Run methods
- [ ] Reaper validates the FSM edge via run.TransitionForReset before each UPDATE
- [ ] Reaper emits run.canceled event with reason="reaper: worker heartbeat lost"
- [ ] Reaper unit tests cover: stale-row reclamation, starting-state eligible, terminal-state ignored, event emitted, concurrent-safe, goroutine ticks + exits cleanly
- [ ] cmd/platform/main.go switch dispatches worker and materialize cases
- [ ] worker subcommand: ReleaseStale at startup, ClaimNext loop, executor.Run dispatch, graceful SIGTERM shutdown, AND spawns StaleRunReaper goroutine
- [ ] materialize subcommand: synchronous default + --detach, JWT auth via PLATFORM_SERVICE_TOKEN (Phase 1 reuse), --timeout, polls runs.state
- [ ] factories.go registers `postgres` factory; structure ready for plan 02-05 to add six more
- [ ] testdata/integration/example_assets.go registers users_raw → users_clean with the SDK
- [ ] testdata/integration/config.yaml uses ${TEST_PG_DSN} placeholder
- [ ] test/integration/e2e_postgres_test.go has TestE2E_PostgresMaterialize, TestE2E_PostgresMaterialize_Failure, TestE2E_TopologicalOrder, TestE2E_StaleRunReaperRecovery
- [ ] All four e2e tests pass against testcontainers Postgres; skip gracefully when docker unavailable
- [ ] go build ./... exits 0
- [ ] go vet ./... exits 0

</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-engine/02-04-SUMMARY.md` documenting:
- Postgres connector API surface and pgxpool lifecycle
- StaleRunReaper contract: SweepOnce semantics, idempotency, FSM gating via TransitionForReset, event emission
- worker subcommand polling cadence, shutdown semantics, AND reaper goroutine spawn
- materialize subcommand auth strategy + flag surface
- e2e test results (which acceptance criteria each closes; reaper recovery proven by TestE2E_StaleRunReaperRecovery)
- D-14 implementation note: Option B (heartbeat + reaper) chosen over River.max_attempts; River wiring deferred to a later phase when out-of-process job dispatch becomes a hard requirement
- Hooks for plan 02-05 (factories.go is the entry point for the six new factories)
</output>